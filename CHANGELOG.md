# 鸿蒙端端开发日志 (CHANGELOG)

记录本项目的开发进度与修改历史。


## 2026.05.01

- **15:48** `chore(api23-complete): 根据“API23 适配已全面完成”的确认，删除临时追踪文档 API23_UIDESIGNKIT_ADAPTATION.md，并从 .gitignore 中移除该文档的放行规则；API23 相关长期真值回归 CHANGELOG.md、README.md 与当前代码。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（17 s 323 ms，仅既有 warn）。next: 提交并推送 GitHub；后续进入常规真机验收、缺陷修复与体验微调，不再维护 API23 临时追踪文档。`
- **15:39** `fix(api23-topic-reply-sheet): 补齐上轮半模态适配遗漏的帖子详情页“回复话题/回复楼层”：TopicDetailPage 的 isReplySheetVisible bindSheet 从 height: SheetSize.LARGE + dragBar:false 改为 detents: [SheetSize.MEDIUM, SheetSize.LARGE] + enableFloatingDragBar:true，默认不再接近全屏且可上拉展开；ReplyComposerSheet 新增 showDragHandle 参数，详情页传 false，避免原生半模态拖动柄和自绘拖动柄重复；保留回复话题、回复楼层、编辑回复、恢复草稿、发送/舍弃、图片上传和表情面板逻辑。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（26 s 169 ms，仅既有 warn）。next: 真机抽查帖子详情页点击底部回复 FAB、楼层更多菜单“回复”、编辑回复入口，确认默认半模态高度、上拉展开/下拉关闭、输入法弹出、草稿恢复、发送/舍弃和表情/图片上传均正常；若默认编辑区偏矮，只单独微调回复 Sheet detents。`
- **15:27** `feat(api23-fab-glass): 适配首页与详情页两个 FAB：新增 FloatingActionChromeHelper 统一 FAB 尺寸、边距、圆角、blur、saturation/brightness 与阴影参数；首页 FAB 与详情页回复 FAB 从 56vp 收敛到 52vp，并同步更新左右手握持位移和底部 HDS 胶囊避让；首页 FAB 展开菜单从实体 surface 改为资源化 fab_menu_glass_tint/stroke + backgroundEffect 玻璃菜单项；明暗两套 fab_glass_tint/stroke 调轻，保证背景轻微透出但图标/文字可读。docs: API23_UIDESIGNKIT_ADAPTATION.md 新增 HDS23-15 记录。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（21 s 210 ms，仅既有 warn）。next: 真机抽查首页 FAB/展开菜单、详情页回复 FAB、左右手握持位置与底部 HDS 胶囊避让；若 FAB 仍偏实或偏弱，只微调 fab_glass_tint / fab_menu_glass_tint alpha，不改位置逻辑。`
- **14:53** `feat(api23-home-sheets): 继续首页体验适配，把首页标签筛选、浏览/编辑分类、创建话题从近全屏上拉页改成通知同款半模态：TopicListPage 的标签/分类共用 Sheet 与创建话题 Sheet 改为 detents: [SheetSize.MEDIUM, SheetSize.LARGE] + enableFloatingDragBar，默认不再接近全屏且可继续上拉展开；TagSelectionSheet 新增 showDragHandle 参数，首页标签筛选和创建话题内标签选择关闭自绘拖动柄，避免与原生半模态拖动柄重复；首页分类 Sheet 与 CreateTopicComposerSheet 内分类选择移除自绘拖动柄；创建话题内部分类/标签 Sheet 也同步半模态化。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（18 s 365 ms，仅既有 warn）。next: 真机抽查首页标签筛选、浏览/编辑分类、创建话题默认高度是否类似通知半模态；确认上拉展开/下拉关闭、输入法弹出、标签确定/清空、分类编辑/移除/排序、创建话题发布/舍弃/草稿恢复链路都正常；若创建话题默认编辑空间偏小，只单独微调创建话题 detents。`
- **14:04** `feat(api23-home-header): 按最新方向不回到底部悬浮 Tab 微调，转为适配首页顶部 Header：新增 TopicListChromeHelper 统一首页状态栏安全区、搜索栏/Tab/筛选栏高度、动态 spacer、blur 与控件尺寸；TopicListPage 根布局改为 Stack 顶部 overlay，移除外层 WindowManager 顶部 padding，搜索 / 分类 Tab / 筛选 Header 由 overlay 承接状态栏并接入 backgroundEffect；列表、加载态、错误态增加动态顶部 spacer，首屏不被 Header 遮挡，滚动后内容可进入 Header 背后；搜索框、通知铃铛、分类管理按钮与折叠筛选入口改用 topic_list_* 资源化 tint/stroke + 轻玻璃材质，保留原折叠 snap、底部 HDS Tabs 显隐、首页 FAB、通知半模态、分类/标签 Sheet 与下拉刷新逻辑。docs: API23_UIDESIGNKIT_ADAPTATION.md 新增 HDS23-13 首页顶部 Header 定制记录。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（15 s 290 ms，仅既有 warn）。next: 真机抽查首页：首屏话题是否不被顶栏遮挡、搜索栏/筛选栏折叠和 snap 是否保持、列表滚入 Header 背后是否有轻微透出、搜索/通知/分类/标签/下拉刷新是否正常；若顶栏偏实，仅微调 topic_list_top_bar_tint / topic_list_search_field_tint / topic_list_action_tint alpha。`
- **13:49** `feat(api23-profile-page): 继续复杂自绘页下一步，处理 ProfilePage 主 Tab 顶栏：不把主 Tab 强套 HdsNavDestination titleBar，新增 ProfilePageChromeHelper 统一状态栏安全区、顶栏高度、内容顶部 spacer、blur 与按钮胶囊尺寸；ProfilePage 根布局改为 Stack 顶栏 overlay，Scroll 内容顶部增加占位，首屏不遮挡且上滑时头像/卡片可进入顶栏背后形成沉浸光感；移除根容器 top padding，顶栏自身承接状态栏区域；设置/通知胶囊从实体 card 背景改为资源化 profile_top_action_tint/stroke + backgroundEffect，顶栏背景改为 profile_top_bar_tint + backgroundEffect。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（19 s 511 ms，仅既有 warn）。next: 真机抽查“我的”页登录/游客两态：状态栏区域是否连续、首屏头像是否不被顶栏遮挡、滚动时内容是否能透到顶栏背后、设置/通知胶囊是否不过实、通知半模态入口是否仍正常；若顶栏偏实，只微调 profile_top_bar_tint / profile_top_action_tint alpha。`
- **13:33** `feat(topic-image-preview): 在继续下一步前补齐话题详情“该楼层图片”左右滑切换：PostRichContent 的图片长按事件从单图 URL 扩展为 previewUrls + initialIndex；普通点击/嵌套富文本图片预览优先映射到当前楼层完整图片数组，ImagePreviewPage 继续复用既有 Swiper 左右滑；TopicDetailPage 的长按 Sheet“查看大图”不再 push [previewUrl] 单图，而是保存楼层图片集合并从当前图片 index 打开；复制/分享/引用仍只使用当前图片 URL。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（26 s 721 ms，仅既有 warn）。next: 真机用同一楼层多图帖子验收：点击任一图片进入后应可左右滑切换该楼层图片，缩放放大时不误触切图，长按菜单“查看大图”也能从当前图开始滑；若嵌套引用/折叠详情内图片仍只出单图，再补解析/映射兜底。`
- **13:09** `feat(api23-image-preview): 进入复杂自绘页下一步，处理 ImagePreviewPage 全屏预览：保留全屏黑底 Swiper + ZoomableImage，不套普通 HDS titleBar；新增 ImagePreviewChromeHelper 统一顶部安全区、浮动控件尺寸/半径/blur 与白色状态栏图标色；HdsNavDestination 接入 ignoreLayoutSafeArea(TOP/BOTTOM) 和 systemBarStyle，避免全屏预览被系统安全区截断；返回/保存按钮与多图计数胶囊从硬编码 #80000000 改为资源化 image_preview_control_tint/stroke/shadow/icon + backgroundEffect 轻玻璃材质，保持可读又能透出图片内容；ActionSheet 业务保持不变。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（20 s 362 ms，仅既有 warn）。next: 真机抽查图片预览：状态栏白字、返回/保存按钮不压挖孔且能透出图片、多图左右滑/缩放禁用滑动、长按 Sheet、保存/分享/复制链接；若按钮仍偏实，只微调 image_preview_control_tint alpha。`
- **12:58** `style(api23-topic-detail): 根据真机反馈“太实了”微调 TopicDetailPage 顶栏沉浸光感透明度：仅调整资源色，不改 overlay 结构和阅读/搜索逻辑；浅色 topic_detail_top_bar_tint 从 #A8F8FAFC 降到 #66F8FAFC，topic_detail_search_field_tint 从 #8AFFFFFF 降到 #52FFFFFF，同时弱化 divider/stroke；暗色 topic_detail_top_bar_tint 从 #8A24262B 降到 #5C24262B，topic_detail_search_field_tint 从 #662A2A2A 降到 #472A2A2A。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（18 s 138 ms，仅既有 warn）。next: 真机复看 TopicDetailPage 顶栏滚动透出和标题/按钮可读性；若仍偏实，下一轮再小幅降 top_bar_tint alpha；若透出过头或标题不清，再只回调 top_bar_tint 一个档位。`
- **12:47** `feat(api23-topic-detail): 继续 TopicDetailPage 定制沉浸光感：把阅读 TopBar / 话题内搜索 TopBar 从文档流移到 Stack 顶部 overlay，HdsNavDestination 接入 ignoreLayoutSafeArea(TOP/BOTTOM) 与 systemBarStyle；主帖子 List 和话题内搜索 List 顶部增加 TopicDetailChromeHelper.getContentTopSpacerHeightVp() 占位，首屏不遮挡、滚动后正文可进入顶栏背后；同步把帖子列表索引映射/底部判断从 1 调整为 2，跳楼/初始定位/加载更早回复锚点后补 scrollBy 反向避让，避免目标帖被顶栏盖住。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（21 s 580 ms，仅既有 warn）。next: 真机重点抽查 TopicDetailPage 首屏话题头间距、滚动时文字透过顶栏的光感、跳转指定楼层/搜索结果跳楼是否不被顶栏遮挡、加载更早回复锚点是否稳定；若顶栏偏实或阅读受干扰，再只微调 topic_detail_top_bar_tint / topic_detail_search_field_tint alpha。`
- **12:10** `feat(api23-topic-detail): 继续复杂自绘页轻量适配，处理 TopicDetailPage 阅读顶栏/话题内搜索顶栏：不套普通 HDS titleBar，保留阅读顶栏、话题内搜索、底部 HdsActionBar、进度胶囊和回复 FAB 联动；新增 TopicDetailChromeHelper 统一顶部安全区、56vp 顶栏高度、搜索框高度、blur 半径与状态栏图标色；移除主体 Column 外层 WindowManager.getSystemAvoidAreaTopVp() 顶部 padding，改由 TopBar / TopicSearchTopBar 内部 Blank(topSafe) 承接状态栏，减少透明状态栏露白；两种顶栏均接入 backgroundEffect + topic_detail_top_bar_tint，话题内搜索输入框改用 topic_detail_search_field_tint/stroke 轻玻璃材质。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（25 s 434 ms，仅既有 warn）。next: 真机抽查帖子详情页展开/滚动标题、话题内搜索顶栏、状态栏背景连续、返回/搜索/更多动作、底部栏显隐与回复 FAB；若顶部 tint 偏实或不够透，只微调 topic_detail_top_bar_tint / topic_detail_search_field_tint alpha，不改阅读页结构。`
- **12:00** `fix(api23-profile-hero): 根据真机截图优化 UserProfilePage 顶部折叠栏按钮：确认问题出在上一轮把 44vp 圆形本体直接作为视觉按钮，导致圆底过大、右侧搜索/私信或更多视觉距离太近，且 disableSystemAdaptation=false 让按钮底色偏实体灰，背景透出弱。本轮保留 44vp 点击热区，但把可见玻璃圆面缩到 36vp、图标缩到 16vp；右侧按钮继续按 44vp 热区排布，但可见圆面之间自然留出 8vp 间隔；ProfileHeroChromeHelper 新增 touch/visual/icon/padding helper，按钮 backgroundEffect 改为更高 blur/saturation/brightness 并关闭系统适配，profile_hero_button_tint/stroke 下调透明度，让头图内容能透出。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（20 s 105 ms，仅既有 warn）。next: 真机复看用户主页展开态和折叠态：按钮视觉圆面应变小、右侧两个按钮不再粘连、圆面应能透出背景图；若仍偏灰，只继续调 profile_hero_button_tint alpha / blur radius，不改折叠结构。`
- **11:43** `feat(api23-uidesignkit): 继续复杂自绘页第一步处理 UserProfilePage：保留 List + 自绘折叠 Hero / pinned Tabs / pinned TopBar，不套普通 HDS titleBar；新增 ProfileHeroChromeHelper 统一顶部/底部安全区、高度、折叠遮罩 alpha、状态栏白字与顶部按钮材质参数；TopBarOverlay 的返回/搜索/私信/更多按钮抽成 HeroTopBarButton / HeroTopBarMenuButton，接入 backgroundEffect、资源化 profile_hero_button_tint/profile_hero_button_stroke 和轻阴影；TabRow 背板/分割线改用 profile_hero_tab_surface/profile_hero_tab_divider，用户信息 Sheet 底部避让改用 helper；清理 TOP_BAR_HEIGHT/TAB_HEIGHT 残留和 computeMaskAlpha 死代码。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步把 UserProfilePage 标成已完成第一步待真机抽查。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（20 s 599 ms，仅既有 warn）。next: 真机抽查用户主页顶部按钮玻璃材质、状态栏白字、折叠标题/Tab 停靠、搜索/私信/更多菜单和用户信息 Sheet 底部避让；若稳定，下轮进入 TopicDetailPage 阅读顶栏/话题内搜索顶栏的轻量 API23 材质化。`
- **11:09** `feat(api23-uidesignkit): 按复杂页评估优先处理 CfChallengePage：独立页模式不再渲染自绘 TopBar，改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBarWithMenu(安全验证/重载验证页)，接入 HDS 官方返回键与右侧重载动作；通过 .onBackPressed(() => { this.onCancel(); return true }) 接管系统/标题栏返回，确保仍会调用 CfChallengeService.complete(false) 而不是只 pop 页面；overlayMode 继续走原内嵌 TopBar，避免破坏全局 CF 浮层；新增 cf_challenge_title / cf_challenge_reload / cf_challenge_reload_page 字符串资源。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步把 CfChallengePage 标记为已实施待真机抽查。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（19 s 259 ms，仅既有 warn）。next: 真机触发 CF 验证分别抽查独立页/overlay：标题栏状态栏不重叠、返回会取消验证并恢复请求、重载按钮可刷新 Web、验证成功仍能关闭页面/浮层；若稳定，下轮处理 UserProfilePage 的折叠 Hero 安全区/材质 helper。`
- **11:00** `docs(api23-complex-pages): 根据你确认“搜索页已经没问题”，将 HDS23-07 后续从 SearchPage 收口转入复杂自绘页评估：阅读代码后把剩余复杂页分层处理——CfChallengePage 最适合作为下一步，按 WebView/Login 同类壳迁移独立页 HDS 标题栏但保留 overlay 模式；UserProfilePage 保留折叠 Hero / pinned Tabs 自绘，只抽安全区与材质 helper；TopicDetailPage 保留阅读顶栏、话题内搜索顶栏和底部 HdsActionBar 联动，不做第一批 HDS titleBar 硬替换；ImagePreviewPage 保持全屏黑底浮动控件；ProfilePage 作为主 Tab 顶栏暂不纳入子页迁移。docs: API23_UIDESIGNKIT_ADAPTATION.md 新增“复杂自绘页评估结论”，README 环境要求同步从 HarmonyOS SDK 6.0.2(API 22) 更新到 6.1.0(API 23)。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（1 s 850 ms）。next: 优先实现 CfChallengePage 独立页 HDS 标题栏/overlay 兼容方案，再处理 UserProfilePage / TopicDetailPage 的材质与安全区统一。`
- **10:54** `docs(api23-status): 本轮按会话协议回看 API23 UIDesignKit 适配进度：确认 build-profile.json5 已切到 targetSdkVersion/compatibleSdkVersion=6.1.0(23)，HDS23-01/02/03 已收口，HDS23-07 正在推进且最新卡点集中在 SearchPage 最近搜索首屏布局；API23_UIDESIGNKIT_ADAPTATION.md 最新记录到 HDS23-07-search-page-recent-direct-layout，当前 next 仍是先真机/预览确认搜索页是否已运行最新包并消除“大标题/大空白”旧画面，再决定是否加临时色块诊断。验证：本轮仅阅读 CHANGELOG/API23 追踪文档并用 ace-tool 检索相关实现，未改业务代码，未运行构建。next: 搜索页复测稳定后，继续评估 ImagePreviewPage / TopicDetailPage / UserProfilePage / CfChallengePage 等复杂自绘 TopBar 页，并同步修正 README 环境要求从 API22 到 API23。`
- **10:48** `fix(search-layout): 根据你最新反馈“还是这样”，继续把搜索页最近搜索从复杂滚动布局里剥离：新增 shouldRenderRecentSearchesDirectly()，当无搜索条件、无错误、非加载且存在最近搜索时，SearchTopBar 后直接渲染 RecentSearchesView + 底部 Blank，不再进入 Scroll(layoutWeight=1) 分支，彻底绕开短内容在 Scroll/HDS 容器里被居中或复用旧布局的风险；搜索结果、加载态、空态、错误态仍保留原 Scroll 流程。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat clean assembleHap --no-daemon BUILD SUCCESSFUL（40 s 337 ms，仅既有 warn）。next: 你真机/预览重新运行搜索页；若仍看到“大标题 搜索 + 搜索框在中部”的旧画面，优先清理 DevEco Preview/Run 缓存或卸载重装，因为当前编译产物已是 hideTitleBar(true)+SearchTopBar；若仍是新顶栏但最近搜索下沉，我再加临时色块定位具体占位层。`
- **02:27** `fix(search-layout): 根据你反馈“依然有非常大的空白”，继续收口搜索页布局：确认单纯限制 safeArea 仍不够，根因是 overlay + spacer 模式本身容易在搜索这种短内容页把空白显性化。本轮将 SearchPage 恢复为旧版同款正常文档流：Column 内先放 SearchTopBar，再放 Scroll(contentScroller) 作为 layoutWeight(1) 的内容区；彻底删除 Scroll 内容顶部 Blank/spacer 和覆盖层 SearchTopBar。这样“最近搜索”不再依赖任何占位计算，会直接跟在搜索栏下面；定制顶栏仍保留 statusBar + 56vp、高亮/搜索/筛选动作和 backgroundEffect 光感。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 969 ms，仅既有 warn）。next: 你真机复测搜索页，最近搜索上方应只剩正常搜索顶栏高度；若仍有空白，说明当前运行包不是最新或还有外层导航缓存，我再加临时可见调试色块定位哪一层在占位。`
- **02:20** `fix(search-layout): 继续修搜索页“最近搜索上面仍有大空白”：定位到定制 SearchTopBar 和内容 spacer 仍直接使用 WindowManager.getSystemAvoidAreaTopVp()，部分真机场景下该值可能异常偏大，导致顶栏内部 Blank 和 Scroll 顶部占位同步被拉高。本轮给 SearchPage 单独增加 getSearchTopSafeHeightVp()，将搜索页顶部安全区限制在 0~48vp，再用该值计算 SearchTopBar 总高度与内容顶部 spacer；不改全局 WindowManager，避免影响已验收页面。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（15 s 34 ms，仅既有 warn）。next: 你真机复测搜索页，最近搜索标题应贴近顶栏下方，仅保留正常状态栏+56vp 顶栏高度；若还有空白，我下一步改为完全不依赖 WindowManager、固定状态栏占位为截图口径。`
- **02:13** `fix(search-immersive): 根据你发的 09b4066f 截图回看搜索页，评估结论是 SearchPage 不适合直接套普通 HDS titleBar：HDS 标题栏只能稳定承载“返回 + 标题 + 菜单”，会把原本应在顶部的一体化搜索框挤到内容区，导致首屏出现大块空白。已回收 02:01 的“官方标题 + 内容区搜索条”方案，改为定制 SearchTopBar：HdsNavDestination 继续 hideTitleBar(true)，页面内覆盖 statusBar + 56vp 的搜索顶栏（返回 + TextInput + 清空 + 搜索 + 筛选），Scroll 内容顶部 spacer 精确改为状态栏 + 56vp；SearchTopBar 使用 backgroundEffect(radius=48, tab_glass_tint, ALWAYS_ACTIVE) + 底部分割线承接沉浸光感，恢复你图里那种紧凑搜索栏布局，同时仍允许内容滚到顶栏背后。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 695 ms，仅既有 warn）。next: 你真机复测搜索页顶部是否恢复成“返回 + 搜索输入 + 搜索/筛选”的紧凑样式，首屏最近搜索不再被推到下半屏；滚动搜索结果时顶栏背后是否有轻微光感/透字；若还想更像原图，我再只微调顶栏 tint/blur/分割线，不再回到普通 HDS 标题。`
- **02:01** `feat(api23-uidesignkit): 继续 HDS23-07，把 SearchPage 迁到浏览历史同款沉浸标题栏：删除自绘 TopBar/RouterManager.pop 返回按钮与外层顶部安全区 padding，改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBar('搜索') + HDS 官方返回键；原搜索输入区改为内容顶部 SearchInputBar，保留清空、执行搜索、高级筛选入口；滚动主体从嵌套 List 结果流收敛为 Scroll(this.contentScroller)+Column，顶部插入状态栏 + 56vp + maskExtraHeight spacer，并接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle、EdgeEffect.Spring 与底部安全区避让。高级筛选 Sheet、更多标签 Sheet、自定义日期范围、最近搜索、排序、分页加载更多、帖子跳转与用户跳转均保留。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 445 ms，仅既有 warn）。next: 你真机抽查“搜索”：官方返回键/标题不压状态栏、搜索输入条首屏不被遮挡、结果滚动进入标题栏背后有光感，清空/搜索/高级筛选/标签/日期/排序/加载更多/帖子跳转/用户跳转均正常；若稳定，下轮评估 ImagePreviewPage（全屏预览保留黑底与浮动控件）或 TopicDetailPage / UserProfilePage 这类更复杂页。`
- **01:40** `feat(api23-uidesignkit): 继续 HDS23-07，处理 WebViewPage / LoginPage 两个 ArkWeb 特殊页的自绘 TopBar：新增 ImmersiveHdsTitleBarHelper.buildImmersiveTitleBarWithTwoMenus(...) 支持 HDS 标题栏双右侧菜单；WebViewPage 删除自绘 TopBar、RouterManager.pop 返回按钮与外层顶部安全区 padding，改用 HdsNavDestinationTitleMode.MINI + HDS 官方返回键 + 双菜单（刷新 / 在浏览器打开），并在内容顶部插入状态栏 + 56vp + maskExtraHeight spacer，页面标题同步 Web onTitleReceive；LoginPage 删除自绘 TopBar 与外层顶部 padding，改用同款 HDS 标题栏并保留右侧刷新登录页动作，登录成功后的 RouterManager.pop 业务返回保留。两页均接入 ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle 与官方返回键；由于 ArkWeb 内部滚动不是 ArkUI Scroller，本轮不强行 bindToScrollable，避免伪绑定破坏 Web 交互。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（16 s 314 ms，仅既有 warn）。next: 你真机抽查“网页 / 登录 Linux.do”：状态栏连续、官方返回键/标题/右侧按钮不压状态栏，Web 内容首屏不被遮挡；WebView 刷新/外部浏览器打开可用，登录页刷新、自动填充、登录成功返回仍正常；若稳定，下轮继续 SearchPage（搜索框自定义标题栏，需谨慎单独设计）或全屏 ImagePreviewPage。`
- **01:30** `feat(api23-uidesignkit): 继续 HDS23-07，把 CategoryTopicPage（分类话题页）迁到浏览历史同款沉浸标题栏：删除自绘 TopBar、RouterManager.pop 返回按钮与外层状态栏顶部 padding，改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBarWithMenu(分类名, 搜索图标, ...)，保留右侧搜索入口；滚动主体从 List(listScroller) 改为 Scroll(this.contentScroller)+Column，顶部插入状态栏 + 56vp + maskExtraHeight spacer，并接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle、EdgeEffect.Spring 与底部导航栏安全区避让。分类筛选条、标签 Sheet、下拉刷新、触底加载更多、置顶紧凑卡、话题预览 Overlay 与跳转详情均保留。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 806 ms，仅既有 warn）。next: 你真机抽查任意分类话题页：状态栏连续、分类名/返回/搜索不压状态栏、筛选条首屏不被遮挡、下滑话题卡进入标题栏背后有光感、筛选/标签/下拉刷新/加载更多/预览跳转是否正常；若稳定，下轮继续处理 WebViewPage / LoginPage / SearchPage 等复杂自绘 TopBar 页。`
- **01:20** `feat(api23-uidesignkit): 继续 HDS23-07，小批次迁移 InviteLinksPage / OpenSourceInfoPage 两个仍自绘 TopBar 的普通滚动页：两页均删除自绘 TopBar 与 RouterManager 返回依赖，改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBar(...)；滚动主体分别绑定 contentScroller，顶部插入状态栏 + 56vp + maskExtraHeight spacer，并接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle、EdgeEffect.Spring 与底部安全区避让。邀请链接页保留邀请生成、有效期/高级选项、复制/分享、最近邀请结果；开源信息页保留分组列表与外链打开。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 835 ms，仅既有 warn）。next: 你真机抽查“邀请链接 / 开源信息”：状态栏连续、首屏内容不被标题栏遮挡、滚动时内容进入标题栏背后有光感、邀请复制/分享和开源外链点击是否正常；若稳定，下轮继续清点剩余自绘 TopBar 页（如登录/搜索/分类/详情等复杂页需谨慎分批）。`
- **01:09** `feat(api23-uidesignkit): 继续 HDS23-07，把 BadgeDetailPage（徽章详情页）迁到 HDS 沉浸标题栏：沿用 MyBadgesPage 的 Hero 页方案，保留 HeroSection 的状态栏承接高度（HERO_HEIGHT + topSafeHeight），删除自绘 TopBar 与手写返回按钮，改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBar(徽章名/徽章兜底标题)；滚动主体绑定 this.contentScroller，接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle 与 EdgeEffect.Spring，让徽章头图/详情卡片可在滚动时进入标题栏背后。原有徽章图标/渐变头图、徽章说明、长描述富文本、获得者列表、点击获得记录跳转话题或用户主页、下拉刷新与底部安全区避让均保留。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 981 ms，仅既有 warn）。next: 你真机抽查徽章详情页：首屏 Hero 是否仍铺进状态栏、官方返回键/标题是否不遮挡、徽章名加载后标题是否更新、滚动时头图/文字进入标题栏背后是否有光感、获得者点击跳转是否正常；若稳定，下轮继续清点剩余自绘 TopBar 页。`
- **01:00** `fix(trust-level): 顺手修复信任要求页“合规记录”中被禁言/被封禁两项的文本对齐：原 vetos 行把 label 放进带 layoutWeight 的 Column，Column 默认居中导致最后两项文字在剩余区域内居中；本轮改为与被举报帖子/举报用户 quota 行一致，直接让 Text(veto.label) 承载 layoutWeight + left margin，确保四项标签统一左对齐。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（17 s 734 ms，仅既有 warn）。next: 你真机回看信任要求页合规记录，确认“被举报帖子 / 举报用户 / 被禁言 / 被封禁”四项标签左边缘一致；若稳定，下轮继续 BadgeDetailPage。`
- **00:54** `feat(api23-uidesignkit): 继续 HDS23-07，把 TrustLevelPage（信任等级页）迁到浏览历史同款沉浸标题栏：删除自绘 TopBar、RouterManager 返回依赖与手写顶部安全区占位，改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBar(trust_level_page_title)；信任等级内容 Scroll 绑定 this.contentScroller，顶部 spacer 改为状态栏 + 56vp + maskExtraHeight，底部补 40vp + 导航栏安全区，并接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle、edgeEffect(Spring)。隐藏 WebView 加载/CF/SSO 解析链路、加载态、错误重试、下拉刷新、HeaderPanel/ContentSection/FooterSection 均保留。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 701 ms，仅既有 warn）。next: 你真机抽查“信任等级”：加载/CF 验证后原生内容是否正常出现、状态栏连续、首屏卡片不被标题栏遮挡、滚动时卡片文字进入标题栏背后有光感、官方返回键是否正常；若稳定，下轮处理 BadgeDetailPage。`
- **00:44** `feat(api23-uidesignkit): 继续 HDS23-07，把 FollowListPage（关注 / 粉丝页）迁到浏览历史同款沉浸标题栏：删除自绘 TopBar 与手写顶部安全区 padding，保留官方返回键；标题栏改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBar(动态用户名标题)，滚动主体从 List 改为 Scroll(this.contentScroller)+Column，顶部插入状态栏 + 56vp + maskExtraHeight spacer，并接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle、edgeEffect(Spring)。原有加载态、错误重试、空态、用户卡片点击进入他人主页、下拉刷新与底部安全区避让均保留。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（16 s 168 ms，仅既有 warn）。next: 你真机抽查任意用户主页里的关注/粉丝列表：状态栏连续、首屏用户卡片不被标题栏遮挡、下滑内容进入标题栏背后有光感、官方返回键与用户卡片点击是否正常；若稳定，下轮继续处理 TrustLevelPage。`
- **00:19** `fix(notification-half-modal): 按你指出“通知不是沉浸页，而应优化为半模态”纠正方向：Playwright 复核华为官方《半模态样式-组件导航-UI Design Kit（UI设计套件）-应用框架》（更新时间 2026-04-29 15:35），确认半模态内推荐使用 HdsNavigation + HdsNavigationTitleMode.MODAL + bindToScrollable，并关闭系统 Sheet close、使用 HDS Navigation 关闭按钮。本轮将 NotificationQuickPanel 从自绘标题/普通大 Sheet 改为官方半模态导航结构：组件内部使用 HdsNavigation().titleMode(MODAL)，titleBar 标题“通知”并配置 xmark 关闭按钮，Scroll(this.modalScroller) 绑定 bindToScrollable；“全部已读 / 查看全部”保留为半模态内容区顶部胶囊动作。首页 TopicListPage 与我的 ProfilePage 的通知 bindSheet 改为 detents: [MEDIUM, LARGE] + enableFloatingDragBar:true + showClose:false。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（18 s 112 ms，仅既有 warn）。next: 你真机点首页/我的右上角铃铛：确认弹层是可拖拽半模态、顶部为 HDS 半模态标题栏和关闭按钮、列表滚动标题栏有动态模糊；若稳定，我再决定是否保留/弱化独立 NotificationsPage 的全页入口。`
- **00:03** `feat(api23-uidesignkit): 继续 HDS23-07，把 NotificationsPage 迁到浏览历史同款沉浸标题栏，并保留右侧“全部已读”动作：ImmersiveHdsTitleBarHelper.buildImmersiveTitleBarWithMenu(...) 新增 menuEnabled 可选参数，内部透传 HdsNavigationIconOptions.isEnabled；NotificationsPage 删除自绘 TopBar 与手写顶部安全区 padding，标题栏改为 HdsNavDestinationTitleMode.MINI + buildImmersiveTitleBarWithMenu(tab_notifications, checkmark/arrow_clockwise, 全部标为已读/正在标为已读, markAllAsRead, enabled)，空列表或标记中时禁用菜单；滚动主体从 List 改为 Scroll(this.contentScroller)+Column，顶部插入状态栏 + 56vp + maskExtraHeight spacer，并接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle、edgeEffect(Spring)。原有下拉刷新、触底加载更多、单条通知点开后标已读/跳转、全部已读、Toast 与本地未读数同步逻辑均保留。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（15 s 672 ms，仅既有 warn）。next: 你真机抽查“通知”：状态栏连续、首屏通知不被标题栏遮挡、下滑内容进入标题栏背后有光感、右侧全部已读在空列表/标记中是否禁用且正常回显；若稳定，下轮继续处理 BadgeDetailPage / TrustLevelPage / FollowListPage 这类仍自绘 TopBar 的详情/列表页。`

## 2026.04.30

- **23:53** `feat(api23-uidesignkit): 继续 HDS23-07，把 MyTopicsPage / BookmarksPage 两个列表页迁到浏览历史同款沉浸标题栏：删除自绘 TopBar、手写顶部安全区 padding 与 List 外层避让；标题栏改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBarWithMenu(...)，保留右侧搜索动作（分别进入 created/bookmarks 搜索范围）；滚动主体从 List 改为 Scroll(this.contentScroller)+Column，顶部插入状态栏 + 56vp + maskExtraHeight spacer，并接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle、edgeEffect(Spring)。两页原有下拉刷新、触底加载更多、空态、错误态、TopicPreviewOverlay、Bookmarks 的编辑 Sheet/删除逻辑均保留。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（15 s 925 ms，仅既有 warn）。next: 你真机抽查“我的话题 / 我的书签”：状态栏连续、首屏第一条不被标题栏遮挡、下滑内容进入标题栏背后有光感、右侧搜索按钮可用；若稳定，下轮继续处理 NotificationsPage（右侧“全部已读”带 loading/禁用态，需先给 helper 补可禁用/动态菜单能力或单独保留局部自绘）。`
- **23:40** `feat(api23-uidesignkit): 继续 HDS23-07，给 MyBadgesPage 这类 Hero 头图页落地“头图承接状态栏 + HDS 沉浸标题栏覆盖”的单独方案：保留 HeroSection 自身的状态栏延伸高度（HERO_HEIGHT + topSafeHeight），不额外插入普通页面顶部 spacer，删除自绘 TopBar 与手写返回按钮，改用 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildImmersiveTitleBar(profile_my_badges)；滚动主体改为 Scroll(this.contentScroller)，接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle，让徽章头图背景铺进状态栏并在滚动时进入标题栏背后。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（15 s 257 ms，仅既有 warn）。next: 你真机抽查“我的徽章”：首屏 Hero 是否仍完整承接状态栏、官方返回键/标题是否不压状态栏、下滑时徽章内容是否能进入标题栏背后并有光感；若稳定，下轮继续处理 MyTopicsPage / BookmarksPage / NotificationsPage 这类仍自绘 TopBar 且带列表动作的页面。`
- **23:29** `feat(api23-uidesignkit): 继续 HDS23-07，把 AppLogsPage 迁到浏览历史同款沉浸标题栏，并保留右侧“清空日志”动作：删除自绘 TopBar 与 RouterManager 返回依赖，标题栏改用 ImmersiveHdsTitleBarHelper.buildImmersiveTitleBarWithMenu('应用日志', sys.symbol.trash, '清空日志', ...)，页面内容改为 Scroll(this.contentScroller)+Column，顶部插入状态栏 + 56vp + maskExtraHeight spacer；接入 bindToScrollable、ignoreLayoutSafeArea(TOP/BOTTOM)、systemBarStyle 和 GRADIENT_BLUR/systemMaterialEffect，FilterBar、日志详情 Overlay、清空确认 Overlay、复制日志逻辑均保留。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 874 ms，仅既有 warn）。next: 你真机抽查 AppLogsPage：状态栏连续、清空图标/动作、筛选行首屏间距、日志详情/清空确认 Overlay；若稳定，下轮再评估 MyBadgesPage 这类 Hero 头图页是否需要单独设计沉浸头图方案。`
- **23:22** `fix(api23-uidesignkit): 按你指出“为什么不做成跟浏览历史那样适配沉浸光感”纠正 HDS23-07 方向：上一轮只接普通官方 titleBar 过于保守，本轮将 DataManagementPage、FeatureSettingsPage、AboutAppPage、DraftsPage 全部改为浏览历史同款沉浸链路；新增 ImmersiveHdsTitleBarHelper.buildImmersiveTitleBar(...)（无右侧菜单版），四页均接入 HdsNavDestination.ignoreLayoutSafeArea(TOP/BOTTOM)、bindToScrollable、systemBarStyle、内容顶部 spacer = 状态栏 + MINI 标题栏 + maskExtraHeight，并把 titleBar 切到 avoidLayoutSafeArea:true / enableComponentSafeArea:false + GRADIENT_BLUR + systemMaterialEffect。DraftsPage 同步从 List 改为 Scroll(this.contentScroller)+Column，以匹配 BrowsingHistoryPage 已验收结构并保留刷新/加载更多/草稿 Sheet。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（17 s 92 ms，仅既有 warn）。next: 你真机抽查 数据管理 / 功能设置 / 关于 LDO社区 / 我的草稿 四页：状态栏是否连续、首屏是否不被标题栏遮挡、滚动时内容是否可进入标题栏背后且有光感；若稳定，再给 AppLogsPage 补带“清空”菜单的沉浸 helper。`
- **23:09** `refactor(api23-uidesignkit): 继续 HDS23-07 第二批小范围收敛自绘 TopBar：AboutAppPage、DraftsPage 删除重复自绘 TopBar 与手写顶部安全区避让，统一接入 HdsNavDestinationTitleMode.MINI + ImmersiveHdsTitleBarHelper.buildPlainTitleBar(...) + 官方返回键；标题使用已有资源 profile_about_app / profile_my_drafts，DraftsPage 保留底部安全区与草稿编辑 Sheet，AboutAppPage 保留开发者模式入口与外链/日志跳转逻辑。MyBadgesPage 因顶部 Hero/渐变头图依赖自定义叠加标题栏，本批暂缓。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（15 s 607 ms，仅既有 warn）。next: 你真机抽查“关于 LDO社区 / 我的草稿 / 数据管理 / 功能设置”四个已迁移普通标题栏页面；若标题栏、安全区、返回、草稿 Sheet 均稳定，下轮再处理带右侧动作的 AppLogsPage（需给 helper 补 HDS menu/action 形态）或继续挑无特殊头图页面。`
- **23:00** `refactor(api23-uidesignkit): 抽出 ImmersiveHdsTitleBarHelper 统一 HDS 官方标题栏配置：沉浸标题栏保留 BrowsingHistoryPage 已验收的 chatcube spacer / GRADIENT_BLUR / systemMaterialEffect 参数，普通标题栏封装 avoidLayoutSafeArea:true + 官方返回键样式；DataManagementPage、FeatureSettingsPage 改为复用 helper，删除重复自绘 TopBar；修复 helper 中 HDS titleBar options 的 ArkTS 显式类型问题（HdsNavigationTitleBarOptions / TitleBarContentOptions / TitleBarStyleOptions / ScrollEffectOptions 等逐层落类型），避免 arkts-no-untyped-obj-literals / arkts-no-noninferrable-arr-literals。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 731 ms，仅既有 warn）。next: 你真机抽查 DataManagementPage / FeatureSettingsPage 返回键、标题安全区、滚动首屏间距；若稳定，下一轮按 HDS23-07 继续挑 2~3 个无复杂右侧按钮页面替换自绘 TopBar。`
- **22:43** `docs(api23-uidesignkit): 根据你真机反馈“可以了，完美”，确认 BrowsingHistoryPage 采用 chatcube 式“NavDestination 延伸进状态栏 + 内容顶部 spacer”方案后已收口：状态栏连续背景、返回/标题/搜索避让、内容滚入标题栏背后与滚动光感均通过验收。API23_UIDESIGNKIT_ADAPTATION.md 将 HDS23-02 / HDS23-03 标记为 [x]，并追加 HDS23-02/03-verified 完成记录。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（12 s 643 ms，仅既有 warn）。next: 下一步抽统一 ImmersiveHdsScaffold/HdsTitleBar helper，把这套稳定结构复用到简单页面，再按 2~3 页一批推进 HDS23-07。`
- **22:38** `fix(api23-uidesignkit): 根据 chatcube 的“NavDestination 延伸进状态栏 + 内容顶部 spacer 避让首屏”结构修复 BrowsingHistoryPage：移除 HdsNavDestination 外层 .padding({ top: WindowManager.getSystemAvoidAreaTopVp() })，避免整个标题栏边界被推离状态栏导致白条/断层；titleBar 改为 avoidLayoutSafeArea:true / enableComponentSafeArea:false，让 HDS 标题栏控件避让状态栏但背景/滚动模糊仍可连续覆盖顶部；Scroll 内容顶部 Blank 从固定 56vp 改为 WindowManager.getSystemAvoidAreaTopVp() + 56 + maskExtraHeight(20vp)，首屏不被标题栏遮挡、滚动时内容可进入状态栏/标题栏背后；轻量提高标题栏 maskExtraHeight 与 blurRadius 以承接滚动光感。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步更新 HDS23-02/03 状态和完成记录。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（13 s 487 ms，仅既有 warn）。next: 请用 API23 真机复测“浏览历史”：A 状态栏是否不再纯白断层；B 返回/标题/搜索是否不压状态栏；C 下滑时帖子文字是否能进入标题栏背后并看到滚动光感；若稳定，下轮抽统一 ImmersiveHdsScaffold/HdsTitleBar helper。`
- **22:27** `research(immersive): 查看 https://github.com/LongLiveY96/chatcube 的状态栏/扩展内容区实现：其核心链路是 EntryAbility 在 loadContent 后对 mainWindow 执行 setWindowLayoutFullScreen(true)，并监听 TYPE_SYSTEM / TYPE_NAVIGATION_INDICATOR avoidAreaChange，把 topAvoidHeight / bottomAvoidHeight 写入全局 AppUiState；页面侧让 HdsNavigation/HdsNavDestination 自身 ignoreLayoutSafeArea([SYSTEM],[TOP])，titleBar 使用 HdsVisualEffectUtil.buildHdsTopNavigationTitleBarOptions(...) 统一配置 avoidLayoutSafeArea:true、enableComponentSafeArea:false、bindToScrollable、ScrollEffectType、透明原始背景、滚动态半透明背景、maskExtraHeight、blurRadius 与 systemMaterialEffect；列表内容不靠外层 padding 下移，而是在可滚动内容顶部插入 top spacer = statusBarHeight + 56 + maskExtraHeight，形成“内容可滑入状态栏/标题栏背后，但首屏不被遮挡”的结构。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（17 s 263 ms，仅既有 warn）。next: 若借鉴到 hmApp，优先抽统一 ImmersiveHdsScaffold/TitleBar helper，替换 BrowsingHistoryPage 当前外层 .padding({ top: WindowManager.getSystemAvoidAreaTopVp() }) 二选一方案。`
- **20:33** `chore(status): 按本轮“看看适配完成到哪一步了”请求盘点当前进度：当前主线功能已进入维护/体验优化阶段，API23 UIDesignKit 适配已完成 SDK 切换、HdsNavigation 根容器、约 23 个子页 HdsNavDestination 容器、DataManagementPage 官方标题栏 PoC、首页 HDS 悬浮 TabBar 与 HDS 原生显隐动画、详情页 HdsActionBar；HDS23-02/03 仍在 BrowsingHistoryPage 验证标题栏沉浸光感与 bindToScrollable，20:18 版本回滚到此前已验证“不重叠 + 有光感”的外层顶部安全区 padding 方案。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（16 s 498 ms，仅既有 warn）。next: 先复测“浏览历史”是否恢复沉浸光感且无大空白；随后不要再硬切 HDS 安全区参数，改设计“状态栏 + 标题栏一体 Scaffold/自绘材质层”，稳定后再推进 HDS23-04 / HDS23-07。`
- **20:18** `fix(api23-uidesignkit): 回滚 19:56 对 BrowsingHistoryPage 的错误安全区策略：真机反馈确认 titleBar avoidLayoutSafeArea:true / enableComponentSafeArea:true 会让 HDS 标题栏沉浸光感消失，并且 Scroll 顶部 Blank 与 HDS 自身安全区叠加造成“浏览历史标题下方到帖子之间大空白”。本轮恢复到此前已验证“不重叠 + 有沉浸光感”的布局：titleBar 回到 avoidLayoutSafeArea:false / enableComponentSafeArea:false；Scroll 顶部 Blank 恢复只占 MINI 标题栏 56vp；HdsNavDestination 外层重新用 .padding({ top: WindowManager.getSystemAvoidAreaTopVp() }) 兜底避让状态栏。中途验证过 titleBar.padding.top 方案，但 UIDesignKit 的 titleBar padding 类型只支持 start/end，不支持 top，已撤销。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（18 s 482 ms，仅既有 warn）。next: 请先复测“浏览历史”是否恢复沉浸光感且不再有大空白；状态栏白条问题下一轮不再继续硬改 HdsNavDestination 参数，改走“状态栏 + 标题栏一体 Scaffold/自绘材质层”方案。`
- **19:56** `fix(api23-uidesignkit): 根据你提供的对比图确认 19:42 全局根容器扩展仍无变化，真正挡住状态栏连续光感的是 BrowsingHistoryPage 自己的 .padding({ top: WindowManager.getSystemAvoidAreaTopVp() })：这会把整个 HdsNavDestination 下移，导致官方 titleBar/scrollEffect 无法覆盖状态栏，顶部只能看到窗口/页面白底。Playwright 复核官方 HdsNavDestination/HdsNavigation 文档（HdsNavDestination 更新时间 2026-04-29 15:35，HdsNavigation 更新时间 2026-04-24 16:10）确认：ignoreLayoutSafeArea 只有组件边界贴近系统安全区时才会延伸，且 HDS 提供 systemBarStyle 让 NavDestination 与状态栏样式联动。本轮移除 BrowsingHistoryPage 外层顶部 padding 与临时 StatusBarMaterialBridge，改为 HdsNavDestination 自身 ignoreLayoutSafeArea([SYSTEM],[TOP,BOTTOM]) + titleBar avoidLayoutSafeArea:true / enableComponentSafeArea:true，让 HDS 标题栏背景/滚动模糊连续铺进状态栏，同时标题/返回/搜索由组件安全区下移；补 .systemBarStyle(...) 让状态栏文字颜色跟随明暗模式；清理不再使用的 title_bar_status_material_tint 明暗资源。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（17 s 272 ms，仅既有 warn）。next: 请再真机复测“浏览历史”：预期状态栏不再单独白条，而是和 HdsNavDestination 顶部材质连续；同时重点看返回/标题/搜索是否仍不压状态栏。若这版标题栏控件又上移，我下一轮不再加外层 padding，而是改成自绘 HDS 状态栏+标题栏一体 Scaffold。`
- **19:42** `fix(immersive-root): 按你判断“不是单页问题、应该看全局沉浸式全面屏”，回查全局链路：EntryAbility 初始化时调用 WindowManager.applyDefaultSystemChrome，当前已经 setWindowLayoutFullScreen(true) + statusBarColor '#00000000'，但 Index 根 Stack / HdsNavigation 没有显式 ignoreLayoutSafeArea，透明状态栏区域会透出 WindowManager.setWindowBackgroundColor('#FFFFFF') 的窗口白底，导致业务页下方有 HDS 沉浸光感但状态栏仍像白条。Playwright 复核官方 Interface (Window)-@ohos.window（更新时间 2026-04-30 17:02）确认 setWindowLayoutFullScreen(true) 只是让布局不避让系统栏，组件可能重叠；setWindowSystemBarProperties 只负责系统栏颜色/图标色，不会替我们把 ArkUI 根节点绘制进安全区。本轮在 entry/src/main/ets/pages/Index.ets 的根 Stack 与 HdsNavigation 同步补 backgroundColor(app.background) 和 ignoreLayoutSafeArea([SYSTEM],[TOP,BOTTOM])，让全局页面背景/材质真正铺到状态栏/导航栏区域。docs: API23_UIDESIGNKIT_ADAPTATION.md 记录 HDS23-02-global-immersive-root。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 433 ms，仅既有 warn）。next: 请真机复测“浏览历史”顶部状态栏是否不再露窗口白底，同时抽查首页/我的/详情页顶部是否仍不重叠；若稳定，下一轮把页内 StatusBarMaterialBridge 临时补丁抽成统一沉浸式基座或回收。`
- **19:24** `style(api23-uidesignkit): 根据你确认“确实好了”后指出的新长期问题——状态栏安全区仍是突兀白底、和下方标题栏沉浸光感断层，本轮不再改动已稳定的 HDS 标题栏参数，而是在 BrowsingHistoryPage 外层增加状态栏材质桥接层：HdsNavDestination 继续保持 18:34 已验证的官方光感参数 + 外层顶部安全区 padding；外层改为 Stack 后，在顶部叠加 StatusBarMaterialBridge，覆盖 statusBarHeight + 12vp，不接管点击，使用 backgroundEffect(radius=64, saturation=2.0, brightness=1.06) 与新增明暗资源 title_bar_status_material_tint，让状态栏安全区从纯白底过渡为近似标题栏的轻玻璃材质。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（14 s 245 ms，仅既有 warn）。next: 请复测“浏览历史”顶部：重点看状态栏区域是否不再纯白割裂、标题/返回/搜索是否仍不重叠、沉浸光感是否仍保留；若桥接层偏灰/偏白，我下一轮只调 title_bar_status_material_tint 透明度和 overlap 高度，不再改标题栏参数。`
- **18:34** `fix(api23-uidesignkit): 根据你截图确认 18:25 后仍然“沉浸光感没有 + 标题/返回/搜索与状态栏重叠”，判定 enableComponentSafeArea:true 在 BrowsingHistoryPage 这台真机场景下既没解决重叠，还破坏了 HDS 光感；本轮回到 18:09 已证实有光感的官方参数 avoidLayoutSafeArea:false / enableComponentSafeArea:false / ignoreLayoutSafeArea([SYSTEM],[TOP,BOTTOM])，但不再依赖 HDS 内部安全区，而是在 HdsNavDestination 外层追加 .padding({ top: WindowManager.getSystemAvoidAreaTopVp() })，把整套官方标题栏整体下移避让状态栏，同时保留 Scroll 顶部 56vp Blank、bindToScrollable、GRADIENT_BLUR 与 systemMaterialEffect(MaterialType.ADAPTIVE, MaterialLevel.ADAPTIVE)。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步记录 HDS23-02-external-safe-padding。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（16 s 53 ms，仅既有 warn）。next: 请复测“浏览历史”：若这版仍不能同时满足“不重叠 + 有光感”，我下一轮立即回退 BrowsingHistoryPage 为自绘稳定标题栏，并把 HDS23-02 强验证迁到 AppLogsPage，不再让这个业务页继续反复抖动。`
- **18:25** `fix(api23-uidesignkit): 根据你反馈“这么一改又没有沉浸光感了”，确认 18:18 的 avoidLayoutSafeArea:true 虽然修了安全区，但会让 HDS 标题栏退出官方全屏沉浸布局导致光感消失；本轮把 BrowsingHistoryPage 改为混合参数：avoidLayoutSafeArea:false 保留官方沉浸布局，enableComponentSafeArea:true 让标题栏内部返回/标题/搜索尝试避让状态栏，并恢复 ignoreLayoutSafeArea([SYSTEM],[TOP,BOTTOM])；Scroll 顶部 56vp Blank、bindToScrollable、GRADIENT_BLUR、systemMaterialEffect(MaterialType.ADAPTIVE, MaterialLevel.ADAPTIVE) 继续保留。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步记录 HDS23-02-safearea-material-hybrid。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（15 s 308 ms，仅既有 warn）。next: 请用 API23 真机复测“浏览历史”：这版预期光感恢复，同时返回/标题/搜索不再压状态栏；如果仍二选一不成立，我下一轮迁到 AppLogsPage 做强验证页，不再继续在 BrowsingHistoryPage 上反复调参。`
- **18:18** `fix(api23-uidesignkit): 根据真机反馈“沉浸光感有了，但返回/标题/搜索又与状态栏重叠”，确认官方沉浸示例的 avoidLayoutSafeArea=false + ignore TOP 在本项目全局 fullScreen 窗口下会把 HDS 官方标题栏压进状态栏；本轮对 BrowsingHistoryPage 采用混合策略：titleBar 恢复 avoidLayoutSafeArea:true / enableComponentSafeArea:true，让返回键、标题、搜索主动避让顶部状态栏；HdsNavDestination 的 ignoreLayoutSafeArea 只保留 BOTTOM，不再忽略 TOP；同时保留 Scroll 顶部 56vp Blank、bindToScrollable、GRADIENT_BLUR 与 systemMaterialEffect(MaterialType.ADAPTIVE, MaterialLevel.ADAPTIVE)，尽量同时保住滚动光感。docs: API23_UIDESIGNKIT_ADAPTATION.md 已记录 HDS23-02-safearea-fix。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（1 s 859 ms）。next: 请用 API23 真机复测“浏览历史”：顶部返回/标题/搜索应不再压状态栏，滚动后光感仍应存在；若修安全区后光感明显变弱或仍重叠，下一轮迁到 AppLogsPage 做强验证，不再在 BrowsingHistoryPage 死调。`
- **18:09** `fix(api23-uidesignkit): 按你指出“是不是哪里写错了”重新核对官方文档后确认前面确实有配置方向错误：Playwright 复核官方《沉浸光感》（页面标题：沉浸光感-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-29 15:35），文档说明 systemMaterialEffect 是“为标题栏按钮设置沉浸光感视效”，不是让整条标题栏背景透出文字；该示例使用 avoidLayoutSafeArea=false、enableComponentSafeArea=false、bindToScrollable(...)、titleMode(MINI)、ignoreLayoutSafeArea([SYSTEM],[TOP,BOTTOM])。同时复核官方《HdsNavDestination-ArkTS组件》（页面标题：HdsNavDestination-ArkTS组件-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-29 15:35），滚动模糊示例在 Scroll 顶部放 Blank().height(TITLE_BAR_HEIGHT_MINI)。据此修正 BrowsingHistoryPage：titleBar 改回官方沉浸参数 avoidLayoutSafeArea:false / enableComponentSafeArea:false，并在 HdsNavDestination 上追加 ignoreLayoutSafeArea；Scroll 内容顶部增加 56vp Blank 占位，让首屏内容先避开 MINI 标题栏、滚动后再经过标题栏背后。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步记录“前面写错点”和官方依据。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 请用 API23 真机复测：如果标题栏/状态栏不重叠且滚动后仍看不到按钮材质/滚动模糊，我下一轮直接迁到 AppLogsPage 做强验证。`
- **17:59** `fix(api23-uidesignkit): 根据你最新截图“还是看不到背景的字体”，确认 17:32 为了放大光感加的按钮材质底色，以及 17:17 加过的高 blur/mask 方向会继续遮住标题栏背后的列表文字；本轮把 BrowsingHistoryPage 的 HDS 标题栏改成“优先透字”的低干预版本：originalStyle.backgroundStyle、scrollEffectStyle.backgroundStyle、backIconStyle、menuStyle 全部改回 Color.Transparent，避免任何白色/蓝色背板盖住内容；TITLE_BAR_GLASS_MASK_EXTRA_HEIGHT_VP 从 72 降为 0，TITLE_BAR_GLASS_BLUR_RADIUS 从 128 降为 8，只保留极轻的滚动模糊；同时移除未再使用的 title_bar_glass_tint / title_bar_button_material_tint 资源。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步记录透明优先策略。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你再复测“浏览历史”页标题区域，如果背后列表文字仍无法透出，我下一轮直接迁到 AppLogsPage，不再在 BrowsingHistoryPage 上继续调。`
- **17:32** `fix(api23-uidesignkit): 你反馈“黑色圆底修复了，但往下滑依然没有沉浸光感”，本轮继续针对 BrowsingHistoryPage 做可证伪修正：保留 17:17 已确认有效的未滑动透明返回/搜索按钮；将滚动主体从 List({ scroller }) 改为 HDS 官方示例同类的 Scroll(this.contentScroller)，再绑定 .bindToScrollable([this.contentScroller])，避免 Refresh + List 组合下标题栏没有吃到滚动偏移；scrollEffectType 从 IMMERSIVE_GRADIENT_BLUR 改为官方《沉浸光感》（页面标题 沉浸光感-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-29 15:35）示例使用的 GRADIENT_BLUR，同时继续保留 API23 systemMaterialEffect(MaterialType.ADAPTIVE, MaterialLevel.ADAPTIVE)；scrollEffectStyle 中返回/搜索按钮背景改为资源化 title_bar_button_material_tint，避免恢复黑色圆底，但给 HDS 按钮沉浸光感一个可见的浅色/蓝色材质承载面。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步记录“List -> Scroll + GRADIENT_BLUR + 按钮材质底色”的二次修正。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机复测“浏览历史”：未滑动仍应无黑色圆底，往下滑后返回/搜索按钮应出现轻量玻璃光感；若仍没有，我下一轮不再在浏览历史页继续试，直接迁到 AppLogsPage 做强验证。`
- **17:17** `fix(api23-uidesignkit): 根据你反馈“BrowsingHistoryPage 未滑动时返回/搜索是黑色圆底、下滑没有沉浸光感”，本轮继续修浏览历史页 HDS 官方标题栏：Playwright 复核华为官方《HdsNavDestination-ArkTS组件》（页面标题 HdsNavDestination-ArkTS组件-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-29 15:35）确认 bindToScrollable/滚动动态模糊能力；复核《沉浸光感》（页面标题 沉浸光感-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-29 15:35）确认 API23 沉浸光感推荐 MaterialType.ADAPTIVE + MaterialLevel.ADAPTIVE。BrowsingHistoryPage 的 titleBar originalStyle/scrollEffectStyle 均显式把 backIconStyle/menuStyle 背景设为 Color.Transparent，去掉 HDS 默认深色圆底；滚动效果补 LengthMetrics.vp(0)->LengthMetrics.vp(56) 阈值、enableRefreshOffsetChange=true，scrollEffectStyle 背板改为资源化 title_bar_glass_tint，并提升 maskExtraHeight=72、blurRadius=128 以放大可见玻璃/光感差异；systemMaterialEffect 从 IMMERSIVE 切回官方示例推荐的 ADAPTIVE。resources/base、resources/dark 同步新增 title_bar_glass_tint。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步记录 HDS23-02/03 修正。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机复测“浏览历史”页：首屏返回/搜索不应再有黑色圆底，慢慢滚动时标题栏应出现更明显的半透明玻璃/光感；若仍不明显，我下一轮迁到 AppLogsPage 做专门强验证。`
- **16:50** `refactor(api23-uidesignkit): 根据你反馈“FeatureSettingsPage 看不到沉浸光感且不必恢复卡片”，将 FeatureSettingsPage 降级回普通 HDS 官方标题栏（移除顶部说明卡、bindToScrollable 与沉浸光感参数），不再作为 HDS23-02 验证页；新的沉浸光感 PoC 迁到内容密度更高的 BrowsingHistoryPage：移除自绘 TopBar 与手写顶部安全区 padding，改用 HdsNavDestination 官方标题栏/返回键，并用 titleBar.menu 保留“搜索浏览历史”按钮；List 接入 listScroller 并绑定 bindToScrollable，标题栏开启 IMMERSIVE_GRADIENT_BLUR + systemMaterialEffect(MaterialType.IMMERSIVE, ADAPTIVE)，滚动态背景设为透明并加 blurRadius=96，放大列表卡片经过标题栏时的可观察差异；dynamicHideTitleBar 仍暂停，避免和光感验证互相干扰。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步改为 BrowsingHistoryPage 验证。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机进入“浏览历史”页（需要有足够历史记录）上下滚动，确认标题栏/返回键/搜索按钮光感是否可见；若历史不足或仍不明显，我下一轮迁到 AppLogsPage 并准备足够日志做强验证。`
- **16:36** `fix(api23-uidesignkit): 你指出删除顶部卡片后 FeatureSettingsPage 又不方便验证标题栏沉浸光感，本轮恢复“功能设置 / 常用行为与交互偏好”HeroCard，但在卡片前新增 HERO_CARD_TITLE_BAR_GAP=72 的标题栏避让留白：首屏卡片不会再压到 HDS 官方标题/返回键，向上滚动时卡片仍会经过标题栏背后，用于观察 IMMERSIVE_GRADIENT_BLUR 与 systemMaterialEffect 的沉浸光感；dynamicHideTitleBar 继续暂停，只验证 bindToScrollable 驱动的滚动光感。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步记录恢复卡片和留白策略。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机复测“功能设置”页：确认首屏卡片不再和标题/返回键重叠，并慢慢上滑让卡片经过标题栏背后，看沉浸光感是否可见；若间距过大/过小，我下一轮只调 HERO_CARD_TITLE_BAR_GAP。`
- **16:27** `fix(api23-uidesignkit): 根据真机截图修复 FeatureSettingsPage 顶部标题区域异常：删除顶部重复的“功能设置 / 常用行为与交互偏好” HeroCard，避免说明卡与 HDS 官方标题栏、返回键重叠；HdsNavDestination.titleBar 继续 avoidLayoutSafeArea 并补 enableComponentSafeArea，originalStyle/scrollEffectStyle 均显式指定 mainTitleColor/backIconStyle.iconColor 为 app.color.on_surface，并将 backIconStyle.backgroundColor 设为透明，避免拉到最顶部时标题发白看不清、返回键变黑色圆底；同时暂停 dynamicHideTitleBar，只保留 bindToScrollable + IMMERSIVE_GRADIENT_BLUR + systemMaterialEffect 做滚动沉浸光感，优先保证页面可读性。docs: API23_UIDESIGNKIT_ADAPTATION.md 同步记录 HDS23-02/03 修正与动态显隐暂停原因。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机复测“功能设置”页顶部：确认不再卡片/标题重叠、标题始终黑色可读、返回键不再黑色圆底；若仍重叠，我下一轮在该页彻底关闭标题栏 scrollEffect，仅保留普通官方标题栏。`
- **15:42** `refactor(api23-uidesignkit): 继续 HDS23-02/HDS23-03，在可持续滚动的 FeatureSettingsPage 做标题栏沉浸光感 PoC：移除自绘 TopBar 与手写 WindowManager 顶部安全区 padding，改用 HdsNavDestination 官方 titleBar，并保留 titleMode(HdsNavDestinationTitleMode.MINI)+avoidLayoutSafeArea: true 避免重现状态栏重叠；为 Scroll 绑定 contentScroller，接入 bindToScrollable、dynamicHideTitleBar({ mode: HideMode.SCROLL_UP_TO })、ScrollEffectType.IMMERSIVE_GRADIENT_BLUR 与 systemMaterialEffect(MaterialType.IMMERSIVE, MaterialLevel.ADAPTIVE)，API23_UIDESIGNKIT_ADAPTATION.md 将 HDS23-02/HDS23-03 标为 [~] 等待真机体验确认。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机进入“功能设置”页上下滚动，确认标题栏沉浸光感/动态显隐/返回键安全区是否稳定；若显隐跳动，我下一轮先关闭 dynamicHideTitleBar，仅保留 bindToScrollable + 沉浸光感。`
- **15:24** `docs(api23-uidesignkit): 修正 HDS23-02/HDS23-03 后续 PoC 页面选择：你指出 DataManagementPage 只有少量选项、无法持续上下滑动，因此它只适合 HDS23-01 官方标题栏/安全区验收，不适合验证标题栏沉浸光感、bindToScrollable 或 dynamicHideTitleBar；API23_UIDESIGNKIT_ADAPTATION.md 已把后续推荐页面改为有足够滚动内容的 FeatureSettingsPage，AppLogsPage 仅在准备足够日志数据后作为备选。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 优先在 FeatureSettingsPage 做 HDS23-02/HDS23-03 PoC，先迁移官方标题栏并绑定滚动容器，再验证沉浸光感和动态显隐。`
- **15:14** `docs(api23-uidesignkit): 根据你反馈“可以了”，确认 DataManagementPage 的 HDS 官方标题栏在 API23 真机上已不再与状态栏重叠；API23_UIDESIGNKIT_ADAPTATION.md 将 HDS23-01 从 [~] 更新为 [x]，并记录 15:00 构建通过 + 15:14 真机确认结果。build: 本轮仅文档状态收口，沿用 15:00 D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL。next: 下一步可继续同页 HDS23-02 标题栏沉浸光感 PoC，或先做 HDS23-03 bindToScrollable/dynamicHideTitleBar 动态标题栏验证。`
- **15:00** `fix(api23-uidesignkit): 根据你提供的真机截图修复 DataManagementPage 官方 HDS 标题栏与状态栏重合：根因是项目全局 WindowManager.applyDefaultSystemChrome 会 setWindowLayoutFullScreen(true)，而 HdsNavDestination.titleBar 默认 avoidLayoutSafeArea=false；本轮显式设置 titleMode(HdsNavDestinationTitleMode.MINI)，并在 titleBar 配置中增加 avoidLayoutSafeArea: true，让官方返回键和标题栏主动避让顶部安全区，继续保留 HDS 官方标题栏 PoC，不回退自绘 TopBar；同步更新 API23_UIDESIGNKIT_ADAPTATION.md 的 HDS23-01 记录与注意事项。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机复测“数据管理”页顶部，确认返回键/标题已下移且不再压住时间和挖孔；若仍重叠，我再尝试 enableComponentSafeArea: true，仍不稳则暂停官方标题栏 PoC 并回退该页自绘 TopBar。`
- **14:36** `refactor(api23-uidesignkit): 推进 HDS23-01 非核心页官方标题栏 PoC：Playwright 复核华为官方《HdsNavDestination-ArkTS组件-UI Design Kit（UI设计套件）》（页面标题 HdsNavDestination-ArkTS组件-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-29 15:35），确认 titleBar/hideTitleBar/hideBackButton 可用于子页官方标题栏；DataManagementPage 移除自绘 TopBar 与手写顶部安全区 padding，改用 HdsNavDestination.titleBar({ content: { title: { mainTitle: $r('app.string.dm_page_title') } } }) 并显式保留官方返回键，路由仍由 NavPathStack/HdsNavigation 管理；API23_UIDESIGNKIT_ADAPTATION.md 将 HDS23-01 标记为部分完成并补充完成记录。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机进入“数据管理”页确认返回键、标题居中/高度、状态栏/安全区无重叠、滚动无异常；确认稳定后我再继续同页 HDS23-02 标题栏沉浸光感 PoC。`
- **14:17** `docs(api23-uidesignkit): 新增 API23 UIDesignKit 适配追踪文档 API23_UIDESIGNKIT_ADAPTATION.md，集中记录官方文档核对基线、当前已完成项（SDK API23、HdsNavigation/HdsNavDestination、首页 HdsTabs 原生悬浮页签、详情页 HdsActionBar）、P0/P1/P2 待办与暂缓项，并给出每项完成时的状态/验证记录格式，方便后续按 HDS23-01~HDS23-11 持续更新完成情况；同时在 .gitignore 放行该追踪文档和 CHANGELOG.md，避免新建 Markdown 被全局 *.md 规则忽略。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL. next: 优先做 HDS23-01 非核心页 HdsNavigation 官方标题栏 PoC；完成后同步更新本文档对应状态和完成记录。`
- **14:03** `research(api23-uidesignkit): 盘点 API23 UIDesignKit 可适配能力与当前项目使用面：1) 当前 target/compatibleSdkVersion 已是 6.1.0(23)，HDS 已落地 HdsNavigation/HdsNavDestination（23 个子页）、首页 HdsTabs 原生悬浮页签 + HdsTabsFloatingStyle(systemMaterialEffect/thermoCtrl/barWidth/barOpacity/gradientMask)、详情页 HdsActionBar；2) Playwright 复核华为官方《HdsTabs-ArkTS组件》（页面标题 HdsTabs-ArkTS组件-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-30 10:41）、《沉浸光感》（更新时间 2026-04-29 15:35）、《hdsMaterial-ArkTS API》（更新时间 2026-04-20 14:34）、《HdsNavigation-ArkTS组件》（更新时间 2026-04-24 16:10）、《HdsActionBar-ArkTS组件》（更新时间 2026-04-24 16:10）；3) 结论：短期优先收尾 HdsTabs 光感参数/miniBar 或动作回调，中期评估 HdsNavigation 官方标题栏沉浸光感与 bindToScrollable 替换一批自绘 TopBar，HdsActionBar 暂不建议继续硬套沉浸光感（官方未提供 systemMaterialEffect），设置/日志类列表可后续评估 HdsListItem/HdsListItemCard。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 若你确认进入适配实现，下一轮建议先做“非业务页官方 HdsNavigation 标题栏 PoC + 首页 HdsTabs miniBar/光效色小步验证”，再决定是否批量替换自绘 TopBar。`

- **13:38** `refine(hds-tab-shadow): 按截图反馈“首页 HdsTabs 悬浮胶囊与白色背景融在一起”，在继续保留 UIDesignKit 原生 HdsTabs barFloatingStyle 的前提下补一层不接管点击的 FloatingTabBarShadowPlate：与 HdsTabsFloatingStyle.barWidth 的 280/320/400vp 分档宽度对齐，跟随 bottom safe area + 10vp + 8vp 上抬距离、滚动显隐透明度与隐藏位移；新增 tab_floating_surface_tint/tab_floating_outline/tab_floating_shadow 明暗资源，并把阴影半径/偏移集中到 LayoutConstants，给胶囊周边增加更明显的柔和投影与浅描边，避免白底场景下边界消失。docs: Playwright 复核华为官方《HdsTabs-ArkTS组件-UI Design Kit（UI设计套件）》页面标题为 HdsTabs-ArkTS组件-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-30 10:41，barFloatingStyle/HdsTabsFloatingStyle 起始版本 6.1.0(23)。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机回归首页底部胶囊，重点看白色列表/底部安全区背景下阴影边界是否足够明显、首页/我的切换是否仍正常、下滑隐藏时阴影是否同步消失；若仍偏淡，我下一轮只微调 tab_floating_shadow alpha 和 shadow radius，不改 HDS 原生栏。`

- **13:01** `chore(app-identity): 按华为知识产权审核反馈，去掉应用名称中的 Harmony/HarmonyDO 商标指向：AppScope 与 entry 资源中的展示名统一改为 “LDO社区”，Ability/module 描述改为 “LDO社区 - Linux.do 第三方鸿蒙客户端”；关于页、开源说明、README 的对外展示文案同步改名；AppScope/app.json5 的 bundleName 从 com.HarmonyDo.myApp 改为 com.bigfanqie.ldocommunity，vendor 从 example 改为 bigfanqie；同时移除 entry 里重复 app_name 资源，消除此前 app_name conflict 警告。verify: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon 已通过 CompileArkTS/PackageHap/PackingCheck，最终 SignHap 因本机 build-profile.json5 的 SigningConfigs 仍绑定旧 bundleName 失败（00303074：bundleName 与生成的签名配置不匹配）。next: 在 DevEco Studio > Project Structure > Signing Configs 对新包名 com.bigfanqie.ldocommunity 点击 Fix/重新生成调试或发布签名 profile 后，再重新 assembleHap；若你也要把 GitHub 远端/源码链接从 Amaz1ny 迁到 bigfanqie，我再同步改 SOURCE_CODE_URL/README clone 地址。`

- **12:14** `refine(topic-detail-hds-actionbar-visual): 按你新截图 90e0848 反馈“背景字又看不见、功能灰色圆底太丑且对比度过高”，在继续保留 UIDesignKit 原生 HdsActionBar 的前提下二次调轻详情页底部栏：DETAIL_ACTION_BAR_OPACITY 从 0.92 下调到 0.86；ActionBarStyle 显式把官方 backgroundBlurStyle 从默认 COMPONENT_REGULAR 改为 COMPONENT_ULTRA_THIN；detail_action_bar_background 由 #DDF8FAFC/#DD24262B 改为 #8AF8FAFC/#8A24262B，让帖子文字能重新透出；默认 ActionBarButton 背景从黑色灰底 #1F000000 改为近透明白 #0FFFFFFF（暗色 #12FFFFFF），并把每个按钮 shadowStyle 改为透明 0 半径，去掉 HDS 单按钮灰色圆底/阴影的脏感；选中态改为更轻的蓝色 tint（#220088CC / #264DB8FF），默认图标色改为更柔和的 slate。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你真机回归同一位置，确认底栏下方文字是否能看见、灰圆按钮是否消失、选中态蓝色是否足够轻；若仍遮字，我下一轮只继续降 detail_action_bar_background alpha，不再加深灰色按钮。`

- **12:06** `style(topic-detail-hds-actionbar): 针对你截图 3a2a841 中详情页 HDS 悬浮操作栏与帖子正文背景区分不明显、各功能按钮颜色不统一的问题，继续保留 UIDesignKit 原生 HdsActionBar，不回自绘：新增 detail_action_bar_background/detail_action_button_background/detail_action_button_selected_background/detail_action_icon/detail_action_selected_icon/detail_action_bar_stroke/detail_action_bar_shadow 明暗两套资源；TopicDetailPage 的 ActionBarStyle 改用更高对比度半透明背板，外层补圆角描边与阴影分离层，五个 ActionBarButton 统一默认底色/图标色，热门回复与只看题主选中态统一主色系，最大透明度收敛为 DETAIL_ACTION_BAR_OPACITY=0.92，保证能从白色帖子文字背景里分离出来且仍保留一定透底感。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你真机打开同一帖子详情页，重点看底部 HDS 悬浮栏是否能明显压住背景文字、五个功能按钮是否同一色系、选中态是否清楚但不突兀；若仍觉得背板太白或遮字，我下一轮只微调 detail_action_bar_background alpha 与按钮默认底色。`

- **01:37** `fix(topic-detail-hds-actionbar-crash): 根据 jscrash-com.HarmonyDo.myApp-20260430012333.273.txt 定位崩溃：Mate 70 / HarmonyOS 6.1.0.117 进入 /ets/pages/Index:TopicDetail 后 9s 触发 TypeError: undefined is not callable，栈顶 TopicDetailPage.ts:4124 对应 HdsActionBar primaryButtonBuilder 内的分享按钮 builder；根因是 HdsActionBar 内部调用 CustomBuilder 时 this 被切到 HDS 组件实例，导致 this.getDetailActionBarButtonSizeVp() 不可调用。本轮移除底部栏分享按钮的 primaryButtonBuilder，改为 HDS 原生 primaryButton: ActionBarButton；点击分享按钮后打开 TopicShareActionSheetBuilder（分享链接/生成分享图片/导出文章），保留原三项能力，同时避免 HDS builder 上下文丢失。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat clean assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn；debug/release 编译产物均确认不再含 primaryButtonBuilder）。next: 你安装新包后直接打开帖子详情页，确认不再闪退；再点底部分享按钮验证 Sheet 三项动作。`

- **01:19** `refactor(topic-detail-hds-actionbar): 经 Playwright 复核华为官方《HdsActionBar-ArkTS组件-UI Design Kit（UI设计套件）》（页面标题：HdsActionBar-ArkTS组件-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-24 16:10，起始版本 6.0.0(20)，margin 为 6.1.0(23)）后，确认详情页底部操作栏可使用 UIDesignKit 原生 HdsActionBar。TopicDetailPage 现将原 Row+Stack+backgroundEffect 自绘玻璃胶囊替换为 HdsActionBar：回顶/热门回复/只看题主/浏览器打开改用 ActionBarButton，分享保留官方 primaryButtonBuilder 槽位并继续绑定原分享菜单；宽度、上抬、透明度、底部避让继续复用首页 FLOATING_TAB_BAR_* 策略，且补齐 ActionBarStyle 的 HDS 背板透明色、间距与展开状态。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机回归帖子详情页底部 HdsActionBar：确认五个操作顺序/点击/分享菜单、HDS 背板通透度、上抬距离与首页胶囊是否协调；若官方 ActionBar 默认按钮阴影或主按钮大小显得突兀，我再只调 ActionBarButton width/innerSpace/primaryButtonBuilderWidth。`

- **00:44** `refine(hds-tab): 按你的反馈继续统一底部悬浮胶囊：新增 FLOATING_TAB_BAR_OFFSET_UP_VP=8，将首页 HDS 原生悬浮栏 barBottomMargin 从“安全区+10vp”提高到“安全区+10vp+8vp”，并同步更新首页列表/个人页/稍后阅读气泡的底部避让，避免胶囊上移后遮挡内容或与浮层打架；帖子详情页底部悬浮工具栏改成跟首页同一套宽度/位置/透明度策略：按 root 宽度分档复用 280/320/400vp，居中定位，最大显示透明度复用 FLOATING_TAB_BAR_OPACITY=0.72，底部位置同样上抬 8vp，同时进度胶囊、回复 FAB、底部预留空间同步避让。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机回归首页/我的/详情页：确认首页胶囊高度不再贴 Home 区、帖子详情页底部悬浮栏宽度/居中/透明度是否与首页一致；如详情页 5 个图标显得太挤，我再只对详情页分档宽度做小幅加宽。`
- **00:33** `refine(hds-tab): 按你确认“宽度可以了”后的反馈，只继续微调 API23 HDS 原生悬浮 TabBar 的通透度与显隐节奏：Index.getHdsTabsFloatingStyle() 将 barOpacity 从 1 改为资源化常量 FLOATING_TAB_BAR_OPACITY=0.72；base/dark 的 tab_glass_mask 从 #CC 下调到 #33，减少底部渐变蒙层压白/压黑，让悬浮栏下方帖子文字更容易透出；TopicListPage 新增 FLOATING_TAB_BAR_HIDE_DELAY_MS=1500 与独立 hide timer，下滑时先 scheduleFloatingTabBarHide，连续下滑满 1.5s 才隐藏，方向上滑/到顶/停滚会清理隐藏计时并按原逻辑回显。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机确认三点：底部 HDS 胶囊下方帖子文字是否可见、通透度是否过头、连续下滑 1.5s 才隐藏且短滑/停手不会误隐藏；若还不够透我再只微调 FLOATING_TAB_BAR_OPACITY 与 tab_glass_mask。`
- **00:17** `fix(hds-tab): 按你的要求继续走官方 API、不回自绘：用 Playwright 打开华为官方《HdsTabs-ArkTS组件-UI Design Kit（UI设计套件）》（页面标题已更新为 HdsTabs-ArkTS组件-UI Design Kit（UI设计套件）-应用框架 - 华为HarmonyOS开发者，更新时间 2026-04-29 15:35）和《设置页签栏的悬浮样式》（更新时间 2026-04-29 15:35），确认 API23/6.1.0(23) 新增 HdsTabsFloatingStyle.barWidth/HdsBarWidthRangeOptions 是原生拉宽悬浮页签栏的能力，官方示例使用固定 vp 分档 barWidth: { smallWidth, mediumWidth, largeWidth }；本轮在 LayoutConstants 新增 FLOATING_TAB_BAR_WIDTH_SMALL/MEDIUM/LARGE_VP=280/320/400，并在 Index.getHdsTabsFloatingStyle() 接入固定 vp 分档 barWidth，避免上一轮百分比宽度在真机导致贴右/越界；不改回自绘、不再使用 72%/14% 组合。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机重点确认底部 HDS 胶囊是否已明显加宽且不再跑到右侧/越界；若仍偏右，下一步只在官方参数内尝试去掉 barSideMargin 或改小固定宽度，不使用自绘。`

## 2026.04.29

- **23:40** `revert(hds-tab): 按你要求回退 23:21/23:26/23:32 三轮 HDS 悬浮 TabBar 调参，回到 23:05 第一版 API23 HDS 原生悬浮接入状态：LayoutConstants 移除新增的宽度/上抬/居中侧边距/透明度常量；Index 的 HdsTabsFloatingStyle 撤销 barWidth、adaptToHandedness、14% barSideMargin 与 0.68 barOpacity，恢复 barSideMargin=FLOATING_TAB_BAR_MARGIN_H_VP、barBottomMargin=安全区+10vp、barOpacity=1；TopicListPage 撤销 3000ms 延迟隐藏与 15vp 底部避让，恢复下滑立即 hideFloatingTabBar、上滑立即 showFloatingTabBarImmediate；base/dark 的 tab_glass_mask 恢复 #CCFFFFFF/#CC1E1E1E。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机确认已回到 23:05 第一版；后续若继续解决靠右问题，建议不再叠加 HDS barFloatingStyle 宽度参数，而单独做一个可控的自绘居中外层或等待官方布局能力确认。`
- **23:32** `fix(hds-tab): 继续修复 API23 HDS 悬浮 TabBar 胶囊仍偏右的问题：实测 adaptToHandedness=false 后真机仍无变化，说明偏移主要来自 HDS 浮动栏 barSideMargin 按“距当前边缘”解释而非左右对称 margin；本轮把 HDS 宽度策略统一为 72%，并新增 FLOATING_TAB_BAR_CENTER_SIDE_MARGIN=14%，Index 的 barFloatingStyle 改为 barWidth=72% + barSideMargin=14%，用几何关系强制左右各留 14%，避免 HDS 继续贴右边缘。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机再确认胶囊是否回到水平居中；如果仍不居中，下一步不再继续赌 HDS 的浮动布局参数，直接恢复一个轻量自绘居中外层容器承载 HdsTabs/或回退自绘胶囊，保留 API23 材质透明度和 3 秒隐藏逻辑。`
- **23:26** `fix(hds-tab): 修复上一轮加宽 API23 HDS 悬浮 TabBar 后胶囊整体偏到右侧的问题：对照截图与本机 UIDesignKit API23 声明确认 HdsTabsFloatingStyle 支持 adaptToHandedness，偏移来自 HDS 默认按单手握持/贴边能力适配；Index 的 barFloatingStyle 现显式设置 adaptToHandedness=false，让加宽后的 72% 浮动胶囊固定居中，保留此前 15vp 上抬、0.68 透明度、#26 渐变遮罩与 3 秒下滑隐藏策略。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机再看首页底部胶囊是否已回到水平居中；若居中后仍显得过宽/过窄，再只微调 barWidth，不再开启 HDS 握持侧适配。`
- **23:21** `refine(hds-tab): 按截图继续调 API23 HDS 悬浮 TabBar 观感与显隐节奏：LayoutConstants 新增 HDS 悬浮页签宽度/上抬/透明度策略；Index 的 HdsTabsFloatingStyle 接入 barWidth（小屏 72%、中屏 60%、大屏 480vp）、barBottomMargin 额外上抬 15vp，并把 barOpacity 降到 0.68；base/dark 的 tab_glass_mask 从 #CC 降到 #26，减少底部渐变遮罩压暗，让后方帖子文字更可见；TopicListPage 的底部避让同步加入 15vp 上抬量，避免最后一项/FAB 与新位置冲突；首页下滑隐藏从“立即隐藏”改为“持续向下滚动 3000ms 后隐藏”，上滑、到顶、停手会清理隐藏计时并按原 1200ms 回显策略处理。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机重点回归首页底部 HDS 悬浮宽度、距 Home 手势区距离、透明度/后方文字可见性，以及“连续下滑超过 3 秒才隐藏、上滑/停手回显”的手感；若仍偏窄或太透，我再按真机截图微调 72%/0.68/#26 这组三个参数。`
- **23:05** `refactor(hds-tab): 按 API23 UIDesignKit 新增能力把首页底部胶囊 TabBar 从手写外层玻璃悬浮容器迁到 HdsTabs 原生悬浮页签：Index 引入 HdsTabsController/HdsAnimationMode/HdsTabsFloatingStyle/hdsMaterial，HdsTabs 改用 barOverlap(true)+barFloatingStyle(...)，开启 systemMaterialEffect(materialType/materialLevel=ADAPTIVE)+thermoCtrl，并继续复用现有自定义 TabBarItem（含“我的”头像）与 TopicListPage 上报的滚动显隐；父级显隐从手写 opacity/translate 改为 HdsTabsController.applyHideAnimation/applyShowAnimation；清理旧 FloatingTabBar/FloatingTabItem 自绘容器代码。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 真机重点看首页底部 HDS 悬浮页签的沉浸光感、底部安全区距离、滚动隐藏/回显动画、切换“首页/我的”与“我的”头像是否正常；若 HDS 默认悬浮宽度和旧全宽胶囊视觉差异过大，我再加 barWidth/miniBar 策略或回收 ReadLaterBubble 到 HdsTabs miniBar。`
- **20:38** `refactor(cf-verify): 按你的要求把 Cloudflare 手动验证从 NavPathStack 路由页改成 FluxDO 同款覆盖层：Index 在根 Stack 中注册 CfChallengeService overlay listener，验证时直接显示 CfChallengePage({ overlayMode: true }) 盖在当前原生页之上；CfChallengeService.verify 不再 push PAGE_CF_CHALLENGE，而是 show challenge overlay；CfChallengePage 抽出 PageContent 以兼容覆盖层/旧路由两种形态，overlayMode 下成功/失败/取消直接 complete(...)，complete 会把 overlayVisible=false 从根 Stack 移除组件，不再经过 NavDestination pop、动画、WebView 页面销毁转场。保留旧 PAGE_CF_CHALLENGE route 仅作无 overlay listener 的 fallback。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 包再次真机触发 CF 验证，预期日志为 Show challenge overlay → cf_clearance verified ready/fast exit → overlay 直接消失，且不再出现 CfChallenge 的 navigation pop animation start/end；若仍慢，下一步只剩 WebView 自身 rc 响应前的 Cloudflare 解题耗时，需要对比 FluxDO 是否后台预验证先完成。`
- **20:28** `perf(cf-verify): 对照 D:\harmony\fluxdo 找到“秒跳”的真实差异：FluxDO 的 CF 页不是普通路由页，而是 OverlayEntry，_finish(true) 直接 entry.remove()，没有 Navigator 页面 pop 动画；并且拦截器在 showManualVerify 后会先检查 CookieJar 中是否已存在 cf_clearance，存在就立刻返回，不会固定 sleep。HarmonyDO 本轮补齐两点：RouterManager.pop 新增 animated 参数并让 CfChallengePage 成功时 RouterManager.pop(false) 无动画移除验证页；CfChallengeService.waitForCfClearanceReadyInternal 在 2s 固定等待前先立即检查 CookieStore 中的 cf_clearance，已存在则直接 Confirm immediate ok 并唤醒原请求，避免你日志里 fast exit 后又等 2 秒才 CF verify completed。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 包再测一次，预期日志应出现 navigation transition animated:0 或不再有 pop animation start/end，同时 fast exit 后紧接 Confirm cf_clearance immediate ok 与原请求重试；若仍觉得慢，再贴同段时间戳我继续把验证页改成 FluxDO 同款 Overlay/Sheet 形态。`
- **20:16** `perf(cf-verify): 按你反馈“API23 已可验证但不能像 FluxDO 一样秒跳回原生页”，继续收口 CfChallengePage 成功路径：读到新的 cf_clearance 后不再等待页面 DOM/iframe 中残留的 challenge 信号消失，而是二次 syncFromWebView 确认 CookieStore 中仍存在新 cf_clearance 后立即 fast exit + RouterManager.pop；同时把验证页前台轮询从 500ms 收到 200ms，配合 rc/challenge-platform 回调的即时 check，使验证成功后更接近 FluxDO 的秒级返回体验。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你用 API23 包再次真机触发 403→CF 验证，重点观察日志是否出现 “cf_clearance verified ready, fast exit ...” 且页面是否在验证通过后立即回到原帖子；若仍慢，请贴从 challenge-platform response 到 fast exit 的时间戳我再继续压缩检测点。`
- **19:47** `fix(cf-verify): 确认 API 23 目标版本下 Cloudflare 验证失败与 ArkWeb 第三方 Cookie 策略相关：本机 API23 SDK 的 WebCookieManager.putAcceptThirdPartyCookieEnabled 文档标注默认 false，真机现象为 Turnstile iframe 一直运行但 cf_clearance 无法同步回原生请求；ArkWebHelper 现统一显式开启 acceptCookie + acceptThirdPartyCookie，并在日志输出 ArkWeb cookie policy configured acceptCookie/acceptThirdParty，CfRefreshHost 绑定隐藏续期 Web 前也先应用同一策略；同时收窄 CfChallengePage 的 active challenge HTML 判断，不再把普通 linux.do 首页里常驻的 challenge-platform/turnstile 脚本当作活跃验证，避免 API23 下卡满 60s 后才超时。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（当前 build-profile 为 6.1.0(23)，仅保留既有 warn）。next: 你用 API23 包重新真机验证：进入验证页前应看到 ArkWeb cookie policy configured acceptThirdParty=Y；若仍回到“Cloudflare 验证未完成”，请贴 CFGuard + ArkWebHelper + CfChallengePage 含 cfLen/baseRetry/timeout 的日志，我再继续查 API23 HttpOnly cookie 读取路径。`
- **19:01** `fix(cf-verify): 根据真机日志修复 Cloudflare 主站 fallback 后过早判失败：日志显示 /challenge 404 已成功切到 linux.do 根页，但 Turnstile iframe 在 no_active_challenge_no_new_cookie pop 之后才开始输出 console；CfChallengePage 对 fallback 主站新增 8 次×3.5s 的观察窗口，期间持续等待延迟出现的 Turnstile/cf_clearance，不再 1.5s 内直接 complete(false) 进入 cooldown；readChallengePageSnapshot 增强 iframe/src 与 html 中 turnstile/challenges.cloudflare.com/cdn-cgi/challenge-platform 识别，避免把正在初始化的 cross-origin Turnstile 误判为“无 active challenge”；失败日志补充 baseRetry。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你真机再次点重试触发 403→/challenge 404→主站验证，观察是否出现 “Base url fallback still waiting...” 后等待 Turnstile 完成并自动返回原帖子；若仍失败，把含 rcIntercept/challengeResponse/baseRetry/cfLen 的日志贴我继续定位 cookie 写回。`
- **18:39** `fix(cf-verify): 修复 Cloudflare 手动验证卡在 /challenge 404/私有页：CfChallengePage 新增 loadVerifyUrl(...) 统一通过 ArkWeb controller.loadUrl() 切换验证 URL，避免只改 Web({ src }) 状态导致 WebView 仍停留在旧页面；/challenge 主框架 404 与页面正文“该页面不存在/不公开”都会优先 fallback 到 linux.do 根地址继续等待 CF 出题/写入 cf_clearance；初始 cf_clearance 快照改为优先使用 CfChallengeService 验证前备份，减少 HttpOnly/WebCookie 读不到或旧值残留导致的误判。build: D:\DevEco Studio\tools\hvigor\bin\hvigorw.bat assembleHap --no-daemon BUILD SUCCESSFUL（仅保留既有 warn）。next: 你真机清理/失效 cf_clearance 后触发一次 403 验证，确认 /challenge 404 会自动切到主站验证入口，并在新 cf_clearance 写入后自动返回原页面重试。`

## 2026.04.02

- **14:55** `refactor(search): 按你刚确认的方向把 HarmonyDO 搜索页“最近搜索”从本地 Preferences 切到服务端 API，对齐 FluxDO：1) `ApiConstants.ets` 新增 `RECENT_SEARCHES=/u/recent-searches.json`；2) `ApiClient.ets` 新增 `getRecentSearches()/clearRecentSearches()`，直接调用该接口 GET/DELETE；3) `SearchPage.ets` 删除原先基于 `search_recent_queries` 的本地读写逻辑，不再在本地保存最近搜索，而是登录态下通过 API 拉取/清空；4) 首屏搜索成功后会自动重新同步最近搜索列表，因此返回空态时能看到服务端刚记录的搜索项；5) 搜索排序 `search_sort_order` 仍保留本地持久化，不影响现有排序体验；6) 已重新执行 `hvigorw assembleHap --no-daemon`，期间先清理了损坏的 hvigor 缓存目录，最终 BUILD SUCCESSFUL（仅保留仓库既有 warn）。next: 请你真机用登录账号回归搜索页：分别验证“执行几次搜索后最近搜索能从服务端回显”“清空后重新进页仍为空”“退出登录后不再读取旧本地最近搜索”。`

## 2026.03.28

- **21:30** `fix(share-image): 修复话题详情页分享图片失败：1) 先按要求用 Playwright 复核 2026-03-28 最新 HarmonyOS Share Kit 官方文档，确认图片/文件分享应通过 `SharedData.uri` 传应用文件 URI，而不是把 `file://...` 路径塞进 `SharedData.content`；同时沙箱路径需要用 `@kit.CoreFileKit.fileUri` 生成标准文件 URI；2) `SystemShareService.ets` 已新增文件输入规范化、标准 share uri 生成和按扩展名推断精确 UTD 的逻辑，`shareImage/shareFile` 统一改为走 `uri + ShareController` 的官方口径；3) 兼容当前详情页分享图、图片预览页下载图、导出文件仍可能传入旧式 `file://<path>` 或原始 path 的场景，服务内部会自动纠正，不需要页面层重复改一遍；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机重点回归三条——A 详情页“生成分享图片 -> 分享”；B 图片预览页“分享”；C 详情页导出文件分享；若仍有个别目标应用不显示，再把目标应用名和日志贴我继续按目标应用匹配规则细调。`

## 2026.03.25

- **13:46** `fix(composer-preview-server-enrich): 继续针对你刚反馈“还是一样”的 HarmonyDO 草稿预览问题做了第二轮真因修复，而且这次先按你的要求用 Playwright 直接复核了 2026-03-25 的 linux.do 网页草稿页与编辑器真实行为，再落代码：1) 已用 Playwright 抓到网页端对同一条草稿的真实请求，确认图片短链解析接口是 **POST `/uploads/lookup-urls` + `short_urls[]=` 表单**，onebox 预览则是 **GET `/onebox?url=...&refresh=true`**，因此此前仅靠本地 Markdown 猜测的方案不够完整；2) `ApiConstants.ets / ApiClient.ets` 新增了这两个预览补全端点与调用，编辑器预览现在会在客户端进入预览时主动解析草稿里的 `upload://...` 并拉取 onebox HTML；3) 新增 `ComposerPreviewOneboxCache.ets` 与 `ComposerPreviewResourceResolver.ets`，把“解析 upload 短链 + 拉取 onebox HTML + 回写缓存 + 触发预览重渲染”串成独立链路，`CreateTopicComposerSheet.ets / ReplyComposerSheet.ets` 在恢复草稿和切换预览时都会触发这套异步补全；4) `MarkdownComposerUtils.ets` 这次继续修正 callout 规则：现在 `> [!success] / > [!info]` 后面**不带 `>` 的正文行**也会被收进同一张提示卡里，不再把正文甩到绿色/蓝色卡片外；同时若 onebox HTML 已经拉回，本地预览会直接把服务端返回的原始 onebox / 站内 quote HTML 交给 `PostRichContent` 渲染，因此 GitHub 卡片、站内话题引用卡和草稿里的应用截图都不再只剩原始 Markdown；5) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机重新打开同一条 HarmonyDO 草稿，重点看三点——A 绿色/蓝色 callout 的正文是否已经完整包进卡片；B `[grid]` 里的三张应用截图是否能回显；C 三个独占一行的链接是否都已变成网页同口径的卡片/引用。`

- **13:18** `fix(composer-preview): 修复发帖/草稿编辑器本地预览的三类问题：1) `MarkdownComposerUtils.ets` 新增对 **loose callout** 与 `> [!success] / [!info]` 样式 callout 的本地转换，预览里绿色/蓝色提示卡现在会把后续正文一起包进卡片，而不是只吃首行标题、让解释文本掉到卡片外；2) 本地预览新增对“**独占一行的裸链接 / Markdown 链接**”生成 synthetic onebox，因此草稿预览里链接卡片不再完全缺失；3) `ComposerUploadUrlCache.ets` 新增基于 Preferences 的上传短链持久化，发帖/回复编辑器进入时会先 hydrate 已上传图片映射，所以像草稿里 `upload://...` 的应用截图在重新打开草稿后也能继续在预览中回显；4) `CreateTopicComposerSheet.ets` 与 `ReplyComposerSheet.ets` 已接入预览资源 hydrate 完成后的重新渲染；5) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机重点回归同一条 HarmonyDO 草稿——确认 success/info 提示块正文已完整包裹、应用截图会显示、独占一行的链接会渲染成卡片；若还有某类特殊 onebox（例如 GitHub/无缩略图链接）样式差异，我再继续细抠。`

- **12:57** `fix(drafts): 修复“我的草稿”里部分草稿点“继续编辑”打不开的问题：1) `Draft.ets` 新增基于 `draft_key + data.action + archetypeId + recipients` 的草稿类型判定，不再只把 `new_topic / new_private_message / topic_*` 三种固定 key 当唯一真值，因此像带自定义 draft_key 的发帖草稿也能被识别成“新话题”而不是落到“暂未支持打开”；2) `DraftsPage.ets` 的打开逻辑与类型标签已改为使用这套判定，同时回复草稿的 `replyToPostNumber` 改为优先读草稿数据本身，避免只靠 `_post_` key 解析；3) `CreateTopicComposerSheet / ReplyComposerSheet / PrivateMessageComposerSheet` 新增显式 `draftKey` 入参，草稿列表进入编辑器时会继续使用原始 draft key 读写，而不是被内部默认 key 覆盖，所以现在“列表里看得到，但点进去恢复为空白/提示不支持”的草稿可以继续编辑；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机重点回归 `我的草稿`——分别验证“新话题草稿 / 回复草稿 / 私信草稿”点继续编辑都能打开；尤其是你刚提到的那条 HarmonyDO 草稿，确认现在能直接恢复到编辑器。`

- **12:39** `refine(home+topic-detail-bottom-bar-show-rule): 继续修正首页与详情页底部悬浮栏的回显时机：你补充得很关键——**延迟回显只适用于“下滑后停住”**，一旦检测到用户已经开始上滑，就应该立即回显，而不是继续傻等 1200ms；本轮已把 `TopicListPage` 与 `TopicDetailPage` 的上滑分支都切回“立刻显示”，包括回到顶部时也会立即回显；只有“纯下滑结束后停住”这一路径才继续走 1200ms 缓冲，并且若缓冲期间再次下滑会取消回显，因此现在不会再出现“下滑过程中突然上滑却一直不回显”的违和感。已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机重点回归两页——A 下滑中突然改成上滑时，底栏应立刻回来；B 纯下滑后停住不动时，才走 1200ms 延迟回显。`

- **12:34** `refine(topic-detail-bottom-bar-delay): 按你刚确认的节奏，把话题详情页底部悬浮工具栏也补上和首页同类的延迟回显，并明确使用 **1200ms**：1) `TopicDetailPage.ets` 新增 `BOTTOM_BAR_SHOW_DELAY_MS = 1200` 与 `bottomBarShowTimerId`，现在详情页向下翻阅时仍会按原规则立即隐藏，但上滑、停住、滑回顶部后不会立刻回弹，而是统一进入 1.2 秒缓冲；2) 如果这 1.2 秒里用户又继续向下翻阅，之前的回显计时会立即取消，避免工具栏在用户还在阅读时来回闪现；3) 显式即时显示的路径仍保留 `setBottomBarVisibleInstant(...)`，只是普通滚动回显链路已切到 `scheduleBottomBarShow()`；4) 页面销毁和重新加载详情数据时也会清掉这个计时器，避免旧状态串到新话题；5) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机重点回归详情页——A 向下翻阅时工具栏立即隐藏；B 停住或开始上滑后约 1.2 秒再平滑回显；C 若缓冲期间又继续下滑，回显应被取消，不再来回闪。`

- **12:26** `refine(home-tabbar-animation): 按最新反馈继续收口首页悬浮 TabBar 的显隐手感：1) `Index.ets` 里的首页悬浮胶囊 TabBar 现在不再是直接瞬间切换可见性，而是改成和话题详情页底栏同口径的过渡动画；`updateTabBarVisibility(...)` 已接入 `animateTo + EaseOut`，同时浮层本身也补了 `animation(...)`，所以首页底部 Tab 现在会像详情页一样平滑地淡入/下移，不再生硬闪现；2) `TopicListPage.ets` 新增 `FLOATING_TAB_BAR_SHOW_DELAY_MS=1200` 与 `floatingTabBarShowTimerId`，现在首页 TabBar 在向下翻阅时仍会立即隐藏，但上滑或停住后不会马上回显，而是先经过约 1.2 秒缓冲；若这段时间用户又继续往下翻，之前的回显计时会被取消，从而避免你说的“用户刚好又要滑动导致 tab 来回显示”；3) 对显式回顶/刷新这类明确动作，首页仍会立即恢复可见，避免手动操作后还要额外傻等；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机重点回归首页——A 向下翻阅时 TabBar 立即隐藏且带过渡动画；B 停住或开始上滑后，大约 1.2 秒才平滑回显；C 若这期间继续往下翻，回显应被取消，不再来回闪动。`

- **12:18** `fix(home+topic-detail-bottom-bar): 按最新反馈继续统一首页与详情页悬浮底栏显隐规则，改成“只有向下翻阅时隐藏，停留或上滑都显示”：1) 首页 `TopicListPage` 不再把底部悬浮 TabBar 直接绑死在 `headerOffset` 上，新增独立的 `floatingTabBarVisibility`；现在列表向下滚动时才会隐藏悬浮 TabBar，向上滚动或滚动停止时都会重新显示，因此不会再出现“已经往上滑回顶部了，底栏反而又隐藏”的反直觉表现；2) 首页 FAB 的底部避让、列表末尾预留空间与父级 `Index` 的悬浮胶囊 TabBar 可见性，现都统一跟随这套新规则；3) 话题详情页 `TopicDetailPage` 的底部工具栏默认改为可见，`onReachStart()`、回到顶部按钮、滚动停止场景都会保持/恢复显示；只有真正向下翻阅累计超过隐藏阈值时才收起，一旦上滑就立即恢复，不再在“从下面一路滑回顶部”时又被顶掉；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机分别回归首页与详情页——A 向下翻阅时底栏会隐藏；B 一旦停手或上滑，底栏会立即回来；C 从中下段一路上滑回顶部时，底栏不应再在顶部消失。`

- **12:03** `fix(home-refresh-animation): 继续修首页“虽然不真正刷新，但第一次回顶上滑仍会出现刷新动画”的残留问题：1) 复查本机 API 22 SDK 的 `Refresh` 定义后确认，`pullToRefresh(boolean)` 更偏向控制“超过阈值后是否进入刷新”，并不一定完全抑制拖拽中的下拉位移动画；因此这次继续把首页 `Refresh` 的**位移表现**也一并收口；2) `TopicListPage` 新增 `shouldEnablePullRefresh()`，并把 `Refresh.pullToRefresh(...)`、`pullDownRatio(...)`、`maxPullDownDistance(...)` 统一绑定到同一个 armed 条件；当首页还没稳定回到顶部时，`pullDownRatio=0`、`maxPullDownDistance=0`，直接禁止 Refresh 跟手位移，所以现在不只是不会执行 `onRefreshing()`，连那段误出现的刷新动画也会一起消失；3) 只有在“列表已到顶 + Header 完全展开 + 当前不是同一次连续手势误触”时，才恢复 Refresh 的正常拖拽表现；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机继续重点回归首页——中段下滑后一路上滑回顶时，第一次回顶手势不应再看到刷新动画；停稳后再单独拉一次，顶部刷新动画与真正刷新都应正常。`

- **11:34** `fix(home-refresh): 继续修首页“从中段回顶的第一次上滑仍会直接触发下拉刷新”的真根因，这次不再只修 FAB，而是把 `Refresh` 组件本身的触发条件一起收口：1) 已查本机 API 22 SDK 的 `Refresh` 定义，确认支持 `pullToRefresh(boolean)`，因此首页 `TopicListPage` 现在会把 `Refresh` 的可触发性绑定到真实顶部状态，而不是永远开放；2) 新增 `pullRefreshArmed / pullRefreshArmTimerId / isListTouchActive`，只要列表离开顶部或 Header 未完全展开就立即 disarm；只有**回到顶部、触摸结束、并停稳一个很短时间窗后**才重新 arm，这样“从中段滚回顶部的同一次连续手势”不会再顺手把下拉刷新拉起来；3) `List.onTouch / onReachStart / onScrollStop / onScrollFrameBegin` 已协同接入这套 armed 机制，确保只有“已经回到顶部并稳定下来后，下一次新的上滑/下拉手势”才允许触发刷新；4) `onRefreshing()` 也补了最终守卫，防止极端情况下误进回调；5) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机重点回归首页——先下滑到中段，再一路上滑回顶，确认这第一次回顶手势不再直接触发刷新；随后停稳后再单独拉一次，确认真正的顶部下拉刷新仍然可用。`

- **11:10** `fix(network): 修复当前本地重定向/代理环境下 App 冷启动首页直接报“加载失败，请下拉重试”且重试无效的问题：1) 已结合真机日志确认根因不是按钮失效，而是 `GET https://linux.do/latest.json` 在 `@kit.NetworkKit` 层持续命中 `2300060 / Invalid SSL peer certificate or SSH remote key`，导致每次重试都重复失败；2) 查阅本机 API 22 SDK 的 `@ohos.net.http` 定义后，确认 `HttpRequestOptions.remoteValidation` 支持 `'skip'`，因此在 `HttpClient` 中新增“仅调试构建生效”的 TLS 兼容开关：调试态若启用 `ALLOW_INSECURE_PROXY_SSL_IN_DEBUG`，原生请求会自动追加 `remoteValidation='skip'`，用于兼容域名被重定向到 192.168.x.x、本地 MITM/抓包证书未进系统信任链的开发环境；3) `AppContext` 现会缓存应用 `debug` 状态，避免把这类绕过逻辑放到非调试运行时；4) 同时补了 `ApiClient.isSslPeerCertificateError(...)` 与更明确的错误文案，首页/详情页再命中同类证书错误时，不会只剩模糊的“加载失败，请下拉重试”；5) 已执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你先真机重新冷启动验证首页是否已能正常拉起；若首页已恢复但登录页/Cloudflare 页在同一代理环境仍遇到证书问题，我下一步继续把 ArkWeb 侧的调试证书兼容也补齐。`

- **10:52** `fix(home): 继续修首页“下滑途中突然反向上滑，第一次上滑就百分百切到刷新”的瞬时误判：1) `TopicListPage` 新增 `refreshGestureReady`，把刷新模式改成“必须上一帧就已经处于顶部可刷新状态，本帧继续上滑才允许切入”；这样从非顶部/半折叠 Header 状态回到顶部的那一帧只会完成“回到顶部可刷新态”的准备，不会在同一帧直接误切刷新；2) `onReachStart()` 与真实刷新路径同步刷新这枚状态位，避免出现你描述的“第一次反向上滑必中、停一下又不复现”的方向切换毛刺；3) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请继续真机重点回归你刚描述的 case——下滑途中突然反向上滑，看第一次反向上滑是否仍会立刻切刷新；若还有，我下一步会继续加“方向反转冷却帧/时间窗”把这类瞬时切换彻底压掉。`

- **10:40** `fix(home): 修复首页“离顶部很远时只要上滑就很容易切到刷新模式”的误触问题：1) `TopicListPage` 新增 `isListScrollerAtTop()` / `canTriggerRefreshFromScroll()`，右下 FAB 只有在“列表真实回到顶部 + Header 已完全展开”后继续上滑时，才允许切到刷新模式；中段列表单纯上滑现在只会负责回看内容，不会再把 FAB 误切成刷新；2) `onRefreshFabClick()` 增加兜底守卫，若当前其实还没回到顶部，则只负责回顶和复位 Header，不会直接触发数据刷新，更符合你说的“先回到顶部，再上滑才刷新”；3) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请真机重点回归首页：A 在离顶部很远处连续上滑时，右下 FAB 是否保持“+”而不是过早切成刷新；B 真正回到顶部后继续上滑，是否才会进入刷新模式并触发刷新。`

- **10:29** `fix(topic-detail): 继续收口话题详情页悬浮底栏显隐手感：1) `TopicDetailPage` 的底部悬浮工具栏改为“上滑更快显示、下滑维持较稳隐藏”的非对称阈值，显示阈值从原先统一 50 收紧到 24，避免用户已经明显上滑了却还要拖很长一段才重新出现；2) 详情页滚动偏移现增加 `Scroller.currentOffset()` 异步同步，修正边界回弹/帧增量累计造成的 `scrollOffset` 漂移，减少标题分割线与悬浮底栏状态偶发不准的问题；3) 页面销毁与重新加载详情数据时会清理滚动同步定时器，避免旧滚动状态串到新内容；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请真机重点回归话题详情页：A 中段连续下滑隐藏后再轻微上滑，底部悬浮工具栏是否更快回显；B 顶部/底部回弹后标题分割线与底栏状态是否仍稳定。`

- **10:20** `fix(home+composer): 修复你刚提的两处交互问题：1) 首页 `TopicListPage` 的折叠 Header 不再要求“列表必须回到顶部”才允许上滑展开；现在中段列表上滑时也会优先回退 `headerOffset`，搜索栏 / 排序栏 / 悬浮底部 TabBar 能及时重新出现；同时 header snap 新增最近一次滚动方向记忆，向上收手时会优先回到展开态，避免只露一点点又立刻吸回去；2) 回复与发帖编辑器不再把最小长度写死为 20/15，而是改为从 `site.json` 解析 `min_post_length / min_first_post_length / min_topic_title_length`，站点改配置后客户端会自动跟随；3) 回复/正文/标题相关提示文案与计数统一从“字”改成“字符”，和实际 `string.length` 口径保持一致；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请你真机优先回归两条：A 首页中段连续下滑隐藏后，再轻微上滑，搜索栏和底部悬浮 TabBar 是否会及时回显；B 发帖/回复页分别用 19/20 边界值验证，确认客户端提示与站点实际限制一致。`

- **01:18** `refine(post-rich-content): 针对话题 https://linux.do/t/topic/1809101 继续收口正文富内容与安卓对齐：1) `PostRichContent.ets` 现已补上对 Discourse `blockquote.callout[data-callout-type]` 的识别与专门渲染，`success / info / warning / danger / tip / note` 等提示块不再继续走普通灰色引用卡片，而会按类型输出有色背景、左侧强调色边条、标题强调和图标胶囊，更接近安卓截图里的绿色/蓝色提示卡；2) onebox 链接卡片现已从标题锚点/注入后的 `data-clicks` 中提取点击数，GitHub 这类预览卡右上角会补“点击数”胶囊，不再像之前那样完全丢失点击量；3) onebox 布局同步改成更贴近安卓的结构：来源小字在上、缩略图移到左侧、标题与描述在右侧分层排布，整体边框、圆角和留白比旧版更克制；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请真机重点回归 `topic/1809101` 里的 callout + GitHub onebox，同时再顺手抽查别的话题里的普通引用、站内 onebox 与无缩略图 onebox，确认没有被这轮富内容改动带歪。`

- **00:55** `fix(cf-verify): 收口 Cloudflare 验证页落到 `/challenge` 404 后长时间不返回原生页的问题：1) `CfChallengePage.ets` 新增响应式 `verifyUrl`，不再把 Web 组件永久锁死在固定的 `/challenge` 占位地址；2) 当验证页首轮检测发现当前实际 URL 仍是 `/challenge`，页面标题/正文却已经落到“找不到页面 / page does not exist”，并且整个过程中既没有 `rc` 拦截也没有 `challenge-platform` 响应时，现会主动判定为“占位页没拿到真正挑战”，自动改为回退加载站点根地址，再给 Cloudflare 一次真正出题/写入 `cf_clearance` 的机会；3) 这样可以避免像你刚才日志里那样一直停留在 `https://linux.do/challenge` 的站点 404 页，用户体感上像是“CF 页面不跳回原生”；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 请重点真机回归两条——A 卸载重装后首次进入话题详情触发 CF 时，是否会从 `/challenge` 404 自动切到站点页并最终返回原生；B 登录过程中若再触发 CF，是否仍能正确回退并完成登录态落地。`

- **00:15** `fix(login+auth): 收口登录态确认被 Cloudflare / 429 卡死的问题：1) LoginPage.ets 新增 WebView 侧登录完成判定，只要已经拿到 `_t/_forum_session`、页面可读到 `username/csrf`，并且当前 URL 已离开 `/login`，即使 native current user 仍被 Cloudflare / 风控拦住，也会先按 Web 侧已登录状态落本地登录态；2) 登录完成阶段不再同步退化去拉 `/u/{username}.json` 做 hydration，避免 `/session/current.json` 先 429、随后又被 `/u/{username}.json` 的 403 challenge 二次卡死；3) applyLoginSuccess(...) 调整为先保存 `currentUser + cookie + csrf + is_logged_in`，再把 summary warmup 挪到延后异步执行，避免网络预热阻塞登录闭环；4) 新增 “Native current user unavailable after token, finalize login from web state ...” Hilog，便于继续排查代理 / Cloudflare 触发场景；5) 已执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仅保留既有 warn）。next: 先让你在真机回归 `登录 -> 返回首页 -> Profile`，确认 `/session/current.json` 即使暂时 429，App 也不会再停留在未登录；随后再观察高风险代理 / 高频触发 CF 环境下，current_user 延迟恢复时 Profile 是否能平滑从 fallback user 过渡到完整资料。`

## 2026.03.24

- **21:39** `chore(public-repo-repush): 用户已手动重建公开仓库 `Amaz1ny/HarmonyDO-public`；本轮我仅从本地 `_opensource_worktree` 将 `opensource` 推送到该公开仓库的 `main` 分支，没有再推送私有仓库 `origin/opensource`；同时已把公开 README 内的 clone 地址校正为 `HarmonyDO-public`，并通过 Playwright 复核仓库当前为 public、默认分支为 `main`、分支数为 `1`，且 `screenshots/home.jpg / profile.jpg / topic-detail.jpg` 已在线可见。next: 如需继续完善公开仓库，可继续补 About 链接、Topics、Release 与贡献文档。`

- **20:37** `chore(public-repo): 已按要求创建新的 GitHub 公开仓库 `Amaz1ny/HarmonyDo-public`，仓库可见性为 public；随后将本地 `opensource` 分支内容推送到该公开仓库的 `main` 分支，因此公开仓库当前仅有 1 个分支（main），不会连带暴露私有仓库里的其它开发分支。verify: Playwright 已确认仓库主页为 public，默认分支为 `main`，分支数为 `1`。next: 如需进一步完善公开仓库，可继续补仓库头像、Topics、About 链接、Issue 模板、Contributing 与 Release。`

- **20:21** `chore(open-source-branch): 已按要求整理并推送 GitHub 开源分支 `opensource`：1) 使用独立工作树避免污染当前开发分支；2) 远端仅保留 `.gitignore / README.md / LICENSE / build-profile.json5 / code-linter.json5 / hvigorfile.ts / oh-package*.json5 / AppScope / hvigor / entry`；3) 其余 Markdown 文档（除 README）、AGENT、本地配置、缓存与 IDE 目录未上传；4) `build-profile.json5` 已改为无真实密钥的占位签名模板，供开源用户自行配置本地签名。push: `origin/opensource` 成功。next: 如需进一步开源化，可继续补 `.gitignore`、贡献指南、Issue 模板和发布说明。`

- **20:03** `docs(readme-expand): 继续完善根目录 README.md：在首版简介基础上补齐了安装方式（当前仅支持 DevEco Studio 编译安装）、环境要求与安装步骤、项目特性、核心功能、技术特性、系统级 API / Kit 接入清单、已声明权限、项目结构说明、主要页面、关于 Linux.do、非官方声明、GPL-3.0 开源协议以及致谢/参考链接；同时同步更新 UI_REPLICA_STATE.md 里的文档进展记录。next: 如需继续增强 README，可再补应用截图、功能演示图、FAQ、构建命令与开发贡献说明。`

- **19:52** `docs(readme): 新增根目录 README.md 初稿，补齐 HarmonyDO 的项目名称、简介、Telegram 入口、项目特点、当前状态、免责声明、致谢与 License 入口；同时按会话协议同步更新 UI_REPLICA_STATE.md，补充本轮文档进展。next: 后续可继续补 README 的安装方式、功能截图、功能清单与开发说明。`

- **09:18** `feat(composer-image-grid-preview): 继续推进发帖/回复编辑器图片链路与 FluxDO 安卓端对齐：1) `MarkdownComposerUtils.ets` 已对齐上游多图策略，上传完成后 **≥3 张图片会自动包裹 `[grid]...[/grid]`**，不再只做逐张竖排插入；2) 本地预览现已识别 `[grid]` 并输出 `div.d-image-grid`，因此发帖/回复预览态会直接走现有 `PostRichContent` 图片网格渲染，不再只是看到原始标记或单列堆叠；3) 新增 `ComposerUploadUrlCache.ets`，上传成功后会把 `short_url -> url` 记入会话内缓存，因此同一次编辑会话里刚上传的图片在预览态可以直接回显；4) `CreateTopicComposerSheet.ets` / `ReplyComposerSheet.ets` 的上传完成提示已补“网格”语义，用户能明确知道多图已按 Android/FluxDO 方式整理；5) 已同步更新 `UI_REPLICA_STATE.md / tasks.md / checklist.md` 真值文档；6) 已执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ComposerImageService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机优先回归四条——A 发帖上传 1/2/3/5 张图片时插入结果是否符合预期；B 预览态单图与 grid 是否都能正常显示；C 发布后服务端 cooked 内容是否与预览一致；D `我的草稿 -> 继续编辑` 后同会话新上传图片是否仍能正常回显。若这轮稳定，我下一步继续补图片编辑/压缩 dialog、历史草稿 `upload://` 短链冷启动解析，以及 sticker 长按滑动预览。`

- **01:21** `feat(composer-sticker-preview-phase3a): 继续推进发帖/回复编辑器第三期能力，先补上最影响体感的 sticker 长按预览：1) `ComposerStickerPanel.ets` 现在已把表情包宫格从单纯 `onClick` 改成 `TapGesture + LongPressGesture` 独占手势，避免长按时误触插入；2) 新增面板内预览 overlay，长按表情包会弹出居中的大图预览卡片，带名称与“轻触关闭”提示，更接近 FluxDO 安卓端“长按先看大图再决定插不插”的使用路径；3) 切换分组、打开市场、实际插入 sticker、刷新订阅时都会自动收起预览，避免面板状态残留；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机优先回归两条——A 发帖/回复里长按 sticker 是否能稳定弹出大图预览、轻触遮罩后是否能正确关闭；B 长按后再切分组/打开市场/直接点别的 sticker，overlay 是否会正确收口。`

- **01:07** `feat(composer-sticker-panel-phase2): 继续把首页发帖/回复编辑器往 FluxDO 安卓对齐，完成第二期 sticker panel / sticker market 最小可用版：1) 新增 `Sticker.ets`、`StickerMarketService.ets`、`ComposerStickerPreferences.ets`，补齐表情包市场模型、24 小时缓存、订阅分组与 recent sticker 持久化；2) 新增 `ComposerStickerPanel.ets`，发帖与回复编辑器现在除了 emoji 外，还能切到 **表情包** 面板，支持最近使用、已订阅分组切换以及点击插入上游同款 `![name|widthxheight,30%](url)` Markdown；3) 新增 `ComposerStickerMarketSheet.ets`，支持在市场里搜索分组、添加/移除订阅；4) 新增 `ComposerEmojiStickerPanel.ets`，把现有 emoji 面板升级成 emoji / 表情包双 tab，`CreateTopicComposerSheet.ets` 与 `ReplyComposerSheet.ets` 已统一接入；5) `MarkdownComposerUtils.ets` 已补 `insertBlockTextAtSelection / insertStickerMarkdown`，因此表情包会按当前光标位置作为块级 Markdown 插入，而不是只能追加到末尾；6) 本地预览对 sticker Markdown 已做基础兼容，至少能在预览里看到图片块，不会只剩原始语法；7) 已执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你这一轮真机优先回归三条——A 发帖 -> 打开表情包 tab -> 从市场添加分组 -> 选择表情包 -> 预览 / 发布；B 回复 -> 打开表情包 tab -> recent 是否回填 -> 发送；C `我的草稿 -> 继续编辑` 后 sticker Markdown 是否保留。`

- **00:39** `feat(composer-emoji-panel-phase1): 按你确认的“先做第一期 emoji 面板”继续把首页发帖/回复编辑器往 FluxDO 安卓对齐：1) 新增 `ComposerEmojiPanel.ets`，发帖与回复编辑器现在都支持底部 emoji panel，不再是旧版“点一下只插一个 😊”的占位按钮；2) 面板已接上 `/emojis.json` 真正的站点表情库，并补了失败时的内置 fallback + recent emoji 持久化（`ComposerEmojiPreferences.ets`），因此搜索与最近使用都能工作；3) `CreateTopicComposerSheet.ets` / `ReplyComposerSheet.ets` 已统一接入 emoji panel 展开/收起、面板激活态工具栏高亮、编辑区 focus 时自动收起等交互；4) 补了 `TextAreaController` 相关光标状态维护，选中 emoji 后不再只能无脑追加到末尾，而是按当前编辑位置插入 shortcode；5) `MarkdownComposerUtils.ets` 已增强本地预览，对 `:emoji:` shortcode 会转换成 `<img class="emoji">`，这样预览态也能看到标准/custom emoji；6) `ApiClient.ets` 新增 emoji 数据缓存与 mock/fallback 入口，`EmojiResolver.ets` 也补了 custom emoji 枚举能力，供面板 fallback 使用；7) 已执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你下一轮真机优先回归两条——A 首页 FAB -> 创建话题/我的草稿 -> 打开 emoji 面板 -> 搜索/插入/预览/发布；B 话题详情 -> 回复 -> 打开 emoji 面板 -> 搜索/插入/预览/发送；如果第一期手感稳定，我下一步继续做 sticker panel / sticker market 与更完整的选区级格式化。`

## 2026.03.23

- **23:52** `feat(topic-compose-align): 按你的要求继续把“首页发帖 / 草稿 / 回复”往 FluxDO 安卓对齐，并修掉上一轮被 BOM 搞坏导致的 ArkTS 编译阻塞：1) 新增 `MarkdownComposerUtils.ets` 与 `MarkdownComposerToolbar.ets`，先补上一套可复用的 Markdown 工具栏与轻量预览转换；2) `TopicListPage.ets` 这轮已正式接通首页 FAB 展开菜单，登录态普通 FAB 可展开 **创建话题 / 我的草稿**，并绑定新的发帖 Sheet；3) `CreateTopicComposerSheet.ets` 已重构为更接近安卓的发帖编辑器：顶部返回/舍弃/发布、大标题、分类选择、标签选择、字符计数、预览、底部 Markdown 工具栏全部接上，同时支持“检测到历史草稿时恢复/丢弃确认”，从 `DraftsPage` 进入则直接恢复；4) `ReplyComposerSheet.ets` 也已重构为更接近上游 `ReplySheet` 的结构：顶部关闭/舍弃/发送、回复目标提示、字符计数、预览、底部工具栏、草稿自动保存与舍弃确认均已打通；5) `DraftsPage.ets` 的话题草稿继续编辑链路已补齐 `requestKey / categoryId / tags / openedFromDrafts` 入参，`我的草稿 -> 继续编辑` 终于能回到新编辑器上下文；6) 同步修掉 4 个新文件的 UTF-8/BOM 问题以及 `@Builder` 链式调用导致的 ArkTS 结构错误；7) 已执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你下一轮真机优先按两条链回归——A 首页 FAB -> 创建话题 / 我的草稿 -> 继续编辑 / 发布；B 话题详情 -> 回复 / 引用回复 -> 稍后继续 -> 我的草稿；若链路稳定，我就继续细抠 FAB 子按钮动画、发帖/回复编辑器视觉和 DraftsPage 卡片细节。`

- **21:25** `feat(profile-dashboard-a): 按你确认的 A 方案，把 Harmony 端“我的”页从“对齐安卓的功能卡片页”继续升级为更克制的轻仪表盘式个人工作台：1) `ProfilePage.ets` 登录态头部已改成弱沉浸卡片头，补上“个人工作台”caption、halo 头像、基于信任等级色的轻 tint 面板与胶囊化顶栏操作区；2) 社区表现区不再是单排白卡，改成带标题/副标题/成长 chip 的数据面板，加载态与占位态也统一成同一套层级；3) 顶部入口区升级为 2×2 工作区卡片，把 **我的话题 / 我的书签 / 我的草稿 / 我的徽章** 改成更像工作台的 tile，而不是简单图标宫格；4) 下方功能区重新分成 **浏览与成长**（浏览历史 / 信任要求 / 邀请链接）与 **扩展服务**（元宇宙 / AI 模型服务 / 外观设置 / 网络设置 / 功能设置 / 数据管理 / 关于 FluxDO）两组，并为每个入口补了 subtitle，信息层级更清晰；5) 新增一批 `profile_*` 字符串资源，避免继续把这套新结构写死在代码里；6) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机重点看四点——A 头部弱沉浸感是否比旧版高级；B 社区表现卡的信息层级是否顺眼；C 2×2 工作区是否像“个人工作台”而不是普通宫格；D 浏览与成长 / 扩展服务 两组的节奏是否还要继续压缩或再拉开。`

- **20:56** `refine(profile-quick-access-layout): 按你给的“我的”页鸿蒙/安卓对比图，把登录态个人页入口区改成更接近安卓截图的两段式卡片结构，并按你的额外要求调整分组：1) `entry/src/main/ets/views/pages/ProfilePage.ets` 不再把“我的书签 / 我的草稿 / 我的话题 / 我的徽章 / 浏览历史 ...”全部塞进同一张长列表卡片；2) 现在已新增顶部快捷入口卡片，把 **我的话题 / 我的书签 / 我的草稿 / 我的徽章** 4 个入口收成同一张四宫格卡片，图标、留白和居中方式更接近安卓端的快捷功能区；3) `浏览历史` 已按你的要求从上方快捷区移出，放到下面的列表卡片里，与 `信任要求 / 邀请链接 / 元宇宙` 同组展示，不再像安卓图那样放在上面；4) 现有路由与点击逻辑保持不变，只调整布局和分组；5) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机重点看“我的”页这两张卡片——A 四宫格卡片的顺序、图标大小和文字对齐是否符合你的目标；B 浏览历史是否已经被移到下面列表；如果还要继续往安卓那种更紧凑的 icon 方块、卡片阴影和上下间距收，我下一轮就继续细抠。`

- **20:46** `refine(search-filter-category-visual): 根据你追加的鸿蒙/安卓对比图继续收高级搜索分类区：1) 已确认上一轮不仅是顺序问题，还存在“父分类块在鸿蒙端仍然视觉居中”的问题；本轮已给 `SearchFilterPanel.ets` 里每个分类分组显式补上 `.alignItems(HorizontalAlign.Start)`，父分类 chip 不会再像之前那样悬在中间，而会像安卓截图一样贴左分组展示；2) 高级搜索里的分类 chip 图标也不再只用“圆点/锁”，而是接入现有 `BadgeIconUtils.getCategorySymbol(...)`，优先按分类 `icon` 映射系统 symbol，视觉层级更接近安卓端的公告/活动/悬赏/模板等分类样式；3) 子分类引导线、父/子分类点击选择逻辑保持不变；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你安装这一版后重新打开高级搜索，重点看父分类是否已左对齐、图标是否更接近安卓；如果还有“某些父分类只显示了 Lv1、没看到 Lv2/Lv3”，我下一轮就继续沿着分类数据源和子分类解析口径深挖。`

- **20:38** `fix(search-filter-category-layout): 按你给的安卓截图继续收口高级搜索 Sheet 的分类布局：1) `entry/src/main/ets/views/components/search/SearchFilterPanel.ets` 已去掉原先“孤立父类 / 分组父类”二段式拆分，不再先把所有无子分类父类堆在前面、再统一渲染有子分类父类；2) 分类区现改为按顶级分类原始顺序逐组输出，保留“全部”在最前，然后每个顶级分类先显示父分类 chip，再在其下方显示对应子分类，因此像“开发调优”这类父分类不会再被整体推迟到后面，布局更接近你给的安卓端；3) 子分类左侧引导线、父/子分类选择与取消选择逻辑保持不变；4) 已重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机打开高级搜索，重点确认“开发调优 / 国产替代”等父分类是否已经按安卓截图顺序出现在前面，且父分类下的 Lv1 / Lv2 / Lv3 仍能正常点击与生效。`

- **20:31** `fix(search-tags): 根据你刚反馈的“搜索结果只显示分类、不显示标签”，继续排查并修复 SearchPage 标签链路：1) 根因已确认，不是 `SearchPostCard` 没渲染，而是 `entry/src/main/ets/models/SearchResult.ets` 里的 `SearchTopic.fromJson(...)` 之前只兼容 `tags: ['a', 'b']` 这种字符串数组；2) 上游 FluxDO Flutter 端的 `Tag.fromJson(...)` 实际兼容字符串和对象两种格式，而站点搜索结果里标签可能是对象结构，因此 Harmony 端之前把对象标签全丢了，最终就只剩分类 badge；3) 本轮已给 `SearchTopic` 补上 `parseTagName(...)`，兼容 `string / { name } / { slug } / number / boolean` 等输入，搜索卡片里的标签现在会正常透传到 `SearchPostCard -> TagBadge`；4) 已再次执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机直接回到同一条搜索结果，看分类右侧的标签是否已经恢复；如果还有“部分帖子有标签、部分没有”的情况，我下一步就继续对着真实响应把搜索 topic 的其它字段也一起补齐。`

- **20:18** `refine(search-ui): 按你给的 FluxDO 安卓搜索页截图，把 Harmony 搜索页重做成更接近上游的“简洁顶栏 + 排序栏 + 帖子主列表卡片流”：1) 新增 `SearchSortOrder`，排序正式支持 `相关性 / 最新帖子 / 最受欢迎 / 最多浏览 / 最新话题`，并持久化到 Preferences；2) 搜索请求构造现已对齐上游 Flutter，把排序拼成 `order:xxx` 直接走真实 Discourse 搜索语法，同时搜索接口改成 `type_filter=topic`，结果流不再显示旧的“话题/帖子/用户三分区”，而是以帖子卡片为主、用户放到列表尾部分组；3) 新增 `SearchPostCard.ets`，补齐头像、标题、摘要、分类徽章、标签、点赞与相对时间，整体卡片层级和安卓截图更接近；4) `SearchPage.ets` 顶栏改为更轻的搜索 AppBar，下面新增文本式排序入口与结果计数，保留高级筛选与激活条件条；5) 同步更新 `tasks.md / checklist.md / UI_REPLICA_STATE.md`，让文档真值与当前搜索页行为保持一致；6) 构建验证已完成，期间先清掉损坏的 hvigor cache 后重新执行 `hvigorw assembleHap --no-daemon`，最终 BUILD SUCCESSFUL（仍仅剩 `ToastService / HoldingHandService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机重点回归搜索页四项——A 顶栏和排序栏视觉是否接近安卓截图；B 5 个排序项切换后结果顺序是否明显变化；C 搜索结果卡片的标题/摘要/标签/点赞时间层级是否顺眼；D 用户结果出现在帖子列表尾部时样式是否还需要再压一轮留白和字号。`

## 2026.03.22

- **20:16** `fix(bookmark-custom-preview): 根据你这次截图继续收口“选完日期/时间后，自定义提醒右侧时间没变”的问题：1) 根因已确认，之前右上角预览文案一直读取的是 `customDate/customHour/customMinute/customSecond` 这组“已确认值”，而你在原生 `DatePicker/TimePicker` 里刚改动的其实是 `pickerDate/pickerTime` 草稿值，所以在点“确认自定义”之前预览不会变化；2) 本轮已把 `BookmarkEditSheet.ets` 顶部自定义摘要改为直接读取 `pickerDate/pickerTime`，因此只要你在日期/时间选择器里选完并关闭 sheet，卡片右上角预览就会立刻刷新，不必等到最后再点一次“确认自定义”才看到变化；3) 保存语义保持不变——摘要会实时跟随草稿，但真正写回书签提醒仍然只发生在点击“确认自定义”之后；4) `hvigorw assembleHap --no-daemon` 已重新 BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你现在重点回归“选择日期/选择时间 -> 关闭 picker -> 看右上角摘要是否立刻变化 -> 再点确认自定义 -> 看上方正式提醒是否同步”，如果还有一处不跟手，我继续把摘要与已选值状态再完全拆干净。`

- **20:13** `fix(bookmark-date-picker-sheet): 根据你最新这句“选择日期拉得过大，而选择时间是对的”，继续把编辑书签里原生日期选择器的弹层高度往安卓/Harmony 正常形态收：1) 已将 `BookmarkEditSheet.ets` 中日期弹层的 `bindSheet` 高度从 `SheetSize.LARGE` 改为 `SheetSize.FIT_CONTENT`，不再强行占据大半屏，因此不会再出现日期轮盘下方留出一大块空白；2) 时间弹层保持现状不动，确保你已经确认“时间是对的”的这一部分不被回归；3) 日期 picker 同时显式补上 `.lunar(false)`，避免后续设备默认配置影响公历选择表现；4) `hvigorw assembleHap --no-daemon` 已重新 BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你现在直接真机回归“编辑书签 -> 自定义 -> 选择日期”，重点看弹层高度是否已经和时间选择器一样明显收紧、底部空白是否消失；如果还有轻微留白，我下一轮继续抠 `DatePicker` 外层容器与底部 padding。`

- **20:00** `refine(bookmark-native-picker): 按你这轮反馈继续把编辑书签“自定义提醒”从自绘选择器切到更正确的鸿蒙原生能力：1) 你指出的三个问题都成立——日期条只能选少量日期、分钟只有 00/30、没有秒，这和安卓以及 Harmony 原生 picker 口径都不一致；2) 本轮已把 `BookmarkEditSheet.ets` 的自定义提醒正式改为原生 `DatePicker + TimePicker` 组合：日期现在支持完整年月日范围（当前实现允许从 2024-01-01 到 2100-12-31），时间改为 `TimePickerFormat.HOUR_MINUTE_SECOND`，因此小时/分钟/秒都可以完整选择；3) 自定义弹层仍保留我们上一轮做好的卡片式视觉骨架，但中间控件不再手搓，而是直接使用系统 picker，既保留了一定的美观度，也把可选范围和交互准确性拉回到正确轨道；4) 选择结果现在会完整落到 `customHour/customMinute/customSecond`，保存后书签提醒会带上秒级时间；5) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机重点回归两条——A 日期是否已经能完整切换年月日而不是只有少量几天；B 时间是否已经能选到任意分钟和秒；如果观感上还想更贴近安卓系统弹窗样式，我下一轮继续把这个原生 picker 的容器层级、标题区、底部按钮和留白再细抠。`

- **19:36** `feat(bookmark-custom-picker): 按你指定的 frontend-skill 方向，把编辑书签里的“自定义提醒”从临时加减面板升级成一套更像鸿蒙原生、也更美观的时间选择器：1) 保留顶部当前提醒概览与快捷预设，但点击“自定义”后不再直接在正文里塞一组“前一天/后一天、±30分钟”按钮，而是浮起独立的自定义选择层；2) 新的自定义层采用更克制的鸿蒙式卡片结构：顶部标题 + 当前时间预览，中间是横向日期选择条（今天起 14 天）和两个选择列（小时 / 分钟），底部是取消 / 确定操作；3) 日期选中态使用主色高亮卡片，小时/分钟则采用简洁的列式选择器，选中项有明显的背景和字体层级，避免旧方案那种“功能能用但不够像选择器”的观感；4) 交互上也更合理：进入自定义时先用当前提醒时间或初始提醒做草稿，取消不会污染当前值，只有点“确定”才真正把所选日期时间写回书签提醒；5) 编译已通过，`hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机重点看三点——A 自定义层视觉上是否已经比之前更像一个正式的鸿蒙时间选择器；B 改日期/小时/分钟后点确定是否能正确回显并保存；C 若你还想再更像系统 picker，我下一轮继续把列式选择器做成更强的滚轮感和焦点遮罩。`

- **18:57** `fix(bookmarks-preview): 根据你这轮真机截图继续收口两处问题：1) “从长按预览进入编辑书签，再点取消后出现一大片白屏”的根因已确认，是 `BookmarksPage.ets` 里关闭书签编辑 Sheet 时过早把 `editingTopic` 置空，导致 Sheet 还没真正消失时内容先被清掉，界面上就只剩一个空白白板；现在改为 `closeBookmarkEditor()` 只负责把可见性置为 false，真正的 `editingTopic` 清理由 `bindSheet(... onDisappear)` 在 Sheet 完全消失后再做，因此取消/关闭不再出现白屏；2) 书签长按预览已进一步按安卓截图收紧：`TopicPreviewDialog` 新增 `compactMode/showParticipants` 参数，书签页现在走紧凑模式，宽度/高度缩小、内边距减少、标题/正文/底栏上下留白同步收紧，底部“关闭”改成左对齐；3) 书签页的长按预览已明确关闭参与者区块，对齐你给的安卓书签预览图，不再额外显示“参与者”这一行；4) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你现在重点真机回归两条——A 长按书签 -> 编辑书签 -> 取消/关闭，确认白屏已消失；B 长按书签预览的卡片尺寸、留白、关闭按钮对齐是否已经明显更贴近安卓，如果还差一点，我下一轮就继续细抠圆角、按钮宽度和动作卡片间距。`

- **18:10** `refine(bookmarks-ui): 根据你给的三张对比图，继续把“我的书签”长按交互往安卓收：1) `BookmarksPage.ets` 已去掉书签卡片右上角那个 Harmony 端自加的“三点/ellipsis”入口，书签编辑不再要求额外点右侧按钮，统一回到安卓的“长按进入操作”语义；2) 书签长按预览层已改成更接近安卓的两段式结构：上方仍是预览卡片（关闭/分享/查看详情），下方单独增加动作卡片，提供“编辑书签 / 删除书签”，不再把“编辑书签”塞进主操作栏，也不再出现截图里那个多余的左下角省略号按钮；3) `BookmarkEditSheet.ets` 已按你截图重排：顶部改成书签图标 + 标题 + 右侧关闭图标，底部操作改成“删除 / 取消 / 保存”三段；4) 提醒预设已改成更贴近安卓的一组：`2小时后 / 明天 / 3天后 / 下周 / 自定义`；5) 由于当前仓内还没有现成日期时间选择器封装，本轮先把“自定义”做成了可直接手测的日期/时间自定义面板：可以在编辑书签里单独调整日期（前一天/后一天）和时间（±30 分钟），实时生成目标提醒时间，先把业务能力补齐到可用可测；6) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你真机重点回归两条——A 书签页单击卡片进详情、长按卡片出预览、预览下方只剩“编辑书签/删除书签”；B 编辑书签里点“自定义”后，日期和时间调整、保存、再次进入回显是否稳定；如果这两条稳定，下一轮我再继续把编辑书签的自定义时间控件做得更像安卓原生选择器。`

- **17:49** `fix(bookmarks): 根据你刚才“详情页已能添加书签，但我的书签一直显示暂无书签”的真机反馈继续深挖并已完成根因修复：1) 问题不在书签创建接口，`POST /bookmarks.json` 这一段其实已经成功；真正断点在于 Harmony 端 `TopicListResponse.fromJson(...)` 之前只会解析标准 `topic_list.topics` 结构，而当前站点的 `/u/{username}/bookmarks.json` 返回的是 Flutter/安卓早已兼容的 `user_bookmark_list.bookmarks`（以及部分场景下的根级 `bookmarks`）结构，所以书签页请求虽然成功，最终却被解析成空数组；2) 现已按 Flutter 真值把 `entry/src/main/ets/models/Topic.ets` 的 `TopicListResponse.fromJson(...)` 扩成多结构兼容：除标准 topic list 外，同时支持 `user_bookmark_list.bookmarks` 与根级 `bookmarks`；3) 书签项解析时会同步把 `id -> _bookmark_id`、`topic_id -> id`、`name/reminder_at/bookmarkable_type/linked_post_number` 映射到 Harmony 当前 `Topic` 模型使用的 `_bookmark_*` 字段，并补齐 `posts_count/reply_count/last_posted_at/posters/last_poster_username/views/like_count` 这些列表卡片依赖字段，这样书签页终于能像安卓一样把真实书签卡片渲染出来；4) `hvigorw assembleHap --no-daemon` 已再次 BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你现在可以直接真机按“详情页添加书签 -> 我的书签 -> 编辑书签/删除书签 -> 下拉刷新”整条链路回归；如果这条链稳定，我们下一步就继续推进“我的话题 / 我的徽章 / 浏览历史 / 设置”这一批业务页。`

- **17:38** `fix(topic-detail+bookmark): 根据你最新反馈收口“已登录但详情页点书签仍提示请先登录”这条真实业务阻塞，并把书签/草稿测试链路补到可直接自测：1) 根因已确认，不是登录态真的丢了，而是 `TopicDetailPage.ets` 的更多菜单里“书签”项此前根本还是占位实现，点击后无论是否登录都直接 `ToastService.showLoginRequired()`，所以你才会在已登录账号下也一直看到“请先登录”；2) 新增 `ApiClient.bookmarkTopic(...)`，正式接通 `POST /bookmarks.json` 的话题书签创建能力，详情页现在可以真正把当前话题加进书签；3) `TopicDetailPage.ets` 已补齐话题级书签状态：除 `bookmarked` 布尔值外，现在还会从 topic detail 响应里的 `bookmark_id / _bookmark_name / _bookmark_reminder_at` 以及 `bookmarks[]` 数组中恢复话题书签 ID、备注和提醒时间，避免“已书签但拿不到 bookmarkId 无法编辑/删除”；4) 详情页“书签”菜单现已改成真实业务：未书签时显示“添加书签”，点击后直接创建书签并自动拉起 `BookmarkEditSheet`；已书签时显示“编辑书签”，点击直接进入编辑 Sheet；Sheet 内保存/删除会同步回写当前详情页本地状态；5) 这样一来你现在已经能在应用内自己造测试数据：书签可直接在详情页右上角更多菜单里创建，草稿则仍可通过详情页回复 Sheet 或发新话题 Sheet 输入内容后点“稍后继续”生成，再去“我的书签 / 我的草稿”回归；6) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你下一轮先真机按“详情页 -> 更多 -> 添加书签 -> 编辑/删除 -> 我的书签”这条链路回归；然后再按“详情页回复 -> 输入几字 -> 稍后继续 -> 我的草稿”回归，如果草稿链路还有断点，我就继续把草稿入口和恢复语义一起收口`

- **17:17** `feat(bookmarks+drafts): 正式切回业务开发主线，先把“我的书签 / 我的草稿”收口到更接近安卓/Flutter、可直接手测的状态：1) 盘点后确认这两个页面、路由、Profile 入口与 API 并不是缺页，而是已经存在，当前工作的重点应从“补页面”转成“补交互缺口”；2) `DraftsPage.ets` 已补上 Flutter/安卓一致的删除确认弹层，点击“删除”不再直接删，而是先弹出确认卡片，展示草稿标题、删除不可恢复提示，并在确认中保留删除中态与失败后留在弹层内可重试；3) `BookmarksPage.ets + TopicPreviewDialog.ets` 已补上书签页长按预览里的“编辑书签”快捷动作，用户现在可以从预览卡片直接进入 `BookmarkEditSheet`，不必先关闭预览再回到列表点右上角更多按钮；4) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你下一轮可直接真机回归两条链路——A 书签页长按任意书签 -> 预览 -> 编辑书签 -> 保存/删除/清提醒；B 草稿页点击删除 -> 确认弹层 -> 取消/确认删除/删除失败重试——若这两条都稳定，我们就继续推进业务主线里的 `我的话题 / 我的徽章 / 浏览历史 / 设置` 收口`

## 2026.03.21

- **21:43** `fix(cloudflare): 继续针对你这次“明明做完验证却回原生仍提示未完成”的真机日志收口 `CfChallengePage`，并直接对齐 Flutter/安卓的完成判定语义：1) 根因已进一步钉死，不是用户没完成 Cloudflare，而是 Harmony 端此前只把 `/rc/` 拦截当成主要完成信号，且在 `/challenge` 跳到“该页面不存在”这类过渡页后，会因 `no_active_challenge_no_new_cookie` 过早判失败；2) `CfChallengePage.ets` 现已新增 `challenge-platform` 被动完成信号，注入脚本会同时监听 `XMLHttpRequest` 与 `fetch` 对 `/cdn-cgi/challenge-platform/...` 的响应，并通过新的 `onChallengePlatformResponse` JS bridge 回传原生；3) 原生 `callRcEndpoint(...)` 也会把 `native_rc` 响应统一走同一条 `handleChallengePlatformResponse(...)` 逻辑，触发多轮 `cf_clearance` 检查与延迟 no-challenge 复检，避免重定向或 JS 上下文切换时把完成信号丢掉；4) `checkNoChallengeState()` 现在会在 `notFound` / `rcIntercept` / `challenge-platform 响应已出现` 这几类高价值信号下额外等待 2 轮，而不是第一时间退出失败，因此更接近 Flutter `_injectChallengeInterceptor + _pollCfClearance + _scheduleNoChallengeCheck` 的组合判定；5) 验证页退出日志已补充 `challengeResponse/retry` 维度，便于下轮根据 Hilog 直接判断是“没收到 challenge-platform 回调”还是“收到了但 cookie 仍没落地”；6) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你直接复测刚才那个详情页 Cloudflare 场景，重点看是否出现 `Challenge platform response observed ...` 与 `No active challenge and no new cf_clearance yet, wait longer ...`；若仍失败，把这两条新日志和最终 `Exit verify page ...` 一起发我，我就继续追是 ArkWeb cookie 落地慢，还是 native retry/header 还没完全接上`
- **21:25** `fix(cloudflare+topic-detail): 根据你这次真机日志继续收 Cloudflare 前台链路：日志已确认详情页请求 `GET /t/1791848.json` 命中真实 CF 403 后，`HttpClient` 已进入“showing verify page”分支，但 `CfChallengeService.verify()` 因仍在 cooldown 内直接返回 false，导致 `TopicDetailPage` 立刻收到 `Cloudflare verification failed or cancelled` 并显示“加载失败”；本轮改动为：1) `CfChallengeService.ets` 对“前台非静默请求”放宽 cooldown，若当前 verify context 属于交互式请求，则记录 `Verify cooldown bypassed for interactive request ...` 后直接重开验证页，不再硬拦成空失败页；2) `TopicDetailPage.ets` 的错误日志改用 `ApiClient.formatError(...)`，后续再失败时不会只剩 `{}`，若用户取消/验证未完成，也会明确显示 `Cloudflare 验证未完成，请重试`；3) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 你直接复测刚才那个详情页入口，重点看这次是否会真正拉起 Cloudflare 验证页，而不是一碰就掉“加载失败”；若仍失败，把新出现的 `CfChallengeService/HttpClient/TopicDetailPage` 三段日志继续发我`
- **21:13** `fix(auth+messagebus): 继续把登录/退出后的后台会话切换收口到可手测状态：1) `Index.ets` 不再只是在首屏游客态硬启动 `TopicTrackingService.start(false)`，现在由全局 auth listener 统一协调 `TopicTrackingService + PreloadedDataService`，启动时会按当前登录态选择 guest/full 模式，登录/退出后也会自动 stop 旧 tracking、清掉旧 shared_session_key / tracking meta，再按新会话重新启动；2) `PreloadedDataService.ets` 新增 `clearSessionScopedState()`，会保留 long_polling_base_url 但清掉 shared session / topic tracking 缓存，并把 `loadConfigOnly/load/configureBusFromCacheIfNeeded` 都补上 `AuthSessionService` generation 校验，避免旧会话异步结果回写当前 MessageBus 配置；3) `TopicTrackingService.ets` 也补上 guest warmup/full start 的 generation 校验，登录页进入、登录成功、退出登录这些会话切换后，旧 warmup/旧 full-start 不会再把旧 tracking state 串回当前会话；4) `LoginPage.ets` 删除登录成功后的重复 `bootstrapLoggedInSession()`，后台 bootstrap 责任重新回到 `Index` 单点协调；5) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。next: 真机优先按“游客启动 -> 登录成功 -> 回首页/切其它页 -> 退出登录 -> 再登录”这条链路回归，重点看 MessageBus/incoming banner、Profile 登录态切换、以及 logout 后是否还会混入旧会话数据；若这条链稳定，再继续把 `AuthSessionService` 扩到更多页面级请求`
- **20:29** `refactor(network+auth): 按“先把 Cloudflare / Cookie 与安卓对齐”这条主线完成一轮底层收口：1) 新增 `AuthSessionService.ets`，引入会话 generation，登录页进入与退出登录时都会推进代际，网络层现在能识别“旧会话返回的响应”；2) `AuthService.ets` 不再把 `auth_cookies` 当作运行时真源，只保留一次性 legacy 迁移：启动时若发现旧 `auth_cookies`，会单次迁移进 `CookieStore` 后删除旧键；登录态持久化现在只保留 `csrf / username / is_logged_in / current_user`，Cookie 真源正式收口到 `CookieStore`；3) `HttpClient.ets` 去掉对 `AuthService.cookies` 的回退依赖，请求头与 CSRF 链路全部以 `CookieStore` 为准，并在收到响应时按 `AuthSessionService` 判断是否仍属当前会话，旧会话响应不再把 `Set-Cookie` 写回，也不会再触发过期会话上的 CF challenge；4) `CookieStore.ets` 新增 `getCookieRecord / restoreCookieRecord`，`CfChallengeService.ets` 现在会在进入验证前完整备份旧 `cf_clearance`，若用户取消、验证失败，或“验证页成功但 confirm 超时”，都会把旧 `cf_clearance` 恢复回 `CookieStore + WebView`，这部分语义已经明显更接近安卓/Flutter；5) `ProfilePage.ets` 的退出登录顺序改为“先推进会话失效并注销，再清 CookieStore/WebCookie”，避免旧请求在清理过程中把过期 cookie 写回来；6) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 `ToastService / ExportUtils / TopicDetailPage` 的既有 warn）。评估结论：当前主干业务请求本身已经主要运行在 `@kit.NetworkKit`，所以“把 NetworkKit 全部转为原生 http”并不是当前高优先级；下一步更有价值的是继续扩大会话代失效覆盖面，并把剩余零散请求统一收口到 `HttpClient`。next: 继续把 `AuthSessionService` 从“阻止旧 cookie 回写/误触发 CF”扩展到更多页面级数据请求失效；随后再决定是否把 CF 验证 UI 从当前路由页升级成更接近 Flutter 的全局 overlay / 悬浮胶囊模式`

## 2026.03.18

- **22:25** `fix(user-profile+webview): 按你刚点名的“5f8867 是鸿蒙现状、40c0e 是安卓目标”继续收口他人主页 About 弹层：1) 新增 `WebViewPage.ets`，补齐 `RouterManager.pushWebView(...) -> Index routeMap -> ArkWeb Web` 的应用内网页链路，支持站内打开、刷新和外部浏览器兜底；2) `UserProfilePage.ets` 的“关于用户”Sheet 改成更接近安卓的半屏 bottom sheet，使用 `SheetSize.MEDIUM`、自绘顶部拖拽条、标题改为“关于”、去掉底部大“关闭”按钮；3) about 内容结构改按 Flutter `_showUserInfo()` 收紧为“封禁/禁言状态 + 个人简介 + 更多信息”，去掉不该继续占位的 用户名/昵称/最后活跃，只保留位置/网站/加入时间；4) “网站”行现在会先关闭 sheet，再进入应用内 `WebViewPage`，不再直接拉外部浏览器；5) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 ToastService/ExportUtils/TopicDetailPage 的既有 warn）。next: 你真机重点验四项：A about 弹层现在是否稳定只占半屏而不是接近全屏；B 标题/拖拽条/分割线/去关闭按钮后的整体层级是否更像安卓；C “网站”行点击是否先收起弹层再进入应用内 WebView；D 外链网页在 WebView 里加载、刷新、再用右上角外部浏览器打开是否都正常`
- **21:44** `fix(user-profile): 继续按你最新真机反馈收口他人主页安卓对齐：1) 查清“从总结点到任意可以，但从动态点到话题/回复/赞没反应”的根因是 `UserProfilePage.ets` 把动态/话题/回复/赞都塞进同一个 `ActionsTab(...)` 分支，ArkUI 在同分支内切 filter 时存在“看起来没切页”的复用风险；现改为显式 tab 内容分支，并在切 tab 时统一回顶，确保 `动态/话题/回复/赞/回应` 之间来回切换都有立即反馈；2) Summary 按安卓重排：热门链接改双行卡片，最多回复至/被谁赞的最多/赞最多的人物 chip 补齐头像后的数字胶囊，热门类别改成“话题数 + 回复数”统计卡，热门徽章按金/银/铜做着色；3) 动态/话题/回复/赞/回应列表卡片统一收紧为更接近安卓的浅底大圆角样式；4) 按你刚纠正的真实站点口径，游客态 `回应` 不是“普通空态”，而是必须登录后才能看，因此当插件返回 `not_logged_in` 时现在明确显示“登录后可查看”提示。build: `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩 ToastService/ExportUtils/TopicDetailPage 的既有 warn）。next: 你真机重点验三项：A Summary 里的数字胶囊/类别统计/徽章颜色是否接近安卓；B 从 `动态/话题/回复/赞/回应` 之间来回点是否每次都立即切换；C 游客进入 `回应` Tab 是否稳定显示登录门槛，而不是通用报错或假空态`
- **21:13** `fix(user-profile): 继续收口游客态他人主页安卓对齐：1) `UserProfilePage.ets` 纠正了登录态合并带回来的游客回退，`回应` Tab 不再被“登录后可查看”硬拦截，现与 Flutter 一致走匿名只读口径；若站点返回 `not_logged_in` 则展示空态而不是登录门槛；2) 头图区补齐 Flutter 现有展示链路：支持背景图叠加、头像挂件 `AvatarWithFlair`、名字旁 `UserStatusEmoji`、头像点击进入图片预览、简介卡按“有无更多信息”决定是否展示箭头/可点击；3) `User.ets` 补齐 `suspend_reason / suspended_till / silence_reason / silenced_till` 解析，`UserInfoSheet` 新增封禁/禁言提示卡与 bio 富文本展示，游客态“关于用户”终于接近安卓/Flutter 真值；4) 同步按 Flutter 口径收紧通知菜单显示条件（仅在可 mute/ignore 时展示对应项）；5) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 你真机重点看他人主页游客态三块：回应 Tab 现在是否能正常进入并加载/空态、头图区头像挂件/状态 emoji/背景图是否贴近安卓、关于用户 Sheet 的 bio/封禁信息是否与真实站点一致；若口径对，我下一步继续抠卡片列表样式与 Summary/Action 行细节`
- **19:42** `feat(user-profile): 对齐安卓他人主页登录态交互并补齐真实后端：1) `ApiConstants/ApiClient/User/Draft/AuthService` 新增用户互动链路，补齐 `/follow/{username}`、`/u/{username}/follow/following|followers`、`/u/{username}/notification_level.json`、`/posts.json (archetype=private_message)`、`new_private_message` 草稿等接口与模型；2) 新增 `FollowListPage.ets` 与 `PrivateMessageComposerSheet.ets`，支持查看关注/粉丝列表、发送私信并保存/恢复私信草稿；3) `UserProfilePage.ets` 现已补齐登录态识别、关注/取消关注按钮、关注/粉丝点击跳转、顶部私信入口、用户信息 Sheet、以及更多菜单中的“常规/免打扰/忽略(1天/1周/1个月/永久)”通知设置与浏览器打开入口；4) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 你真机优先回归他人主页登录态四条链路：关注/取消关注是否即时生效、关注/粉丝列表是否能打开、私信是否发送成功、通知设置是否真正同步到站点；若口径都对，我下一步继续收口菜单层级与细节样式，进一步贴近安卓`
- **19:13** `fix(topic-preview): 修复你刚指出的“背后灰色的框没铺满全屏”：根因不是系统弹层，也不是页面背景露白，而是 `TopicPreviewOverlay()` 把整个全屏遮罩宿主都做了 `.padding({ left: 16, right: 16, top: 24, bottom: 24 })`，导致灰色 backdrop 和白色预览卡片一起被缩进，看起来像背后又套了一层没铺满的灰框。现已在 `TopicListPage.ets` 与 `CategoryTopicPage.ets` 同步移除这层宿主 padding，让遮罩真正铺满全屏；预览卡片自身仍由 `TopicPreviewDialog` 的 `width('92%') + height('72%')` 控制尺寸，不会贴边。next: 你真机重新长按看这层灰色遮罩应当已经铺满；如果你下一步还觉得“卡片太矮/太高”或“遮罩太深/太浅”，我就继续按安卓截图细调卡片尺寸和蒙层透明度`
- **18:34** `fix(topic-preview): 修复你刚反馈的“预览里头像都不显示”：根因不是 `SmartAvatar` 组件，也不是 `TopicPoster.user` 丢了，而是 `TopicPreviewDialog.ets` 取作者/参与者头像时直接用了 `TopicUser.getAvatarUrl(56)`，拿到的是未补全域名的相对模板路径；而列表页实际可用口径一直是 `UrlUtils.getAvatarUrl(avatarTemplate, size)`。现已把预览里的作者头像和参与者头像统一改走 `UrlUtils.getAvatarUrl(...)`，与首页话题卡片保持同一条真值链路，预览中的头像应当恢复显示。next: 你真机重新长按同一帖确认作者头像和参与者头像是否都出来；如果还有个别头像空白，我下一步就继续查那类用户是不是 `avatar_template` 本身为空或被站点返回成特殊 CDN 路径`
- **17:59** `fix(topic-preview): 收口你刚指出的长按预览弹层三处问题并继续向安卓靠拢：1) `TopicPreviewDialog.ets` 不再把首帖 `cooked` 剥成纯文本，而是直接解析首帖 `Post` 并交给 `PostRichContent` 渲染，因此 onebox 链接卡片、正文图片、站内跳转与正文真实排版终于能在预览里显示；2) 预览主体改成“上方可滚动内容区 + 底部固定操作栏”，长帖不再被 `Text(maxLines: 8)` 截死，用户现在可以继续往下滑看到链接和后续内容；3) 标题字号从 26 收紧到 22，并沿用正文 dense 版排版，整体观感比原来更接近安卓，不再出现一打开就显得过大过挤；4) 预览内点击站内 `/t/...`、`/u/...` 链接会先关闭弹层再走原生路由，图片点击也会先关弹层再进入应用内预览，避免“盖着弹层继续跳页面”留下状态残留。next: 你真机重点回看这张同帖预览：灰蒙层观感是否可接受、正文是否已经能顺畅下滑到底、onebox 链接卡片是否出来；如果还剩细节偏差，我下一轮再继续按安卓截图微调高度/边距/遮罩强度`
- **17:37** `feat(topic-list): 补齐首页/分类页长按话题卡片预览能力，对齐 FluxDO 的 TopicPreviewDialog 主交互：1) 新增 `TopicPreviewDialog.ets`，采用居中卡片 + 遮罩的预览形态，显示标题、作者、分类/标签、参与者、回复/点赞/浏览/最后回复，并提供“关闭 / 分享 / 查看详情”底部操作；2) 预览内容会优先异步拉取 `getTopicDetail(topicId)` 中的首帖 `cooked`，转成紧凑纯文本摘要展示，失败时自动回退到 `topic.excerpt`；3) `TopicCard` / `CompactTopicCard` 改成 `TapGesture + LongPressGesture` 独占手势，避免长按时误触点击详情；4) `TopicListPage` / `CategoryTopicPage` 已接入预览弹层宿主，主页和分类页长按卡片都能直接弹出预览并继续分享或进入详情。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 你真机重点验三点：长按卡片是否只弹预览不误进详情；预览里的首帖摘要是否接近安卓；若确认交互成立，下一步我再决定要不要继续把 FluxDO 的“长按预览开关”迁到设置页`
- **15:57** `feat(topic-detail+text): 收口详情页“正文文本处理”最后一段语义链路：1) 重写 `HtmlToMarkdown.ets`，补齐 heading / blockquote / aside.quote / ol-ul / table / details / spoiler / hr / math / mention / 普通链接 / emoji-status 图 / code fence 等结构化 HTML→Markdown 转换，不再只会把大部分块级结构拍平成纯文本；2) `PostRichContent` 为 heading、引用卡、列表项、spoiler、table cell、details 内部选区补上外层 `sourceHtml` 语义包装，确保长按“引用/复制引用”时能知道自己原本处在什么 block 里；3) `TopicDetailPage` 新增 `applySelectionHtmlContext(...)`，会在 `HtmlTextMapper.extractHtml(...)` 返回 fragment 之后把 heading/list/blockquote/details/spoiler/table 的上下文重新包回去，再走 markdown 转换；4) 整楼“引用回复/复制引用”也从 `stripHtml` 升级到优先走 `HtmlToMarkdown.convert(post.cooked)`，让整帖引用同样保留标题、列表、引用块、分割线等正文语义；5) `hvigorw assembleHap --no-daemon` BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 正文“文本处理”现在可以先视为收口完成，下一步切去主页遗漏功能和他人主页；详情页剩余项主要变成非文本类：代码块语法高亮、登录态写交互真机回归、embed/math/poll 的运行时验证`
- **15:04** `fix(topic-detail+code-block): 收口你刚反馈的“4 行代码却被拉满整屏”回归问题：根因不是行数计算，而是短代码分支的正文容器没有显式锁定内容高度，ArkUI 在 `Row + Scroll + height('100%')` 这组布局里把代码区向可用空间错误扩张，导致下面出现大片空白；现已把通用代码块正文区与 mermaid 代码视图都显式钉到 `getCodeContentHeight(...)`，短代码只占真实代码高度，不再把剩余屏幕一并吃进去。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 你真机重新看这类 1~6 行短代码块，应该已经恢复成紧凑高度；如果还有个别帖子异常，我下一步就直接对那帖 HTML 做定点比对，继续追是不是代码块外层还混进了额外空标签`
- **14:54** `fix(topic-detail+code-block): 对齐安卓/Flutter 的代码块卡片形态并修正手势冲突：1) PostRichContent 的通用代码块不再使用深色大黑框，而是改成浅色卡片 + 轻工具栏 + 细分隔线，整体视觉口径对齐安卓 `code_block_builder.dart`；2) 新增 `normalizeCodeText/getCodeLineCount/getCodeContentHeight`，会去掉末尾多余换行，并把单行/短代码块的高度改为内容自适应，不再硬吃 `min-height: 120` 导致一条短链接也占很高；3) 代码块只在内容足够长时才启用内部纵向滚动，普通短代码仅保留横向滚动，因此手指落在代码块上时页面纵向拖动不再被无意义地锁住；4) 行号列改为浅灰底独立栏，代码文本改为浅底深字，复制动作统一复制规范化后的正文代码；5) mermaid 的“代码视图”也同步去掉固定大高度与默认纵向滚动，避免同类块再次出现相同问题。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 你真机重点验三类帖子：单行短代码/短链接是否已经紧凑、长代码块触摸时页面是否还能自然上下拖动、mermaid 切到“代码”视图后是否不再莫名撑高`
- **14:22** `fix(topic-detail+quote): 继续收口你新给的三处安卓差异：1) PostRichContent 为 `aside.quote` 新增结构化引用头信息（`quoteUsername/quoteAvatarUrl/quoteTitleHtml/quoteContentHtml`），引用卡现在会像安卓一样显示小头像 + 用户名，避免继续把头像直接吞掉；2) TopicDetailPage 的回复胶囊补齐“从已加载目标楼层回填头像/用户名”的兜底逻辑，即使 `replyToUser` 缺失也尽量显示头像，不再只退回 `#楼层号`；3) 详情页大标题区移除 2 行硬截断，改为完整展示；滚动后的 TopBar 标题布局改成 `Stack + 居中标题 + 操作区覆盖`，减少无谓占位导致的标题裁切；4) 正文基础行高从 14/20、13/19 调整到 14/21、13/20，整体排版比之前松一点，更接近安卓；5) 重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 请你真机重点验三项：引用卡头像是否出现、回复胶囊头像是否补齐、详情页标题是否不再被过早截断；如果正文仍觉得“挤”，我下一轮继续追段落级 `<p>` 间距而不只调行高`
- **14:01** `fix(post-rich-content): 继续补齐游客态正文可见 parity：1) PostRichContent 新增 `<hr>` 分割线块渲染，正文中的横线现在会像安卓/Flutter 一样显示为淡灰色 divider，不再被吞掉；2) 对齐 Flutter 的 `_injectClickCounts` 链路，基于 `post.linkCounts` 在正文链接后注入 `span.click-count`，并在 ArkTS 行内渲染为灰底圆角点击数胶囊（例如 `2.0k`），对应你截图里链接右侧那个灰色数字；3) 这套点击数注入走统一 `inject -> parseBlocks -> tokenizeInline -> render` 主链，因此在引用/折叠内容等递归正文里也能复用；4) 重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 继续按游客态优先补细节，重点回看真实帖子里的 `spoiler/table/footnote/math/iframe-lazy-video` 显示与复杂嵌套，再收口剩余只读正文差异`
- **13:31** `fix(topic-detail+auth): 收口详情页正文 parity 与登录态写操作根因：1) `PostRichContent` 已补齐 `spoiler / table / poll / footnote / mention 状态渲染 / math / mermaid / chat transcript / iframe / lazy-video`，并把划词引用升级为 `HtmlTextMapper + HtmlToMarkdown` 的“HTML 片段反查 -> markdown 真值”链路；2) 继续查清“为什么鸿蒙端投票/回复/草稿看起来接了接口却仍不稳”，根因不是正文块本身，而是 `HttpClient` 之前没有真正把 `AuthService` 里的 CSRF token 注入写请求，也没有像 Flutter 一样在写接口前补 `/session/csrf.json`；现已补齐 `AuthService.updateCsrfToken`、`HttpClient` 的 cookies/csrf 同步、写请求前自动拉取 CSRF，以及命中 `["BAD CSRF"]` 后自动刷新 token 并重试一次，因此 `/polls/vote`、`/posts.json`、`/drafts` 这类登录态写接口终于走到与安卓同口径的鉴权链路；3) 重新执行 `hvigorw assembleHap --no-daemon`，BUILD SUCCESSFUL（仍仅剩既有 warn）。next: 真机优先回归 `poll` 的提交/撤销/查看结果切换、回复发送、草稿恢复与覆盖，再继续补长帖 chunking/perf 与 `math/iframe/lazy-video` 的真实站点兜底验证`
- **12:05** `feat(topic-detail): 打通详情页回复/引用闭环并补齐正文划词引用基础能力：1) Post/ApiClient/HttpClient/ApiConstants 新增回复、草稿、投票所需字段与接口（`mentionedUsers/polls/pollsVotes`、`/posts.json`、`/drafts`、`/polls/vote` 等），并新增 `Draft.ets`；2) 新增 `ReplyComposerSheet`，详情页 FAB 不再是 TODO，已可直接回复话题/回复指定楼层，草稿会按 `Draft.replyKey(topicId, replyToPostNumber)` 自动恢复与保存；3) 楼层三点菜单补齐“回复此楼 / 引用回复 / 复制引用”，图片长按菜单补齐登录态“引用到回复框”，游客态保留“复制引用”；4) `PostRichContent` 已接入长按划词菜单，支持“引用 / 复制引用”，并把该能力透传到引用块、列表项、details 内的递归正文；5) 回复成功后按现有 `streamIds + mergePostsByStream` 真值合并并自动跳到新楼层。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（仍含既有 warn）。next: 继续补正文 parity 大块能力，优先 `spoiler / table / poll / footnote / math / mention`，并把划词引用从当前纯文本 fallback 升级为 HTML 片段反查 markdown 真值`
- **10:45** `fix(topic-detail): 按你最新对比图继续对齐安卓详情页：1) 被采纳答案楼层补齐右上角斜着的“已解决”水印（对齐 FluxDO 的 accepted-answer stamp，而不是只保留首贴绿色 solved 横幅）；2) 查清“为什么有的首楼有回复有的没有”：这块显示的是“针对该楼层的 reply history”，安卓/FluxDO 只看 `post.replyCount > 0`，并不排除首贴；鸿蒙端之前额外写了 `post.postNumber > 1`，导致首楼即使有定向回复也被硬隐藏，现已移除该条件；3) 顺手补齐实时新回复在底部自动追加时对被回复楼层 `replyCount` 的本地递增，避免首楼/其它楼层回复数停在旧值；4) 详情页底部工具栏改为与首页一致的悬浮玻璃胶囊样式，并同步调整列表底部避让与进度胶囊位置。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含既有 warn）。next: 你真机重点验收三点：最佳答案那一楼右上角是否出现斜水印；首楼若有定向回复时左下角回复折叠按钮是否出现且可展开；详情页底栏是否已经变成和首页一样的悬浮胶囊且不遮挡末尾内容`
- **10:11** `fix(topic-detail): 修复跳楼后列表断层/楼层跑位：此前点击“最佳答案 #33”等未加载楼层时，鸿蒙端会先 getTopicPosts([postId]) 把单个目标帖合并进当前列表，于是出现“20 后面直接接 33、21-32 消失”的稀疏列表；现对齐 FluxDO/安卓：目标楼层不在当前已加载列表时，直接 reloadWithTargetPostNumber 重载目标楼层附近的连续帖子；若手头只有 postId，则先解析真实 postNumber 再重载。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含既有 warn）。next: 你真机再点“查看最佳答案 #33”，应直接定位到 33 楼附近的连续列表，不会再把 33 楼孤零零插在 20 楼后面`
- **10:05** `fix(topic-detail): 继续修复“此话题已解决”横幅仍未显示：定位到鸿蒙端此前只用 has_accepted_answer 决定 hasAcceptedAnswer，而部分真实详情数据可能只有 topic 级 accepted_answer 对象（安卓/FluxDO 正是用它直接认定已解决）；现新增 resolveHasAcceptedAnswer，对齐安卓口径：accepted_answer 对象 / 已解析出 acceptedAnswerPostNumber / 任一已加载 post.accepted_answer=true 均视为已解决。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含既有 warn）。next: 你真机回到这帖再看，首贴底部绿色横幅应当出现；若仍没有，我下一步直接加一条临时 hilog 把该帖真实 JSON 的 accepted_answer/has_accepted_answer 打出来，一次性钉死返回结构`
- **09:58** `fix(post-rich-content): 继续修正文内图片仍然过小：定位到 PostRichContent 的 contentWidthVp 可能被 onAreaChange 宽度再次 px2vp，导致正文可用宽度被误缩小，横向截图会被压成一小块；现改为“先算 px2vp，再对明显过小结果做启发式兜底回 raw 宽度”，并把单图容器从居中改为左对齐，继续贴近安卓。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含既有 warn）。next: 你真机重点回看这类横向截图/附件预览图，宽度应明显放大并更靠左；若还不够，我下一步就直接对该类 lightbox 图按容器宽度强制铺满，不再依赖测量值`
- **09:51** `fix(topic-detail): 对齐安卓“此话题已解决”卡片：1) TopicDetailPage 新增 acceptedAnswerPostNumber，优先解析 topic JSON 的 accepted_answer.post_number，缺失时回退扫描 post.accepted_answer；2) 首贴正文底部补齐绿色“此话题已解决 / 查看最佳答案 #n”横幅，点击复用现有跳楼逻辑直达最佳答案楼层；3) Mock topic detail 同步补齐 accepted_answer 真值，离线也能验收该横幅。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含既有 warn）。next: 你真机对照安卓确认：首贴底部是否出现这张绿色卡片；点按后是否准确跳到最佳答案楼层；若样式还想继续贴近安卓（圆角/留白/绿色深浅），我再按截图继续微调`
- **09:37** `fix(topic-detail): 修正正文单图仍然过小的根因：PostRichContent 不再把服务端返回的图片 width/height 当作最终渲染尺寸，而是改成“图片宽度跟随正文容器，width/height 仅用于计算宽高比”；因此截图类、缩略图类正文图片会像 FluxDO/安卓一样按容器宽度展开，不再按缩略尺寸小块显示。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含既有 warn）。next: 你真机再看这类横向截图/卡片图，宽度应明显接近安卓；如果还不够，我下一步就继续追 `lightbox-wrapper` 里 thumb/original 的选择口径`
- **09:12** `fix(topic-detail): 继续放大正文单图以贴近 FluxDO/安卓：1) PostRichContent 去掉正文单图实际上的强限高收窄路径，竖图不再因为高度上限再次被按比例缩窄；2) 对缺失尺寸信息的单图把默认展示高度继续抬高，弱元数据图片首屏不再显得“缩略图化”。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含既有 warn）。next: 你真机重点看两类图：超高竖图是否终于够大；无 width/height 的单图是否也不再发小`
- **01:02** `fix(topic-detail): 对齐 FluxDO/安卓正文引用与图片：1) PostRichContent 识别 aside.quote 并整体渲染为灰色引用卡片，引用标题不再被拆到灰框外；2) 引用卡片内部改为递归富文本渲染，灰框内的链接/图片/列表/代码块都可正常点击预览；3) 正文单图展示策略改为优先满宽+更高自适应上限，竖图不再被压到很窄；d-image-grid 仅 1 张图时回退为单图块避免半宽小图。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 warn）。next: 你真机回归：引用是否与安卓一致落在灰框内且链接可点；灰框内若含图片能否预览；单图（尤其竖图/小图）尺寸是否不再偏小`
- **00:17** `fix(topic-detail): 继续按最新反馈收口详情页体验：1) PostRichContent 再次上调单图最小展示宽度，小图会放大到更接近安卓的视觉尺寸；2) 长按抑制点击逻辑从一次性布尔标记改为短时间窗口，避免长按后误吞后续正常点击，同时修复“长按也会打开图片”的冲突；3) TopicDetailPage 新增 `refreshTopicDetail()`，并按主页相同模式用 `Refresh` 包裹帖子列表，详情页现在也支持下拉刷新。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL。next: 你真机确认单图大小是否已贴近安卓；详情页是否已能下拉刷新；长按图片是否只弹菜单不再打开预览`
- **00:10** `fix(topic-detail): 按最新反馈继续收口正文图片体验：1) PostRichContent 为单图新增最小展示宽度策略，小图不再过小，会按正文可用宽度的一定比例放大到更接近安卓的视觉尺寸，同时仍保持左对齐；2) 新增长按抑制点击逻辑，长按图片触发菜单后会屏蔽紧随其后的图片预览点击，修复“长按同时也打开图片”的冲突；3) 图片宫格同样复用该抑制逻辑。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL。next: 你真机确认单图尺寸是否已接近安卓；长按图片时是否只弹菜单、不再同时打开预览`
- **00:06** `fix(topic-detail): 按最新截图继续收口正文小图尺寸：PostRichContent 单图尺寸逻辑改为“优先尊重图片自身宽高，只在超出正文可用宽度或最大高度时按比例缩放”，移除此前会把小图/横条图硬撑到较大高度的最小高度策略；因此小缩略图、贴图、长条图会保持更接近安卓的原始占位，同时继续保持左对齐。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL。next: 你真机确认小图不再被拉高；若仍偏大，我再继续收窄图片默认 fallback 尺寸`

## 2026.03.17

- **23:49** `fix(topic-detail): 继续按安卓截图收口正文布局：1) PostRichContent 的 blockquote 改为稳定左侧边线写法，并对引用文本做多行清洗，修复“优惠持续更新...”这类引用在鸿蒙端被异常拉高、留下大空白的问题；2) 正文单图改为按实际显示宽高计算后左对齐渲染，不再默认整宽居中，贴图/表情图观感更接近安卓；3) 正文基础字号/行高与列表项同步收紧（常规 14/20、dense 13/19），emoji 行内尺寸随正文缩放，整体版式更紧凑。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL。next: 你真机验收：引用块不再出现大面积留白；单张图片/贴图改为左对齐；正文文本密度更接近安卓`
- **21:30** `fix(ui): 全面对齐安卓图片交互与正文细节：1) ImagePreviewPage 重构为浮动圆形按钮（返回+保存），移除拥挤的 TopBar，长按弹出紧凑 Sheet 仅保留 复制图片/复制链接/分享图片 三项（对齐安卓 ImageContextMenu）；2) PostRichContent 图片块去除灰色 backgroundColor、增大最大高度至 600vp、添加长按手势触发 onLongPressImage 事件；3) TopicDetailPage 新增图片长按上下文菜单 Sheet：查看大图/复制图片/复制链接/分享图片/复制引用（对齐安卓 image_context_menu.dart 五项）；4) 正文链接去除下划线（对齐安卓 text-decoration:none）；5) 引用块增加 4px 左侧边框（对齐安卓 blockquote）；6) 行内代码增加浅色背景+区分色（#E06C75）；7) Onebox 圆角改为 8（对齐安卓）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL。next: 你真机验收：图片预览页按钮不再贴屏幕边缘；帖子内长按图片出五项菜单；正文图片不再有灰色边框且更大；链接无下划线；引用块有左侧边框`
- **20:46** `fix(image-preview+post-image): 对齐安卓图片操作菜单与楼层图片展示：1) ImagePreviewPage 将“更多(三点)”与“长按”动作单拆分——更多仅保留 复制图片/复制链接/分享图片 三项，长按保留完整菜单，避免弹层过高遮挡图片信息；并微调顶栏右侧内边距避免视觉贴边；2) PostRichContent 在缺失 <img width/height> 时从图片 URL 中猜测尺寸（例如 1506x1582 / _690x409），用于计算自适应高度，改善竖图被默认高度压小与 letterbox 观感。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（仍有少量 deprecated warn）。next: 你真机回归：帖子内长按图片动作单是否与安卓一致（查看大图/复制图片/复制链接/分享图片/复制引用）；竖图/无尺寸图是否不再明显偏小且灰边减轻；图片预览页三点菜单是否不再贴边且不挡信息`
- **20:20** `fix(topic-detail): 深度对齐 FluxDO：1) PostRichContent 列表项改为递归渲染，<li> 内图片/onebox/details/code 均可完整显示（修复 topic/1770103 列表内图片变蓝字）；2) lightbox-wrapper/d-image-grid 解析同时拿 thumb+original，正文用 optimized/thumb、预览/保存/分享用 original；3) 正文块间距收紧（blockGap 12→10 + 空块过滤/换行压缩）；4) 图片预览页补齐保存/分享/复制链接/浏览器打开/长按动作单，并走二进制下载保存原图清晰度；5) 相关链接 PostLinks：相关链接(数字)+折叠展开+还有X条；6) SmartAvatar 增加 GIF 动态头像播放（animated_avatar 真实动起来）；7) 分享图主题切换修复：shareImageCardId 变化必触发 snapshot 调度（延迟 240ms），Sheet 内切主题预览/保存/分享一致。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机按 topic/1770103 验收：正文空隙是否接近安卓、列表内图片不再蓝字；点开图片清晰可缩放且长按菜单可用；BeautifulSoup 等 GIF 头像是否动；“相关链接”是否出现并可展开；分享图在 Sheet 内切 6 主题是否立即变化且保存/分享输出一致`
- **14:31** `fix(topic-list): 修复首页/分类页话题卡片“回复数不更新/落后安卓”：TopicTrackingService 额外记录 MessageBus(/latest,/new) payload 的 highest_post_number 并提供 getHighestPostNumber；TopicListPage/CategoryTopicPage 在 tracking 回调里用 tracked highest 修正 Topic.postsCount/replyCount/highestPostNumber 并触发列表重绘；TopicCard/CompactTopicCard 回复数显示改为基于有效 replyCount（不再只看 postsCount-1）。revert(topic-detail): 按你反馈撤回首贴左下角“回复”入口（楼主层不显示）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机验收：主页/分类页同一话题回复数是否会随新回复即时增长并与安卓一致（无需退出再进）；TopicDetail 首贴底部不再显示回复入口；若仍有个别话题不增长，把该话题 id + Hilog 中 TopicTrackingService 收到的 payload（含 highest_post_number）发我继续追`
- **13:53** `fix(topic-detail): 对齐安卓折叠回复列表：回复标题“X 条回复”与回复卡片内容改为左对齐（不再整体居中）；TopicDetail 楼层头像改为优先使用 Post.animated_avatar（支持动态头像），并补齐 animated_avatar 可能包含的 {size} 占位符替换；同时分享图作者头像也同步走 animated_avatar。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机验收：展开回复后标题/正文是否左对齐；BeautifulSoup 等动态头像是否能动（首贴/回帖/折叠回复列表/分享图预览）`
- **13:30** `fix(topic-detail): 对齐 FluxDO 安卓正文富文本：PostRichContent 新增标题块(h1-h6)样式、details/summary 折叠块、d-image-grid 两列图片网格；代码块加“语言标签+复制”工具栏并显示行号/双向滚动；吞掉 lightbox-wrapper 仅保留图片并过滤上传元信息（文件名×尺寸 KB）。同时修复分享图切主题仍偶现不更新：离屏分享卡片改为 onAppear 后再触发 snapshot。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机验收：标题/副标题字号加粗与间距是否贴近安卓；两图网格是否一行两张且不再显示文件名尺寸KB；details 是否可展开/收起且代码块可复制；分享图在 Sheet 内切主题后预览/保存/分享是否立即变化（无需退出再进）`
- **11:30** `fix(topic-detail): 按你新截图继续对齐安卓：1) 正文图片块从 Cover 改为 Contain，避免图片被裁切（同帖同位显示不全问题）；2) 首贴底部操作行补齐左下角“回复”入口（显示话题回复数，点按跳到 2 楼）；3) 修复分享图片“生成后切主题仍不变”：离屏分享卡片改为 ForEach keyed 强制重建 + snapshot 延迟收敛为常量（180ms），切主题会重新 snapshot 并更新预览/保存/分享输出。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机复测：该帖图片是否完整不裁切；首贴左下角回复入口是否出现且可跳到 2 楼；分享图在 Sheet 内切 6 主题预览/保存/分享是否都随主题变化`
- **10:49** `fix(topic-detail): 对齐安卓 TopicDetail 正文细节：1) PostRichContent 新增 <ul>/<ol> 列表块渲染（圆点/序号 + 右侧文本换行自动缩进），消除 li/p 叠加导致的空行/错位；2) 解析 Discourse aside.onebox 为一个灰色圆角链接卡片（整卡可点打开），不再碎片化为大图+多段文字；3) user_status.emoji 图片优先，Unicode/加载失败时显示原始 emoji/短码兜底；4) “种子用户”标题保留渐变并增加 fontColor 兜底确保必定可见；5) 分享图片主题切换改为每次生成使用新节点 id 截图（延迟 140ms + 过期请求保护），预览/保存/分享输出随主题变化。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机验收：长列表缩进是否贴近安卓；onebox 是否只剩一个灰卡且整卡可点；多种 emoji（短码/Unicode/404）是否都能看到；“种子用户”是否必显；分享图 6 主题预览/保存/分享是否一致变化`
- **09:00** `fix(topic-detail): 继续按安卓/FluxDO 复刻话题详情页：1) 分享底栏改为三选项菜单（分享链接/生成分享图片/导出文章），补齐导出 Sheet（仅主帖/全部 + MD/HTML）并生成文件走 ShareKit 分享；2) 生成分享图片修复白屏（离屏渲染改为移出屏幕但保留渲染），新增 6 套主题与偏好持久化，支持保存到系统相册（新增 WRITE_IMAGEVIDEO 权限 + reason/usedScene + PhotoAccessHelper 落盘）；3) 跳楼定位口径改为 startIndex 并新增 2s 紫色高亮；4) 底部工具栏显隐方向修正；5) 补齐头像 flair 角标与名字旁 user_status emoji 图标；6) 清理正文图片附件尾巴文本（image...KB）；7) 图片预览页修复 TopBar 位置、彻底去除中间“1/1”叠层并支持双击/双指缩放与拖拽；8) 订阅 MessageBus /topic/{id} 实时追加新回复（在底部时自动拉取正文，否则仅更新 stream/count）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机逐条对照安卓验收（尤其：头像角标/状态图标、图片预览缩放手势与滑动冲突、topic channel 新回复在底部是否自动追加与滚动保持）`
- **00:58** `feat(ui): 对齐安卓/Flutter：“查看 X 个新的或更新的话题”点击插入后，短暂高亮本次插入/更新的话题（2.5s 自动消退，卡片背景带动画过渡）；并清理 TopicListPage incoming 插入与去重逻辑（抽出 insertIncomingTopics/mergeUniqueIds/上下文校验 helper，Mock/真实插入统一 pinned 前缀规则）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机确认：点击 banner 后插入的条目会高亮约 2.5s 然后自动淡出；若希望分类页/其它列表也支持同样高亮，我再把同样机制抽成可复用实现`
- **00:32** `fix(messagebus): 针对真机 NetStack 长轮询总是 httpVer=3（HTTP/3）且 dataReceive 疑似被整段缓冲的问题，游客态 MessageBus poll 主动缩短 readTimeout=5000ms（登录态仍 60000ms），用“超时→重连”机制强制更频繁冲刷缓冲区，避免 anon_polling_interval 生命周期内的批量跳变。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机看 MessageBus poll 的 NETSTACK total_time 是否从 ~25200ms 下降到 ~5000ms 左右，并观察 incoming 是否变为更频繁到达；若仍总是 25s，说明 readTimeout 未生效，我再改为主动定时 cancel/destroy 请求强制重连`
- **00:17** `fix(messagebus): 对齐安卓端“实时”体验：强制 MessageBusService.requestInStream 使用 HTTP/2（req.usingProtocol=http.HttpProtocol.HTTP2），避免部分设备默认走 HTTP/3（NetStack httpVer=3）时 dataReceive 回调被缓冲到 dataEnd 才触发，导致 incoming 计数呈现“等很久→批量跳变”。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机抓 NETSTACK 的 httpVer 是否从 3 变为 2，并观察 incoming 是否能更及时到达；若仍批量跳变，我再把 usingProtocol 改为 HTTP1_1 并补充 dataReceive 时间戳诊断`
- **00:03** `fix(tracking): 修复游客态“查看新话题”轮询不生效：TopicListPage.maybeArmIncomingBanner 之前在 refreshTrackingUI 后 incomingTopicCount>0 时直接 return，导致游客态虽然已实现 /latest.json 轮询，但在“已有 incoming（来自 MessageBus）/页面恢复”场景下不会启动轮询，表现仍像 25s 批量跳变；现在改为游客态无论当前 incomingTopicCount 是否>0 只要在“最新”列表就会挂起轮询（并继续与 MessageBus 去重合并）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机复测：游客态刚进/切回页面时是否每 ~5s 都会有 GET latest.json（hilog: HttpClient GET .../latest.json），banner 计数应更平滑；若仍 25s 一跳请把 MessageBusService 的 dataReceive/Chunk parse 日志 + HttpClient latest.json 的 GET 日志一起发我`

## 2026.03.16

- **今日** `fix(topic-detail): 对齐安卓端帖子渲染与楼层交互：1) TopicHeader 修复 tags 兼容（string/object）与回复数真值（replyCount=postsCount-1，postsCount 优先 post_stream.stream.length）；2) PostRichContent 原生渲染增强：块级线性分割，emoji/小图内联 ImageSpan，不再误判为正文大图占位；链接/加粗/斜体/删除线/行内代码/引用/代码块/正文图片可见效果更贴近安卓；3) 楼层头部 badgesGranted 支持 icon→sys.symbol 映射并按金银铜资源色渲染；右上角 reply_to 胶囊放宽为 replyToPostNumber>0（无 replyToUser 也兜底显示 ↩ #n），点击跳楼；4) 游客态严格隐藏点赞；5) 楼层三点菜单保留“分享链接/生成分享图片”，生成分享图采用离屏组件快照→cache PNG→预览 Sheet→ShareKit 分享；6) 新增应用内图片全屏预览页 ImagePreviewPage（Swiper）并接入路由。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机对照安卓截图验收：首帖含 emoji 不再出现大图占位；正文图片点击进入全屏 Swiper；reply_to/回复列表跳楼准确；分享图可生成预览并分享成功；badge icon/颜色与安卓一致`
- **23:46** `fix(tracking): 游客态“查看新话题”不再完全依赖 MessageBus（站点 anon_polling_interval≈25s 导致计数批量跳变）：TopicListPage 对游客态持续轮询 /latest.json（silent CF guard + 指数退避，默认 5s），并与 MessageBus incoming 做去重合并，避免 refreshTrackingUI 覆盖轮询结果；插入新话题后立即重新挂起轮询。ApiClient.getFilteredTopics 新增可选 guardOptions 透传到 HttpClient.get。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机复测：未登录时 banner 计数是否由“25s 一跳”变为约 5s 递增；若触发 CF 导致轮询退避（hilog 有 backoff=...ms），把 HttpClient/CfChallengePage 的关键日志发我再调间隔与策略`
- **23:13** `fix(messagebus): Playwright 抓包确认 linux.do 网页端轮询 https://ping.linux.do/message-bus/.../poll 时不带 Discourse-Background 请求头（仅带 X-SILENCE-LOGGER），因此鸿蒙端 MessageBusService 对齐网页端：移除 Discourse-Background=true，并将 Accept 改为 text/plain。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机复测：incoming 是否不再严格按 25s 一批回包（计数更“实时”、跳变更小）；若仍 25s 一跳，再把 NETSTACK 的 total_time 与 MessageBusService 的 baseUrl/url 发我确认是否为站点 anon_polling_interval 限制`
- **22:57** `fix(tracking): TopicTrackingService.notify 改为 setTimeout(0) 合并同批次回调，避免一轮 MessageBus 响应（多个 latest/new_topic）触发多次 UI 刷新导致“查看新话题”banner 计数抖动/卡顿。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机复测：incoming +1 连续出现时 banner 是否更平滑递增，点击插入是否不再卡；若仍觉得“不实时”，请对比 MessageBus poll 的 total_time 与发帖时间差（确认是否为站点 anon_polling_interval 行为）`
- **22:24** `fix(messagebus): Playwright 验证 linux.do 的 siteSettings.long_polling_base_url= https://ping.linux.do（/site.json 不包含该字段，且主域 /message-bus 返回 404 属正常）；因此 MessageBusService 在 baseUrl 未配置时改用 ping 子域作为兜底 long_polling_base_url，并仅首次输出 hilog（DEBUG_TRACKING）提示，避免真机“永远轮询主域→持续 404→backoff 30s”刷屏。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机看 Poll failed 的 base/url 是否已切到 https://ping.linux.do；若仍失败把新的 code/base/url/tail 发我（重点看是否变成 403/429/timeout）`
- **22:29** `fix(preloaded): 针对“未通过过 CF 验证→silent 取 /latest 永远 CF_CHALLENGE_SILENT→config-only 一直重试”的死循环，PreloadedDataService 在拿不到 long_polling_base_url 时对 linux.do 直接兜底写入 https://ping.linux.do（并持久化到 Preferences），从源头避免 MessageBus 长期走主域 404；同时移除无效的 /site.json fallback（该接口不含 long_polling_base_url）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机确认 PreloadedDataService 出现 Config-only updated baseChanged=Y baseUrl=https://ping.linux.do，并且 MessageBusService 后续不再打印 HTTP 404`
- **21:54** `fix(messagebus): 修复 MessageBus 预热阶段 long_polling_base_url 未正确解析导致长期走主域名轮询（/message-bus 404）：PreloadedDataService config-only 提取逻辑兼容 Discourse 内层 JSON 的 \\&quot; 与 \\/ 转义，并在 fast path 失败时回退 decode+JSON.parse 解析 siteSettings；同时 config-only 也更新 Turnstile sitekey 供 cf_clearance 自动续期；ApiClient 读取 /site.json 时兼容 client_settings/site_settings/siteSettings 三种结构提取 long_polling_base_url。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机确认 hilog 中 MessageBusService 已 Configured baseUrl=...（应非 https://linux.do），并观察 Poll failed: HTTP 404 是否消失；若仍 404，把 PreloadedDataService/MessageBusService/ApiClient 相关 hilog 一并发我（含 Config-only updated 日志）`
- **21:30** `fix(network+tracking): 修复静默预热命中 Cloudflare challenge 后“自动续期被 stop 掉”导致的 MessageBus 长期 404：HttpClient 在 silent=true 且检测到 challenge 时改为触发 cf_clearance 自动续期（startIfPossible）而非 stop；TopicTrackingService 的 preloaded config-only 预热失败重试改为 5s 起步指数退避（上限 30s），成功/stop 时重置计数，减少“等很久才出现、出现后 +1 不加”的体感。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机复测首页/分类页 banner 是否能持续实时 +1（观察 MessageBusService 不再长期 HTTP 404），以及 cold start 后是否更快进入可用状态`
- **16:00** `fix(tracking): 修复首页“查看 X 个新的或更新的话题”在鸿蒙端看似阻塞/突增：1) aboutToAppear 仅首进执行 bootstrap（不再每次回到首页都重新拉列表导致 banner 延迟出现并集中跳大）；2) 点击 banner 后立即重新挂载 maybeArmIncomingBanner（兜底轮询不再断档，避免“点完后很久才再出现”）；3) tracking listener 改为 setTimeout(0) 调度 UI 刷新，降低 MessageBus 回调直改状态导致的卡顿/不刷新风险；4) 新增 AppConstants.DEBUG_TRACKING 与关键路径 hilog（MessageBus 就绪、UI 刷新、轮询递增）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机验证：banner 在首页不操作也能持续累加；点击插入后短时间内仍能继续出现并累加；若仍“阻塞/突增”，打开 DEBUG_TRACKING 抓 TopicListPage/MessageBusService hilog 我继续定性`
- **17:09** `refactor(tracking): 重构首页“查看 X 个新的或更新的话题”为纯 MessageBus 实时模型（对齐安卓/FluxDO）：1) Index aboutToAppear 全局常驻启动 TopicTrackingService.start(false)，人在其它页也持续累加；2) TopicListPage 移除 incoming 轮询兜底与 setTimeout 防抖，banner 仅由 TopicTrackingService listener 驱动，incomingCount 始终从 service 同步（切回“最新”立即可见），点击时实时从 service 取 ids → getTopicsByIds → 插入到 pinned 前缀之后 → clearIncoming；3) PreloadedDataService 新增 config-only 提取 shared_session_key/long_polling_base_url + Preferences 缓存（message_bus_*_v1），启动先用缓存 configure MessageBus、后台刷新变化自动重启轮询；4) MessageBusService 429 重试加入 jitter，短结束且无 dataReceive 加 800–1500ms 随机延迟避免短轮询风暴；5) DEBUG_TRACKING 补齐 incoming+1/clearIncoming/poll 结束原因与耗时日志。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机按 Case A–E 验收：A 冷启动首页计数持续变化不再“长时间不动→突然跳大”；B 在其它页停留 1–3 分钟返回首页 banner 立刻反映最新计数；C 点击插入顶部成功且 banner 清零后仍可继续累加；D 下拉刷新后不反复显示旧计数；E 开 DEBUG_TRACKING 看是否仍有持续 429/短轮询风暴，并把 hilog（TopicTrackingService/MessageBusService/PreloadedDataService/TopicListPage）发我`
- **19:58** `fix(messagebus): 修复游客态首页“查看 X 个新的或更新的话题”计数卡住/跳变：1) MessageBusService.poll 增加 hasBusinessMessage（忽略 /__status），短轮询延迟改为“无业务消息”才触发，避免仅状态包导致请求风暴；2) processChunk 改为返回是否包含非 /__status 消息，并在 dataReceive/endStream 聚合；3) short poll delay 调整为 1500–2300ms，降低触发 429 后长等待（60~90s）概率。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（含少量 deprecated warn）。next: 你真机验证：观察 hilog 是否显著减少 Rate limited, wait=...；banner 计数是否更接近逐步递增（不再长时间不动→突然跳大）`
- **20:29** `fix(messagebus): 继续修复 MessageBus 持续 404 导致的“banner 计数长时间不动→突然跳变”：1) 规范化 long_polling_base_url（补全 scheme-relative //host，并移除尾随 /），避免 NetworkKit 对不完整 URL 解析异常导致打到错误路径；2) MessageBusService 统一使用 getEffectiveBaseUrl 生成轮询 URL；3) 增强诊断日志：Poll failed 时额外打印 code/base/url/content-type/server/tail preview，Chunk parse failed 打印 err.toString+响应片段，便于真机一次定位到底是 URL 错、返回 HTML 还是其它网关页。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL。next: 你真机抓 MessageBusService/PreloadedDataService/TopicTrackingService 的 Info+Warn hilog，重点看是否仍 HTTP 404 以及打印出来的 url/base/ct/server/tail`
- **20:56** `fix(incoming): 修复游客态“查看 X 个新的或更新的话题”计数更新不及时（长时间卡住→突然跳变）：1) HttpClient.get 支持按请求覆盖 connect/read timeout；2) ApiClient.getFilteredTopics 透传 timeout；3) TopicListPage.checkIncomingTopics 使用 5s timeout（避免默认 30s 慢请求阻塞导致 banner 停更）；4) MessageBusService 在非 2xx 时跳过 tail JSON.parse，404 不再反复刷 Chunk parse failed（仅保留 Poll failed 的诊断日志）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL。next: 你真机验证：在首页“最新”停留 3 分钟，制造 5~10 次新/顶起事件，banner 计数应更接近每 2s 内逐步递增；同时确认 hilog 中 404 仅出现 Poll failed（不再有 Chunk parse failed）`

## 2026.03.15

- **今日** `fix(filter): 修复排行榜/热门筛选与安卓端不一致：1) 热门改用 /hot.json（filterName='hot'，非 /top.json?period=weekly），对齐 Flutter _topics.dart getHotTopics；2) ApiClient.getFilteredTopics 将 order/ascending 守卫从 filter==='latest' 扩展为 filter!=='top'，确保 latest/new/unread/unseen/hot 均可传排序参数，排行榜(/top.json)不传；两处改动同步到 TopicListPage.ets 和 CategoryTopicPage.ets。build: 待验证。next: 你真机验收：排行榜/热门切换后列表结果应与安卓一致；组合"热门+点赞数排序"应正确请求 /hot.json?order=likes；排行榜不应带 order 参数`

## 2026.03.15

- **22:39** `feat(topic+profile): 对齐安卓端游客态两页：1) 话题详情页（TopicDetailPage）补齐完整阅读分页（post_stream.stream + /t/{id}/posts.json?post_ids[]=... chunk=20，含“加载更早/加载更多”）、热门回复(filter=summary)/只看题主(username_filters)/取消过滤、话题内搜索并从结果跳楼（/t/{id}/{postNumber}.json）、时间线跳楼 Sheet、系统分享/外部浏览器打开、更多菜单入口保留（游客态点击 toast“请先登录”）；2) 他人主页（UserProfilePage）补齐 TopBar 搜索（默认 @username）与更多菜单（关于/分享），6 个 Tab 全部可用：Summary 增加“热门链接”且条目可点跳楼；动态/话题/回复/赞 走 /user_actions.json；回应走 /discourse-reactions/posts/reactions.json；全 Tab 支持分页/下拉刷新/点击跳转话题并定位楼层；3) 新增 AppContext/Toast/Share/Clipboard/ExternalLink 等通用能力封装 + ApiClient 扩展 + UserAction/Reaction 模型。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL（ToastService 有 deprecated warn）。next: 你真机按验收用例验证话题详情页分页/过滤/话题内搜索/分享与他人主页 6 Tab 分页/跳楼；无 bug 我再推送到 gitee`
- **19:05** `fix(network+filter): 收敛鸿蒙端 Cloudflare 启动频繁弹验证：1) CfChallengeService.isCfChallenge 对齐 Flutter（仅 Just a moment/cf_chl_opt/challenge-platform，移除 turnstile 误判）；2) HttpClient 增加 CF silent 模式，PreloadedDataService/TopicTracking warmup 遇 challenge 不弹验证页；3) 验证成功后增加 cf_clearance 生效确认（延迟 1.5s + 3 次检查）再重试原请求一次；4) 隐藏续期 Web 初始改 about:blank，减少冷启动触发面；补齐 HttpClient/CfChallengePage/CfClearanceRefresh 可验收字段日志（cookie 只记是否存在/长度）。同时实现 selectedTags 持久化：TopicListPreferences 新增 topic_selected_tags（JSON，去重保序），首页/分类页初始化恢复并在删除/清空/确定应用时落盘。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 真机按 Case A–E 验收 CF（冷启动不必弹、silent warmup 不打断、验证后自动重试、前台 10~15 分钟不频繁弹）；以及按 Case B1–B5 验收标签持久化（首页/分类页一致恢复，坏 JSON 回退空数组）`
- **15:45** `fix(tracking): 彻底修复 banner 数字不累加，根本原因是 @Builder 传参按值拷贝，IncomingTopicBanner(count) 首次拿到 1 后永远不更新；改为 @Builder 内直接引用 this.incomingTopicCount 响应式字段。另删除 addIncomingFromValue 屎山（把 payload key 当 topicId 迭代），processChannelPayload 对齐 Flutter LatestChannelNotifier 只取 data['topic_id']。日志验证链路全通 1→10。build: 待验证。next: 真机验证 banner 数字实时累加与安卓一致后提交`
- **13:56** `fix(incoming-poll): 兜底轮询不再“出现一次就固定为 1/固定为某个数不再变化”：游客态当 MessageBus 未就绪（everSucceeded=false）时，checkIncomingTopics 仍会每 2s 继续对比 anchor 更新 count，并且只在 count 变大时更新（不回退），从而可逐步累加接近安卓“一个一个加”的体验；MessageBus 就绪后仍会自动停轮询避免频繁触发 CF。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机验证：在 banner 已出现且仍有新帖时，数字应从 1→2→3 继续增长（不会卡死在 1）；若仍不涨，再发一张同时间安卓截图 + Hilog 的 MessageBusService warn/error 我做最终定性`
- **13:44** `fix(tracking): 继续收口“查看 X 个新的或更新的话题”固定为 1：扩展 incoming topic id 提取逻辑，兼容 topic_ids 在顶层或 payload、数组元素为对象（含 id/topic_id）、以及 topic_ids 为 map（key=topicId）的多种形态；统一通过 addIncomingFromValue() 解析并去重累计。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机停留在首页“最新”，观察 banner 是否能从 1 持续累加到 2/3/…；若仍固定为 1，请同时截一张安卓同一时刻的 banner（看安卓是否 >1），并把 Hilog 中 MessageBusService 的 warn/error（Chunk parse failed/429/timeout）贴我做最终定性（是服务端只推同一帖，还是我们仍漏字段）`
- **13:31** `fix(tracking): 修复“查看 X 个新的或更新的话题”只显示 1 不累加：Discourse 的 /latest 推送可能在 payload 内用 topic_ids/topic_id 批量携带更新话题；此前仅取顶层 topic_id 导致 incomingTopics 只进 1 个。现 TopicTrackingService 在 message_type=latest/new_topic 时同时解析 payload.topic_ids + payload.topic_id + 顶层 topic_id，并在顶层缺失时回退用 payload.topic_id 作为 tracking 的 topicId。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机停留在首页“最新”，顶部 banner 的数字应能从 1→2→3 持续累加；若仍固定为 1，把 Hilog 里 TopicTrackingService/MessageBusService 的日志（尤其 Chunk parse failed / 429 / timeout）贴我继续追`
- **12:05** `fix(messagebus): 修复“查看 X 个新的或更新的话题”仍不实时：NetworkKit 需使用 requestInStream 才能让 dataReceive 真正按流式持续回调；此前用 request() 仍可能整包缓冲导致“很久不动→一次固定数→不再更新”。现改为 requestInStream + dataReceive 按 '|' 分块解析，并增加 currentAbort：configure/新增频道 destroy 当前请求时会先 resolve 挂起的流式等待，确保立即重启不挂死。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机验收：停留在首页“最新”，顶部 banner 的 X 应该像安卓一样逐步增加（不是几分钟才跳一次且固定数），出现后也会继续增长；若仍不增长，把 Hilog 里 MessageBusService 的 warn/error 贴我继续追（尤其 429/2300028 timeout/2300007 connect fail）`
- **11:29** `fix(messagebus+url+tags): 对齐安卓三处真机问题：1) MessageBus 长轮询改为 NetworkKit dataReceive 流式接收并按 '|' 分块解析，configure/新增频道会 destroy 当前请求快速重启（避免等 60s 才切配置/包含新频道），incoming 计数更“实时增长”；2) PreloadedDataService 解析 data-discourse-setup 的 data-cdn/data-s3-cdn/data-s3-base-url，UrlUtils.resolveUrl 支持 // 协议相对并做 S3→CDN 重写，修复“浏览分类/话题卡片/详情页分类 icon 无图片只剩色点”；3) 标签选择 Sheet 对齐安卓：列表仅展示未选标签，首页/分类页采用 draftSelectedTags，弹层内选择不再每点一次刷新话题列表，点“确定”后一次性应用。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机验收三点：A) 浏览分类网格“开发调优”等应显示真实图片；B) 首页顶部“查看 X 个新的或更新的话题”计数应持续增长、不再很久不动后突然 +一堆；C) 标签弹层连点多个 + 不应触发后台列表刷新/卡顿，点“确定”后只刷新一次且已选标签不会同时出现在列表里`
- **09:54** `fix(ui+network): 针对你真机反馈继续收口三处阻塞：1) 标签筛选 /tags/filter/search 将 limit 限制为 <=20（默认 8），并为该请求缩短超时，避免 HTTP 400/卡很久；2) 分类图标组件 CategoryIcon 回归“后端 uploaded_logo 真值”渲染（参数改为 iconSize 且 build 单根容器 Row），移除 FontAwesome 字体尝试，修复分类图标不显示；3) 首页“查看 X 个新的或更新的话题”更接近安卓实时：TopicTracking 游客态后台 warmup preloaded 配置 MessageBus（shared_session_key/long_polling_base_url），MessageBus 长轮询 timeout 视为正常不退避，incoming 轮询避免早退导致停止。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机验收：标签筛选 Sheet 是否不再 HTTP 400 且能快速拉到真实 tags+count；浏览分类/QuickChip/子分类菜单等分类图标是否恢复显示并与安卓一致；首页“查看 X 个新的或更新的话题”是否能更快出现且计数更实时（逐步增长），下拉刷新是否会清掉旧计数并重新开始轮询/订阅`
- **08:42** `fix(ui+network): 按你真机反馈继续收口首页/分类/标签对齐：1) 分类图标不再走 sys.symbol 近似映射，统一渲染后端 category.uploadedLogo（含父级兜底），覆盖首页话题卡片/详情页/浏览分类弹层；2) 标签筛选 Sheet 真数据加载增强：TagSearchResult/ApiClient 解析更健壮，HttpClient 的 CF guard 兼容 503/200 challenge，Sheet 错误提示可区分 Cloudflare；3) 首页“查看 X 个新的或更新的话题”更接近安卓实时：TopicTracking 提前启动，游客态 tracking 启动后自动停轮询兜底并收紧兜底间隔。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机验收：标签筛选 Sheet 是否能稳定拉到真实 tags + count（遇到 CF 是否会自动弹验证并成功重试）；首页“查看 X 个新的或更新的话题”是否更快出现/更实时，且不会频繁轮询触发 CF；浏览分类/话题卡片/详情页分类图标是否已按安卓一致优先显示图片 logo（无 logo 时 lock/色点兜底）`
- **04:04** `fix(ui+network): 继续对齐 FluxDO 安卓端首页/分类页：1) “最新”筛选登录态新增 新话题/未读完/未浏览，并在按钮与菜单项显示括号计数（MessageBus + topic-tracking）；2) “默认”排序支持再次点击同一项切换升/降序，并将 ascending 透传后端；3) 话题 tags 解析兼容 string/object 并过滤空 tag，修复标签不显示/灰条占位；4) 标签选择 Sheet 请求参数对齐 selected_tags 且失败改为错误+重试（非 Mock 不再静默假数据）；5) 首页“查看 X 个新的或更新的话题”在顶部也可刷新显示，登录态由 tracking 驱动并在插入后清理 incoming；6) 收紧下拉菜单宽度/高度；7) 补齐分类 icon 映射（award/book-open-reader/clone/hard-drive/hurricane/square-share-nodes/users/bullhorn...）。build: hvigorw assembleHap --no-daemon BUILD SUCCESSFUL. next: 你真机验收：首页/分类页话题卡片 tags 是否稳定显示；标签筛选 Sheet 是否能拉到真实 tags + count（失败时是否出现错误页+重试）；登录态“最新”下拉是否出现 新话题(x)/未读完(x)/未浏览 且计数会变；“默认”下拉再次点同一项是否切换 ↑/↓ 且列表顺序反转；下拉菜单是否明显变窄；首页顶部不离开是否也会出现“查看 X 个新的或更新的话题”且点击后插入到 pinned 后并回到顶部`
- **01:20** `fix(ui+network): 对齐安卓首页/分类/标签的缺失项：1) TopicListResponse 解析 posters + 顶层 users，话题列表头像可展示真实 avatar；2) 首页/分类页“最新/排行榜/热门 + 默认/活跃度/创建时间/点赞/浏览/回复/参与者”改为真实走 getFilteredTopics 后端参数；3) 标签选择 Sheet 接入 /tags/filter/search（支持搜索、话题数、selected_tags[]、上限与清空）；4) 话题卡片/分类网格/标签 pills 增加分类/标签 SymbolGlyph 图标映射（未知退化为 logo/lock/dot 或纯文本）；5) 首页“查看 * 个新的或更新的话题”banner 改为真实轮询并用 topic_ids 增量插入（Mock 下仍可模拟）。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机验收首页/分类页：头像/分类图标/标签 pill 是否都显示；切换“最新/热门/排行榜 + 默认/创建时间/点赞…”列表是否真的变化；标签搜索是否可搜到并显示话题数；滚动离顶后是否出现“查看 X 个新的或更新的话题”且点击后能插入到 pinned 之后并回到顶部`

## 2026.03.14

- **21:11** `fix(network): 修复 Cloudflare 手动验证“显示验证成功但不返回”的卡死：ArkWeb 的 WebCookieManager 无法稳定读到 HttpOnly 的 cf_clearance，导致 CfChallengePage 轮询永远检测不到。改为在 CfChallengePage 注入 fetch 拦截，捕获 /cdn-cgi/challenge-platform/.../rc/... 调用并由 NetworkKit 代发 rc 请求、同步 Set-Cookie→CookieStore；一旦 CookieStore 写入新 cf_clearance 即自动 pop 返回并让原请求继续重试。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机再次触发 403→验证：完成后应立即自动返回原帖子页并正常加载；同时前台续期仍可用（10~15 分钟后刷新不应频繁弹验证）`
- **20:36** `feat(network): 游客态切真实后端（linux.do/Discourse）：关闭 Mock 默认走真实接口；新增 CookieStore 持久化与 HttpClient 自动 Cookie 同步（Cookie/Set-Cookie）；实现 Cloudflare 403 自动弹验证页、验证通过后自动重试；前台隐藏 Web+Turnstile 自动续期 cf_clearance；SearchPage 改真实搜索三分区（话题/帖子/用户）+ 分页；TopicDetail 支持 postNumber 跳楼；UserProfile 接 /u/{username}.json + /summary.json 渲染 Summary。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你按测试清单真机验证 CF（首次 403→验证→自动重试、前台 10~15 分钟续期）与搜索/跳楼/他人主页 Summary；若无 bug 我再按规范 commit+push 到 Gitee`
- **17:58** `style(ui): 针对你反馈“底部悬浮导航栏看不出变化”，将 FloatingTabBar 的模糊实现从 backgroundBlurStyle 切换为 backgroundEffect（实时模糊）并关闭系统自适应：backgroundEffect({ radius: 64, saturation: 2.2, brightness: 1.08, color: tab_glass_tint, policy: ALWAYS_ACTIVE, inactiveColor: tab_glass_tint }, { disableSystemAdaptation: true })，保持“背景层模糊 + 内容层不模糊”的分层结构，提升后景文字/色块经过底栏时的可见模糊度。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机慢速滚动，让任意话题行/蓝色 banner 经过底栏区域，对比是否能明显看到“后景变糊”；若仍不明显，我再加一键“模糊可证伪对比底图/开关”让效果一眼可判定`
- **17:08** `style(ui): 参考华为论坛“导航栏悬浮效果”示例，优化底部悬浮胶囊 TabBar 玻璃材质：改为 Stack 分层（背景层 backgroundBlurStyle(BlurStyle.Thin)+tint，内容层 Row 图标/文字不参与模糊），并补齐 BackgroundBlurStyleOptions（policy/inactiveColor）作为模糊失效兜底，提升“悬浮玻璃+后景文字变糊”的稳定性。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机对照 CelesMark X/安卓截图验收底栏：模糊强度、通透感、滚动时后景文字是否更明显变糊；若仍偏弱我再给一版可切换 blurStyle/scale 的调参开关`
- **16:22** `revert(ui): 按你的反馈回退底部 TabBar“液态玻璃增强”两处改动：恢复 Index 的 HdsTabs 继续隐藏默认 TabBar（barHeight(0)），FloatingTabBar 回到 backdropBlur(25)+backgroundBlurStyle(BlurStyle.BACKGROUND_THIN) 的自绘玻璃方案；同时撤销 tab_glass_mask/tab_glass_tint/tab_glass_stroke/tab_glass_shadow 的调参与新增颜色资源。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你确认接下来是继续基于回退版做“可证伪”的真机模糊诊断（我会先加一条高对比测试底图/文字让模糊一眼可见），还是先不做 iOS 玻璃材质了`
- **15:50** `style(ui): 调低 HdsTabs 渐变模糊 maskColor 的不透明度（tab_glass_mask: #CC→#66），避免底部 blur plate 过白洗掉细节，让“后景文字变糊”的对比更容易被看见。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机再看底栏区域（尤其列表文字经过底栏时）是否更容易观察到模糊；若仍看不出，我再把 mask 再降一点并额外叠一层轻噪点纹理做 iOS 玻璃颗粒感`
- **15:48** `style(ui): 引入华为 HdsTabs 官方 barOverlap + barBackgroundStyle 作为底部 blur plate（增强“能看到后景文字变糊”的玻璃效果）：Index 的 HdsTabs 开启 barOverlap(true) 并设置 barHeight 覆盖胶囊所在底部区域；按官方约束移除默认 blur（barBackgroundBlurStyle(BlurStyle.NONE)），改用 barBackgroundStyle(maskColor/maskHeight) 做渐变模糊；同时将 HdsTabs 默认 TabItem 隐藏并禁用点击，底栏仍由自绘 FloatingTabBar 展示。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机把列表滚动到“话题行/文字压在底栏区域”时，应能明显看到文字在胶囊附近变糊；若仍不糊，说明设备/系统层禁用了实时模糊，我再给一版纯图片噪点+透明材质的降级拟态方案`
- **15:10** `style(ui): 强化悬浮胶囊 TabBar 的“可见模糊”玻璃材质：将模糊实现改为 backgroundEffect(radius=64, saturation=2.2, brightness=1.08) 并禁用系统自适应（disableSystemAdaptation=true），确保能看清“后景文字被模糊”的实时效果；同时把阴影拆为独立底层（shadow layer），避免与实时模糊同层导致只剩透明不生效；下调 tab_glass_tint 透明度让模糊层更通透。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机确认底栏现在能明显看到“文字/列表在胶囊下变糊”；若仍不糊，我再继续增大 radius、提高对比（调 brightness/saturation）并补一版纯 HdsTabs barOverlap + barBackgroundStyle 的官方实现对照`
- **14:49** `style(ui): 修复悬浮胶囊 TabBar “上下半部割裂”：移除中段高光叠层，将高光/渐暗收敛为仅压在顶部/底部边缘（8vp）的轻微叠层，使材质更均匀、更贴近华为 HdsTabs 模糊示例的统一玻璃观感。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机看底栏是否不再出现明显“上亮下灰”的分层；若仍能看出层次分割，我再继续下调高光/渐暗 alpha，或改成更平滑的渐变遮罩实现`
- **14:29** `style(ui): 优化底部“悬浮胶囊 TabBar”玻璃质感：调整 tab_glass_tint/stroke/shadow 透明度以贴近页面底色；背景层改为 backdropBlur(38)+BlurStyle.COMPONENT_THICK 增强动态模糊通透感；新增顶部高光与底部渐暗叠层，提升 iOS“液态玻璃”观感与悬浮层次。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机对比两页（首页/我的）底栏是否不再“发灰/违和”，滚动时是否能明显看到内容在胶囊下被动态模糊；若仍偏磨砂或边缘高光过强，我再细调 blur 半径与高光/阴影 alpha`

## 2026.03.13

- **20:22** `fix(ui): 修复首页底部“永久留白”导致玻璃模糊不明显：撤掉 TopicListPage/ProfilePage 根容器的 bottom padding（会缩小内容区并露出白底），改为仅在滚动内容末尾预留空间（TopicListPage List 尾部 spacer、ProfilePage Scroll content padding），让列表可滚动到悬浮胶囊 TabBar 背后，恢复“能模糊看到内容”的液态玻璃观感。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机确认底栏 blur 是否恢复；若仍偏“像磨砂”不够通透，我再调 tab_glass_tint/stroke 的 alpha 与 blurStyle 强度，并补高光渐变层`
- **20:05** `feat(ui): 底部 TabBar 升级为“悬浮胶囊液态玻璃”（左右留白+大圆角）：Index 里隐藏 HdsTabs 默认 TabBar（barHeight=0），改为自绘 FloatingTabBar 覆盖在内容之上（BlurStyle.COMPONENT_THICK + tint/stroke/shadow 资源）；并新增 LayoutConstants 统一管理胶囊栏尺寸常量。fix(ui): 全局开启沉浸式窗口（内容延伸到状态栏下），首页/个人页等补齐顶部安全区 padding；首页/个人页补齐底部 reserved padding 避免列表/按钮被悬浮胶囊遮挡，首页 FAB 按系统底部安全区+胶囊高度动态避让。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机确认“悬浮胶囊”是否达到目标（宽度/圆角/高光/阴影/模糊强度）；确认首页列表底部与 FAB 不被遮挡；确认个人页下滑时顶部黑色横杠是否已消失（若仍存在我再继续定位具体阴影/背景来源）`
- **19:06** `feat(ui): 底部 TabBar 启用“悬浮液态玻璃”：Index 的 HdsTabs 打开 barOverlap，并按官方约束接入 barBackgroundBlurStyle(BlurStyle.NONE)+barBackgroundStyle(渐变模糊 maskColor/maskHeight)；同时将选中态胶囊色提取为资源 app.color.tab_selected_pill。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 你真机确认底栏是否已呈现“玻璃模糊+内容可透”观感；若列表底部被遮挡或想做“左右留白胶囊悬浮”，下一步补齐各 TabContent 底部 padding 并尝试 barWidth/间距方案`
- **18:43** `docs(research): Playwright 查证 UIDesignKit 的 HdsTabs 支持 barOverlap + barBackgroundBlurStyle/barBackgroundEffect + barBackgroundStyle(渐变模糊)；可用于实现底部 TabBar 悬浮叠加与“液态玻璃”效果。next: 若你确认要在项目里落地，下一步在 Index 打开 barOverlap 并按官方约束接入 barBackgroundStyle/BlurStyle，再真机调 barWidth/圆角/边框高光，避免列表底部被遮挡`
- **17:39** `refactor(ui): 全面接入 HDS(UIDesignKit) 路由与页签容器：Index 改用 HdsNavigation + HdsTabs；路由子页统一用 HdsNavDestination（替换 NavDestination），保持 NavPathStack 路由栈不变。build: hvigorw assembleHap BUILD SUCCESSFUL. next: 继续推进 HDS 渐进重构（首页/分类/他人主页 TabRow → HdsTabs；筛选/标签等 Sheet 标题栏与按钮 → HDS），并真机回归确认 UI 无回退/无双返回`
- **15:56** `fix(ui): 个人中心游客态（ProfilePage）按 Flutter 结构重排：首屏统一外层 padding(16/12)、头像占位 72、卡片/按钮间距对齐（Header→24→AboutCard→32→登录按钮→48）；TopBar 下滑后用 shadow 模拟 AppBar “scrolled under” 阴影变化，并在未登录时隐藏右侧占位按钮，避免与安卓不一致。next: 你真机对照安卓验收个人中心游客态：首屏一张 + 下滑一小段一张（看 TopBar 阴影/标题区变化），若有间距/字号/阴影偏差我继续逐像素微调`
- **14:24** `fix(ui): SearchPage 高级搜索面板分类分组“引导线 height=100% 拉满”导致子分类被推到屏幕底部/出现大空白——改为按子分类数量估算引导线高度，并统一 Wrap/分组为左对齐（更贴近 Flutter/安卓）。next: 你真机复测高级搜索 Sheet：状态/时间范围/分类/标签整体是否顺序正常；重点确认“资源荟萃→网盘资源”“运营反馈→公告/活动/悬赏”不再被拉开大空白，且引导线高度观感正常`
- **00:30** `fix(ui): 修复他人主页流体动效背景“滑动时闪烁”：UserProfilePage 固定背景 Canvas 的渲染高度（用 clip 裁剪），避免折叠高度变化触发 Canvas 重建/重置；AnimatedGradientBackground 移除 clearRect 并将 blob/highlight 改为 arc 局部填充，降低滚动时重绘负担并避免“先清空后绘制”的可见闪帧。style(ui): “6分钟前”胶囊对齐安卓（背景 alpha≈0.15、文字/图标 white70、SymbolGlyph 强制单色渲染），去除黄色违和感。next: 你真机复测快速滚动是否仍闪；若仍闪，下一步引入 OffscreenCanvas 双缓冲或 RenderNode(ArkGraphics2D) 彻底消除撕裂/闪帧`
- **00:05** `feat(ui): 他人主页 100% 对齐安卓背景：新增 AnimatedGradientBackground（Canvas 径向渐变色块流动 + 高光叠加）复刻 Flutter 流体动效背景，并接入 UserProfilePage 头图；Tabs 行改为 layoutWeight=1 均匀铺满屏宽。fix(ui): 修复“滚动到最底再上滑时 Tabs 与内容间偶发大留白”——ScrollOffset 增加异步纠偏（scroller.currentOffset 同步），避免回弹/边界导致偏移漂移。docs: 同步更新 spec/checklist/UI_REPLICA_STATE。next: 你真机对照安卓验收：流体动效是否顺滑、颜色/速度是否一致；若仍有留白或 Tabs 抖动，录屏+机型信息我继续收口`

## 2026.03.12

- **23:10** `fix(build): 修复 ArkTS 编译硬错误 arkts-no-types-in-catch：移除 WindowManager/EntryAbility 的 catch 子句类型标注；同时消除 px2vp 废弃警告：改为从 mainWindow.getUIContext().px2vp 做 px→vp 转换（用于 getWindowAvoidArea 的安全区高度换算）。next: 你重新 hvigor 构建确认：ERROR/WARN 清零；若仍提示系统栏/沉浸 API 不存在（不同机型/SDK 差异），把报错方法名与行号贴我再做兼容分支`
- **23:06** `docs: 对齐技术选型真值：明确 UI 优先 @kit.UIDesignKit(HDS)、网络主方案为 @kit.NetworkKit(http)；补充 Playwright/Context7(MCP) 查证规则（以 Playwright 最新网页为准）；同步修正 spec.md 的 dio 映射并在 checklist 增加 Kit 约束。next: 将本轮决策与进度摘要写入 Nocturne Memory 的 writer://project_notes/fluxdo_hmapp，并继续按真机/截图收口游客态 UI 差异；随后进入“游客后端对接（只读）”`
- **23:00** `fix(ui): 对齐安卓“沉浸式”系统栏：WindowManager 支持 setWindowLayoutFullScreen/setWindowSystemBarProperties（含 statusBarContentColor/navigationBarContentColor）与窗口背景色；UserProfilePage 进入时开启全屏布局并将状态栏设为透明叠加头图、导航栏白底，顶部/底部安全区高度用 getWindowAvoidArea 计算，避免出现“状态栏/导航栏灰条与页面背景不一致”。同时将头图底色升级为 linearGradient（更接近安卓渐变氛围）。docs: 同步更新 spec/checklist。next: 你真机对照安卓验收：状态栏区域是否已显示头图背景且图标为白色、底部导航栏是否白底无灰条；若要完全复刻 Flutter 的 AnimatedGradientBackground（流体动效），下一步用 ArkUI Canvas/自绘实现或先用静态资源图兜底`
- **22:37** `fix(ui): 统一他人主页底部导航条/安全区背景：将 UserProfilePage 的 NavDestination 背景恢复为 app.background（白底），避免底部导航条区域在手势/透明导航栏下透出头图紫色造成“上下颜色不一致”。头图区域仍由 FlexibleSpace 自身底色/遮罩负责，状态栏颜色由 WindowManager 设置。next: 你对照安卓确认：底部不再出现紫色条，列表底色与导航条区域一致；若要“状态栏透明显示渐变”，再做沉浸式 fullScreen + safeArea/insets`
- **22:25** `fix(build): 修复 HttpClient 编译硬错误 arkts-limited-throw：禁止 throw 任意 Object，统一改为 throw new Error(...)（错误细节仍写入 hilog）。next: 你本地重新 hvigor 构建确认 BUILD SUCCESSFUL；若仍有 ArkTS 规则类报错，把完整日志贴我继续修`
- **22:20** `fix(build): 消除 HttpClient “Function may throw exceptions” 警告：对 httpRequest.request(...) 显式 catch 并记录失败日志后再抛出，满足 ArkTS 对可抛异常函数的特殊处理要求。next: 你本地 hvigor 构建确认 WARN 是否清零；若仍提示其它可抛异常位置，把行号贴我继续收口`
- **22:16** `fix(build): 修复 ArkTS 编译失败：移除 WindowManager/EntryAbility 中的 unknown 类型断言（arkts-no-any-unknown），并删除无效的 Alignment.CenterStart 常量引用，恢复 hvigor 可编译。next: 你本地重新 hvigor 构建确认 BUILD SUCCESSFUL；若仍有“系统栏 API 不存在/类型不匹配”报错，把完整日志贴我继续兼容`
- **21:58** `fix(ui): 他人主页继续对齐安卓：修复“6分钟前”胶囊未左对齐（ExpandedHeaderContent 默认居中导致）；新增 WindowManager 统一设置系统状态栏/导航栏颜色，避免顶部/底部默认灰条破坏沉浸观感；头图底色从纯黑调整为偏紫底色，并下调遮罩强度避免整体发黑（更接近安卓头图氛围，后续再补真实渐变）。next: 你对照安卓真机确认：状态栏/导航栏不再灰、6分钟前左对齐、头图不再过黑；若仍需“真正沉浸（内容延伸到状态栏下）”，我再接入 fullScreen + safeArea/insets 适配`
- **21:24** `fix(ui): 修复他人主页滚动时 Header/Tabs 错位与大面积留白：UserProfilePage 外层 Stack 与 FlexibleSpace 内层 Stack 显式设置 alignContent=Alignment.TopStart，避免默认居中导致折叠头图“悬浮在页面中间”，滚动时出现内容跑到头图上方/顶部空洞等严重错乱。next: 请真机复测他人主页：顶部不再大白屏、TopBar 固定在顶部、Tabs 吸顶稳定；若仍有状态栏安全区高度差异，再补 safeArea/insets 适配`
- **20:58** `fix(ui): 修复他人主页顶部“挖孔/安全区”露白：UserProfilePage 将 NavDestination 背景改为头图深色，并为 Scroll 增加白底承接内容，避免顶部出现大块白底空洞导致返回/操作区观感异常。next: 请在挖孔/刘海真机上确认顶部返回/搜索/更多按钮与状态栏区域观感是否符合安卓（必要时再加状态栏样式切换）`
- **20:09** `fix(build): 修复 hvigor/esbuild “Argument name clash”：SearchFilterPanel 的 @Builder 参数名避免使用 parent（改为 parentCategory），消除隐式参数命名冲突导致的打包失败。next: 你本地重新 hvigor 构建确认 BUILD SUCCESSFUL；若还有新的具体报错，把日志贴我继续修`
- **20:06** `docs(agent): 更新 hmApp/AGENT.md 的代码搜索协作策略：已知函数/类先 Grep，语义探索先 ace-tool search_context，结果不足再走 Fast-Context 完整模式（文件路径/行范围/grep 关键词/完整代码片段，输出约 40KB）；并补充默认参数、禁用项与子代理启用边界。next: 后续凡是代码定位/排查类任务，先按这套搜索顺序执行，再决定是否需要更深层探索`
- **20:00** `fix(build): 修复 ArkTS 编译失败：移除 @Builder/UI 语法块内的 const 局部变量声明（SearchFilterPanel/ActiveSearchFiltersBar/UserProfilePage），并放到普通 helper 方法计算；调整 Chip/FilterChip/ErrorView 的参数类型以兼容 Resource；搜索页高级筛选图标改用已存在的 sys.symbol.line_3_horizontal 作为兜底，避免 unknown resource。next: 你本地重新 hvigor 构建确认 BUILD SUCCESSFUL；若仍有 symbol/资源缺失报错，把日志贴我继续修`
- **18:54** `feat(ui): 搜索/高级筛选与他人主页三态收口：新增 SearchFilter 模型（对齐 Flutter，支持 Discourse query 拼装）；SearchPage 补齐最近搜索持久化、激活条件条、高级搜索 Sheet（状态/时间范围/分类/标签/重置/应用）、“搜索更多标签”与自定义日期范围；UserProfilePage 实现 ScrollOffset 驱动三态折叠 + Tabs 吸顶 pinned + 收起态标题点击回顶。docs: 同步更新 spec/tasks/checklist/UI_REPLICA_STATE，并在 AGENT.md 增加“UI 优先 HDS(UIDesignKit)+网络优先 NetworkKit”的强制规则。next: 真机/模拟器验收游客态 SearchPage/UserProfilePage 的像素细节；继续收口个人中心游客态首屏与下滑标题栏变化；随后进入“游客后端对接（只读：topic list/detail/search/user summary）”`
- **16:08** `feat(ui): 补齐游客态缺失页面闭环：新增 SearchPage（首页搜索栏/分类页/详情页搜索按钮进入），离线 Mock 复用 latest topics 做本地过滤展示搜索结果；新增 UserProfilePage（详情页点头像/用户名进入），补齐顶部深色头图 + 统计 + Tabs + 总结分区骨架，整体图标全使用 sys.symbol。next: 请补充安卓端“搜索页”和“他人主页（总结 tab）”截图，我再按截图逐像素对齐（字号/间距/圆角/阴影/颜色），并继续对齐个人中心游客态首屏与下滑标题栏变化`
- **15:02** `fix(ui): 对齐安卓详情页细节：Header 内部改为左对齐（“常规”不再居中）；底部工具栏显隐方向改为“手指上滑显示/下滑隐藏”，停止后保持上次状态；楼层回复折叠按钮仅在非首贴楼层显示，点击可展开“谁回复了该楼层”的列表；楼主在回帖楼层名旁显示“主”标识。并同步完善 Mock：去掉 pinned.enabled 的 system small_action（仅 closed 时才生成），补充 reply_to_post_number/reply_to_user 用于离线验收。next: 继续按你提供的同话题左右对照截图逐像素微调详情页三态（标题/间距/阴影/进度胶囊位置），并把“回复折叠列表/回复指向 pill（↩+头像）”做得更贴近安卓样式`
- **14:12** `style(ui): 继续“系统图标化”补漏：ErrorView 错误提示页的 ⚠️/🔄 emoji 图标替换为系统 SymbolGlyph（info_circle / arrow_clockwise），与全局图标风格保持一致。next: 让你在真机/模拟器对比安卓截图确认详情页三态的观感（TopBar 白底+Divider、small_action 行、进度胶囊），再按截图逐像素调数值`
- **14:08** `style(ui): 详情页进一步“干净化”微调：TopBar/页面底色统一改为 surface（更贴近安卓白底列表），滚动后 TopBar 补一条轻 Divider；进度胶囊文案改为 “1/总楼层”（去掉空格更紧凑）；TagBadge 图标色改用 app.primary 资源，避免硬编码紫色导致风格跳脱。next: 基于你发的“同话题鸿蒙/安卓左右对照”继续逐像素微调详情页三态（标题阈值/间距/阴影/进度胶囊高度），并补齐 small_action 的 actionCode 图标与文案映射覆盖`
- **13:26** `style(ui): 继续对齐安卓/Flutter 的“干净布局”：详情页 Header 改为 surface+divider 的信息区结构，标题补齐 closed/accepted 图标；small_action 行按 Flutter 样式重写（圆形图标容器 + 描述/时间 + 右侧头像 + 底部分割线），去掉整条灰底；首页 Tab 行右侧“≡ 最新”紧凑筛选入口提升最低不透明度，解决折叠态偏透明看不清。next: 基于你提供的同话题安卓/鸿蒙对比截图，继续逐像素微调详情页三态（Header 字号/间距/阴影、进度胶囊与底部工具栏距离底部的数值），并补全 small_action 的 actionCode 文案/图标映射覆盖面`
- **12:19** `style(ui): 全局 UI 图标系统化：将首页/分类页/详情页/个人中心/话题卡片/标签控件/胶囊控件中的 emoji/字符图标统一替换为系统 SymbolGlyph（sys.symbol.*），整体观感更“干净”；并修复 SymbolGlyph 不支持 textAlign 的编译问题。next: 真机对比安卓截图，继续逐像素微调详情页三态（头部信息区/进度胶囊/底部工具栏的间距与阴影），再统一校准各处 SymbolGlyph 的字号与颜色层级`
- **11:17** `fix(ui): 详情页浮层三态对齐：进度胶囊改为常驻显示（不再依赖底部工具栏显示），并随滚动首个可见楼层更新；底部工具栏按滚动方向显隐（向上滚动显示/向下滚动隐藏，delta 阈值 50 防抖），与进度胶囊位置联动；顶栏与底部工具栏图标替换为系统 SymbolGlyph，移除 emoji 以提升“干净”观感。next: 真机对比安卓详情页三态截图，继续逐像素微调头部信息区（字号/间距/阴影/按钮位置）与进度胶囊/底部工具栏距离底部的数值`
- **10:21** `fix(ui): 修复话题详情页进度胶囊楼层不随滚动更新：移除 ListItem onAppear/onDisAppear 可见楼层 Map，改用 List.onScrollIndex 以首个可见项驱动 currentPostIndex；首页底部 TabBar 随 Header 折叠进度隐藏/显示（TopicListPage→Index 回调同步）；首页 FAB 改为仅登录态显示并按滚动方向切换“+ / 刷新”（使用 sys.symbol 图标），游客态隐藏；紧凑筛选入口（≡ 最新）提升对比度与背景，解决看不清问题。next: 真机确认详情页楼层胶囊随滚动递增、首页底栏隐藏动画与登录态 FAB 切换行为；继续逐像素精修详情页三态（字号/间距/阴影/位置）`
- **00:31** `feat(ui): 游客态 UI 冲刺：详情页补齐头部信息区（大标题/分类+tags pills/回复·浏览·时间/“常规”胶囊），并按滚动触发显示底部工具栏+进度胶囊；游客态隐藏回复 FAB。首页加载态替换为 TopicListSkeleton 骨架屏，并新增右下浮动“刷新”按钮（回顶+复位Header+刷新）。同时扩展 Mock topic detail JSON 支持 views/tags/category_id/created_at/last_posted_at，保证离线验收字段齐全。next: 基于你提供的详情页三态截图逐像素微调（字号/间距/位置），并补齐首页骨架屏/浮动按钮与个人中心游客态的安卓对齐截图`

## 2026.03.11

- **22:21** `style(ui): 收紧首页折叠态 Tab 行右侧“紧凑筛选入口”（≡+文字）的字号/内边距与右侧 ☰ 的间距，更贴近安卓“≡ 最新 ☰”排布。next: 真机逐像素对齐首页 Header 折叠态（Tab 行高度/紧凑筛选入口）并继续精修分类话题页（标题栏/筛选栏/列表间距/图标）`
- **21:52** `fix(ui): 调整 Mock“查看新话题”banner 触发逻辑：触发时不再要求仍处于非顶部，且下滑折叠 Header 阶段也可 arm 定时器，避免回到顶部后看不到 banner。next: 真机确认按“下滑离开顶部→等待 2~3 秒→回到顶部”可稳定出现 banner，并继续精修分类话题页（标题栏/筛选栏/列表间距/图标）`
- **21:18** `feat(ui): 补齐首页“查看新话题”banner（Mock 离线自动模拟触发 + 点击插入并回顶），并对齐 pinned 紧凑行样式；浏览分类网格图标改为 logo/锁/色点兜底，子分类菜单补齐圆角/阴影/图标行布局；修复 Category uploaded_logo.url 解析与话题列表 ForEach key，避免插入后复用错位。next: 真机对比 banner/pinned/分类菜单与安卓截图细节（颜色/间距/阴影/行高），并继续精修分类话题页（标题栏/筛选栏/列表间距/图标）`
- **19:57** `feat(ui): 实现首页 Header 停手吸附 snap（松手后仅停 0/100），并将折叠态 Tab 行高度收紧至 36；Tab 行右侧“紧凑筛选入口”改为图标+文字风格；搜索提示改为资源引用。next: 真机确认首页 Header 松手只吸到 0/100 且折叠态“≡ 最新 ☰”间距/字号与安卓截图一致；随后补齐“查看新话题”banner + pinned 行样式`
- **18:34** `fix(ui): 修复排序栏标签 chips 仍“居中留空”：为 Scroll 内 Row 增加 constraintSize(minWidth: '100%')，让内容容器至少占满剩余宽度并保持左对齐，同时超出时可横向滚动（首页/分类页统一）。next: 真机确认 chips 紧贴“默认”右侧，不再出现中间空白`
- **18:18** `fix(ui): 修复标签选择 Sheet“完成”按钮对齐（去掉多余右侧占位）；修复添加标签后 chips 在排序栏居中留空问题（强制左对齐）；抽离 TagControls（TagChip/TagSelectionSheet/默认标签列表）并在分类话题页复用，补齐分类页标签筛选可打开且与首页一致。next: 真机确认首页/分类页添加 1~3 个标签后 chips 紧贴“默认”右侧且不留空，标签选择 Sheet 右上角“完成”对齐正确`
- **18:01** `fix(ui): 统一关闭 Sheet 右上角系统关闭叉（showClose=false），避免“浏览分类”标题栏“编辑”与叉重叠；保留下拉手势关闭，符合安卓交互。next: 真机确认浏览分类/选择标签弹层顶部不再出现叉，且下拉可顺畅关闭`
- **17:49** `perf(ui): 全面收紧组件占用：PillMenuButton 默认尺寸对齐安卓（padding/圆角/高度），下拉菜单统一压缩 MenuItem 高度减少遮挡；TopicListPage Tab 字号与分类/标签 Sheet 字号、网格尺寸同步紧凑化；分类页/详情页 TopBar 返回与更多按钮字号收敛。next: 让你在真机上对比两张截图确认（最新/默认下拉覆盖、Tab 行高、分类网格 48x48、标签选择列表行高），再按反馈做逐像素微调`
- **16:34** `refactor(ui): 将“最新/默认”下拉胶囊与“+按钮”封装为通用组件 PillControls（首页/分类页复用）；对齐安卓“默认”下拉选项（默认/活跃度/创建时间/点赞数/浏览量/回复数/参与者）；菜单容器加圆角与阴影更接近安卓。next: 结合安卓截图逐像素精修分类话题页（最新/默认/+ 的尺寸与间距、菜单宽度/圆角/阴影），再继续推进首页 Header snap 与“查看新话题”banner`
- **15:51** `style(ui): 分类话题页“+”按钮样式调整为 +💬，更贴近安卓“创建话题”入口。next: 基于安卓截图逐像素精修分类话题页（最新/默认/+ 的尺寸与间距、图标样式），再继续推进首页 Header snap 与“查看新话题”banner`
- **15:46** `fix(ui): 修复所有 NavDestination 页面出现“双返回/重复顶栏”：对使用自定义顶部栏的页面隐藏系统默认 TitleBar/ToolBar（TopicDetail/CategoryTopic）。next: 基于安卓截图逐像素精修分类话题页（最新/默认/+ 的尺寸与间距、图标样式），再继续推进首页 Header snap 与“查看新话题”banner`
- **15:13** `style(ui): 分类话题页补齐“最新/默认/+”筛选行与点击反馈，占位搜索/+按钮不崩；列表上边距微调并加分割线，更接近安卓。next: 结合安卓截图逐像素对齐分类话题页（字体/间距/按钮样式），再继续推进首页 Header snap 与“查看新话题”banner`
- **14:54** `fix(ui): 分类 Sheet 点击分类/子分类改为跳转到分类话题页（独立页面）；修复“编辑我的分类”可添加网格末行被拉伸留空；修复 Sheet 右上角“编辑/完成”与系统关闭按钮重叠。next: 按安卓截图精修分类话题页（标题栏/筛选栏/列表间距），并继续推进首页 Header 三态折叠细节与 snap 吸附`
- **13:00** `docs: 更新 hmApp/AGENT.md：新增“会话协议（快照/日志/三段清单）”、spec/tasks/checklist 真值用法、Nocturne Memory(MCP) 使用规范与提示词；更新华为官方参考链接到 2026 可用入口（API 22）。next: 后续每轮开发严格按会话协议输出三段清单，并将关键决策沉淀到 writer://project_notes/fluxdo_hmapp（提要+指针）`
- **11:43** `style(ui): 对齐首页 Header（Tab 标题/排序筛选 pill/+🏷 按钮）与分类 Sheet 标题/网格尺寸，更接近安卓截图。next: 继续补齐首页三态折叠缺图后的逐像素对齐、Header snap、“查看新话题”banner 与 pinned 行样式`
- **11:24** `fix(ui): 修复首页分类按钮打不开（Sheet 统一绑定）；标签“+🏷”按钮贴近排序筛选；分类管理 Sheet（浏览/编辑/子分类菜单）结构与样式对齐安卓；Mock 分类名称/层级对齐安卓。next: 补齐首页三态折叠细节（含吸附 snap）、“查看新话题”banner、分类图标/TopicCard 细节逐像素对齐`
- **02:22** `feat: 引入全局 Mock 数据模式（默认开启），离线渲染首页/详情/个人中心；实现首页 Header 两段折叠、分类管理与标签选择 Sheet，并修复相关编译问题`
- **00:04** `refactor: 将UI代码中的重复颜色、尺寸和字符串提取到资源文件中，并抽离重复UI为组件，提高复用性。同时增加构建CHANGELOG.md机制`

## 2026.03.10

- **23:52** `feat: 完善首页与分类页UI弹窗联动`
- **23:41** `refactor: 全部UI组件100%复刻安卓版`
- **23:35** `refactor: 100%复刻安卓版UI布局`
- **23:23** `feat: 实现项目基础架构和核心页面（阶段一至六）`





